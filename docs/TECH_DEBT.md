# Technical Debt / P2·P3 Backlog

## P3 — Ops / KIS / Paper Review Backlog

### KIS read-only / tiny-live
- ~~Add manual KIS read-only smoke ops entrypoint.~~ Done: `ops/run_kis_read_only_smoke.py` (explicit `--run` opt-in; no orders).
- Verify KIS OpenAPI endpoint paths and TR IDs in `src/broker/kis_client.py` against the official KIS Open API documentation before real read-only smoke.
- Keep `StdlibKisHttpTransport` as a manual smoke scaffold only. Do not connect it to scheduler/runtime default paths without a separate review.
- `broker.kis_read_only.enabled=true` is not an automatic trigger. Read-only smoke must remain explicit/manual.
- Tiny-live actual order submission remains intentionally unimplemented. Do not add `submit_tiny_live_order()` / `place_tiny_live_order()` before a separate manual rehearsal.

### KIS real-time market monitor (fast loop — GitHub Issue #1)
- ~~RTM-1: fixture-first market-event domain + transport contract.~~ Done: `src/market_data/models.py` (normalized trade/quote/heartbeat + provider sequence, discriminated union), `src/market_data/protocols.py` (read-only async `MarketEventSource`), `src/market_data/kis_ws_parser.py` (parses only `provider_contract="kis-ws-fixture-v1"`; fail-closed; per-channel sequence dup/decrease/gap detection; no raw-frame/credential leak). Network-free, broker-free, ledger-free, scheduler-free.
  - **Official KIS WebSocket transport and field mapping remain UNVERIFIED and are deferred to RTM-6.** The `kis-ws-fixture-v1` envelope is a declared fixture contract, NOT a claim about real KIS frame layout / field positions. Do not build a real WS client against guessed offsets before RTM-6 verifies the official contract.
  - ~~RTM-2: latest market-state store (read-only, in-memory).~~ Done: `src/market_data/latest_state.py` (`LatestMarketStateStore`). Per-`(market, symbol)` independent trade/quote slots; `(provider, channel)` stream-identity guard (cross-stream sequence comparison rejected as `STREAM_MISMATCH`, never auto-reset); fail-closed on decreasing sequence / `received_at` regression / event-time regression / future event (`FutureMarketEventError`); explicit `now` injection (naive rejected); event-time freshness default 10s; typed `ApplyResult` (`APPLIED`/`DUPLICATE`/`OUT_OF_ORDER`/`STREAM_MISMATCH`) with non-APPLIED ⇒ zero state change; `peek`/`require_fresh(required=...)`/`peek_liveness`/`require_live`; heartbeat liveness kept in a separate `(provider, channel)` map (received_at-based, does not mutate trade/quote slots); `threading.Lock` atomic check-and-update; immutable frozen snapshots.
    - **Scope decision (operator, 2026-06-07):** Issue #1's RTM-2 acceptance list included "market-hours checks", but the repo has no trading-calendar infrastructure, so building a KRX/US session+holiday calendar now would be guess-based. **Market-hours is deferred out of RTM-2 to RTM-3 (monitor) / RTM-7 (4x/day scheduler).** RTM-2 ships freshness + ordering + immutable snapshot only. This deviation from Issue #1 is recorded here for traceability; mirror into an Issue #1 comment if/when desired.
    - Reconnect/session sequence-reset handling is intentionally NOT in RTM-2 (a higher incoming sequence from a *different* stream is rejected, not treated as a reset). Explicit reset API is deferred to RTM-3.
    - **RTM-2 hardening (2026-06-07):** trade/quote now also reject future `received_at` (matches heartbeat; a future `received_at` previously poisoned the slot and blocked normal events); `require_fresh` rejects an empty `required` set (was fail-open) and validates unsupported event types before reading state.
  - ~~RTM-3: fake-transport monitor daemon (asyncio, bounded replay).~~ Done: `src/market_data/replay_source.py` (`ReplayMarketEventSource`, in-memory finite event iterable, deterministic EOF, no filesystem/network) and `src/market_data/monitor.py` (`MarketMonitor`: asyncio orchestration; state machine IDLE/CONNECTING/RUNNING/BACKING_OFF/EXHAUSTED/STOPPED; pure deterministic `ReconnectPolicy` backoff with injected clock+sleep; **explicit `store.reset_stream` only on confirmed reconnect — no automatic sequence reset**; per-event `ApplyStatus` + `future_event_error` counts; normalized `MonitorEvidence` that never carries raw frame/token/account/exception repr; graceful cancellation re-raises `CancelledError`; typed `MonitorExhaustedError` on attempt exhaustion; bounded by `max_events`/EOF). `ops/run_kis_market_monitor.py` is the bounded CLI (loads JSONL fixtures via the RTM-1 parser; `--validate-only` writes no files, summary to stdout). New per-file import guard: `monitor.py` may import `asyncio`, `latest_state.py` may import `threading`; socket/websocket/http/broker/ledger/etc remain globally forbidden across `src/market_data`. No real KIS WebSocket, no trigger engine, no PaperBroker, no LLM, no ledger.
    - **Market-hours still deferred (now to RTM-7).** RTM-3 is a bounded fixture/fake-transport monitor with no trading action, so exchange-calendar logic adds no value here. Authoritative KRX/US session+holiday scheduling moves to RTM-7's 4x/day scheduler.
    - **RTM-3 hardening (operator, 2026-06-07) — CLOSED.** Six design gaps in the initial RTM-3 commit were verified against the code and closed: (1) `store.reset_stream` is deferred to the new epoch's *first received event* (apply-time), not reconnect-start — so a new source that dies before yielding leaves prior state intact (`test_reconnect_without_new_event_preserves_old_state`); (2) heartbeat stale watchdog (`heartbeat_watch` + `heartbeat_timeout_seconds`) detects half-dead connections and drops→reconnects; (3) `max_runtime_seconds` now bounds the *next-event wait itself* via `asyncio.wait_for`, so a silent source still stops; (4) transport errors (`source_factory`/iterator) classify as reconnectable `drop`, while internal/store/evidence defects raise typed `MonitorInternalError` and fail-closed (no backoff); (5) `ReconnectPolicy` (`jitter_ratio` removed) and monitor budget args validate in constructors (negative/zero/mismatched → `ValueError`); (6) `ops/run_kis_market_monitor.py` now has CLI tests (`tests/test_run_kis_market_monitor_cli.py`: validate-only writes nothing + summary schema, `--evidence-out` append, `--max-events` bound, malformed JSONL raises). Full suite 2676→2694.
    - **RTM-3 follow-up hardening (operator, 2026-06-07) — CLOSED.** Three further defects were reproduced (repro scripts confirmed the failures, then the fixes) and closed: (a) **heartbeat reconnect could never recover** — `_heartbeat_deadline` read the *previous epoch's* stored heartbeat via `peek_liveness`; once wall-clock advanced past `old_heartbeat + timeout` during backoff, the new epoch's `wait_for` got `timeout≈0` and stale-dropped before the healthy source could deliver its first heartbeat, so `reset_stream` never ran and a perfectly healthy connection exhausted. Fixed: while the watched stream is in `_pending_reset`, the deadline bases on `_epoch_started_at`, not the stale heartbeat (`test_reconnect_recovers_after_heartbeat_stale`). (b) **runtime-timeout misclassification** — the timeout cause was re-derived after the fact via `_runtime_exhausted()`, so with a fixed/non-ticking injected clock a `max_runtime_seconds` expiry was misread as `heartbeat_stale` and reconnect-looped; `_next_event_timeout` now returns the chosen deadline's reason and the timeout branch uses it directly (`test_runtime_timeout_fixed_clock_not_misclassified`). (c) **evidence sink errors** now wrap to typed `MonitorInternalError` (fail-closed, no reconnect), matching the documented contract, while the cancellation path suppresses sink errors so `CancelledError` still propagates (`test_evidence_sink_failure_is_internal_error`). Full suite 2694→2697.
  - ~~RTM-4a: deterministic trigger engine (latest-snapshot conditions only).~~ Done: `src/market_data/conditions.py` (`Metric` = last_trade/best_bid/best_ask/spread_bps; `Comparator` LTE/GTE; `ConditionClause` frozen with threshold validation — spread_bps ≥ 0, price metrics > 0; `evaluate_all` ALL-combine only; `metric_value` returns `None` on missing slot ⇒ fail-closed False; `rule_required_slots` → needs_trade/needs_quote), `src/market_data/trigger_engine.py` (`TriggerPlan`/`TradingPermission`/`ConditionObservation`/`TriggerSignal`/`DecisionTriggerBundle` schemas + `TriggerState`/`TriggerStatus`/`TriggerReason` typed results + `TriggerEngine`), and `src/market_data/decision_loader.py` (`load_decision_bundle` fail-closed: schema/validation/payload/decision_id/created_at/plan-match all raise `DecisionLoadError`). **Trigger-only: returns a single deterministic `TriggerSignal`, never an order/quantity/PaperBroker/ledger call (RTM-5).** Locked-in policies: (1) latest-snapshot conditions only — rolling window/MA/volume/VWAP deferred to RTM-4b; (2) conditions live in `TriggerPlan`, never inferred from `AnalysisDecision` prose; (3) first pilot `max_fires_per_decision=1`; (4) market-hours via injected `TradingPermission` (default deny), NOT a calendar (calendar → RTM-7). State machine DISARMED/ARMED/DEBOUNCING/TRIGGERED/COOLDOWN/LOCKED; debounce requires BOTH `debounce_events` AND `debounce_seconds`; rearm requires `reset_events` consecutive reset-true AND cooldown elapsed AND `fires < max`. HOLD is blocked first (never executable). Idempotency key hashes only `decision_id/plan_id/rule_set_id/market/symbol/action/activation_epoch` (NO wall-clock/price/raw payload) so the same activation yields the same key. Active-decision replace + trigger evaluation share one `threading.Lock` (atomic; older `created_at` rejected, same id+created_at unchanged); concurrency test asserts exactly one TRIGGERED under 16 racing evaluators. BUY reference_price = ask, SELL = bid. Import guard extended: `trigger_engine.py` may import `threading`; `decision`/broker/ledger/paper_loop/llm/socket/websocket/http remain forbidden (idempotency hashing is self-contained `hashlib`+`json`). Full suite 2697→2754.
    - **RTM-4a hardening (operator, 2026-06-08) — CLOSED.** A second-pass review flagged 6 items; each was reproduced against the code before fixing. Five were real and fixed; one was refuted. (1) **Snapshot identity gate (BLOCKER)** — `evaluate()` validated quote/trade freshness but never checked the snapshot belonged to the planned instrument, so a snapshot for a *different* symbol/market fired a signal carrying the plan's symbol with the foreign book's reference_price (reproduced: US/AAPL snapshot → 005930 TRIGGERED @ AAPL ask). Fixed: `evaluate` now suppresses on `snapshot.market`/`snapshot.symbol` mismatch AND on inner-slot mismatch (`quote.market/symbol`, `trade.market/symbol` must equal the plan), with no state advance (`SNAPSHOT_MARKET_MISMATCH`/`SNAPSHOT_SYMBOL_MISMATCH`). (2) **Bundle replace conflict** — `replace_bundle` keyed UNCHANGED on `decision_id`+`created_at` only, so a *different* plan/payload under the same identity was silently dropped, and same-`created_at`+different-id was replaced with no ordering judgement. Fixed with a `_bundle_fingerprint` (decision+plan canonical sha256): same identity+different content → new `REJECTED_CONFLICT`; same time+different id → `REJECTED_CONFLICT`; older → `REJECTED_OLDER`; newer → `REPLACED`. (3) **Permission time window fail-open** — model lacked a `checked_at <= valid_until` invariant and the engine never rejected `now < checked_at`, so a future-issued permission was usable now. Fixed: model invariant + engine `PERMISSION_NOT_YET_VALID` (boundary `now == checked_at`/`now == valid_until` allowed). (4) **Cooldown reset continuity** — `reset_events` requires *consecutive* reset-true ticks, but `_suppress_break` left `_reset_count` intact during COOLDOWN, so a data gap between two reset-true ticks counted as consecutive. Fixed: suppression in COOLDOWN now zeroes `_reset_count`. (5) **Plan/decision time binding** — added bundle invariants `plan.created_at >= decision.created_at` and `plan.valid_from >= decision.created_at` (a plan cannot predate its decision). (6) **Non-finite Decimal — REFUTED:** GPT claimed NaN/Infinity thresholds/seconds slip through; verified that pydantic v2 `Decimal` fields reject non-finite at the core boundary (`allow_inf_nan=False` default) on *every* Decimal field (threshold/debounce_seconds/cooldown_seconds/observation.value/reference_price) — regression tests added to lock it. Full suite 2754→2770.
    - **Deferred policy guard (not engine):** the engine intentionally generalizes `max_fires_per_decision >= 1`; the first-pilot "fire once" rule must be enforced at the RTM-5 pilot plan-construction layer (`max_fires_per_decision == 1` profile), not relaxed here. Rejecting a future-dated decision at replace-time is intentionally NOT done — future decisions are governed by `plan.valid_from` (`DECISION_NOT_YET_VALID`).
    - **Spec deviation note:** `AnalysisDecision.market` is `str` not `Market`, so `DecisionTriggerBundle` compares `plan.market.value == decision.market`. `debounce_seconds`/`cooldown_seconds`/`threshold` are `Decimal` (coerced from str/int) for exact comparison.
    - **Not in RTM-4a (deferred):** KIS WS live read-only (RTM-6); PaperBroker/OrderIntent/QuantityResolver/ledger write + `PaperLoopInput` linkage (RTM-5); 4x/day scheduler + authoritative market-hours calendar (RTM-7); rolling/tick/volume/VWAP indicators (RTM-4b); any LLM in the fast loop.
    - ~~RTM-4b.1a: bounded raw trade history store.~~ Done: `src/market_data/rolling_window.py` (`RollingTradeHistoryStore` keeps one bounded raw trade history per `(market, symbol)`; **spec-agnostic** — no indicator math, window selection, or readiness; those are RTM-4b.1b). `RollingRetentionPolicy(hard_max_events, hard_max_age_seconds)` is a caller-mandated memory safety cap (no hidden defaults); `hard_max_age_seconds` (Decimal) is validated to be representable in whole microseconds and converted via integer microseconds (NOT `float(...)`) for determinism. `observe()` returns a typed `RollingObserveResult` (APPLIED/DUPLICATE/OUT_OF_ORDER/STREAM_MISMATCH) mirroring RTM-2 ordering: `sequence ==`→DUPLICATE, `<`→OUT_OF_ORDER, received_at/trade_at regression→OUT_OF_ORDER, **same trade_at + increasing sequence→APPLIED**; a future tick is fail-closed as OUT_OF_ORDER (RTM-2's latest store blocks it first with `FutureMarketEventError`; differs by design — observe keeps an exception-free typed contract). A *different* `(provider, channel)` is rejected with STREAM_MISMATCH and **no history change** (no automatic discard); new streams are accepted only after an explicit `reset_stream()` (matches RTM-2/RTM-3 reset contract). `reset_stream` clears samples but preserves continuity metadata (`was_ever_observed`, `continuity_epoch`, `epoch_start_reason` INITIAL/EXPLICIT_RESET) so RTM-4b.1b can distinguish MISSING vs DISCONTINUOUS. `TradeHistorySnapshot` is immutable (frozen `TradeSample` tuple) and self-contained: it carries the `retention` policy (so the indicator layer can fail-closed on `lookback > cap` even before any eviction) plus truncation metadata (`evicted_event_count`, `evicted_through_event_time`, `retention_truncated`). Single `threading.Lock` makes observe/peek atomic; concurrency tests assert (a) racing observers leave monotonic, dup-free, max-sequence-terminal history and (b) a concurrent reader never sees partial/corrupt state. Import guard extended: `rolling_window.py` may import `threading`; broker/ledger/decision/socket/websocket/http/asyncio remain forbidden. Full suite 2770→2799.
    - ~~RTM-4b.1b: pure rolling-window indicators.~~ Done: `src/market_data/indicators.py` (`IndicatorWindowSpec` + canonical `window_id`; window selection + `IndicatorReadiness`; pure-Decimal SMA_PRICE/RETURN_BPS/ROLLING_VOLUME/VWAP). Reads only an immutable `TradeHistorySnapshot` + caller `now`; holds no state; does NOT touch store/monitor/conditions/trigger_engine. See the dedicated RTM-4b.1b note below.
    - **Not in RTM-4b.1a (the indicator layer = RTM-4b.1b, now done above; condition integration still deferred to 4b.2):** `ConditionClause.window` + Metric enum extension; frozen `IndicatorContext` injected into `TriggerEngine.evaluate(..., indicators=)` (engine never reads a store); latest/indicator coherence gate (stream identity + latest_sequence + latest_event_time must match the trade snapshot); monitor optional rolling-store plumbing with `latest APPLIED ⇒ rolling observe required, else MonitorInternalError` fail-closed.
  - RTM-4b.1b/4b.2/RTM-5..RTM-8 (rolling-window indicators + condition integration, paper execution bridge, KIS WS live read-only smoke, 4x/day decision refresh, 1-day unattended paper pilot) are sequenced in Issue #1; each opens only on a separate explicit manual gate.
    - **Fast-loop audit (operator, 2026-06-10) — F2/F3/F7 CLOSED; F1 tracked; F4–F9 deferred.** A pre-RTM-4b.1b audit raised 9 findings. Each was reproduced against the code with a minimal repro before any fix (no report trusted verbatim). Closed now (in scope):
      - **F2 — reset-rule freshness fail-open (trigger engine), CLOSED.** In `_evaluate_locked`, COOLDOWN re-arm evaluated `reset_rules` against whatever slots existed without a freshness/identity gate, so a stale or missing trade/quote could satisfy a reset clause and re-arm (reproduced: stale `LAST_TRADE_PRICE` re-armed AND fired a second time → `ALREADY_FIRED`/`LOCKED`). Fix: new `_reset_slot_gate(plan, snapshot)` runs before reset counting only in COOLDOWN; it derives required slots via `rule_required_slots(plan.reset_rules)` and suppresses (holds COOLDOWN, `_suppress_break` zeroes `_reset_count`) on `MISSING_QUOTE`/`MISSING_TRADE`/`STALE_QUOTE`/`STALE_TRADE`/`SNAPSHOT_SYMBOL_MISMATCH`. Reset is now fail-closed and freshness is required to advance the consecutive-reset counter. 8 tests in `tests/test_trigger_engine.py`.
      - **F3 — monitor `max_attempts` lifetime accrual (reconnect), CLOSED.** `MarketMonitor.run()` accrued reconnect attempts over the whole process lifetime, so a long-lived connection that delivered traffic and then dropped counted toward the same exhaustion budget as a never-connecting one — a healthy monitor would eventually `EXHAUSTED` for no fault (reproduced: 3 healthy drops exhausted old code; survived 10+ after fix). Fix: per-attempt `_applied_this_attempt`; on drop, `consecutive_failures` resets to 0 if the attempt applied ≥1 event else increments; exhaustion gates on `consecutive_failures >= max_attempts` and backoff uses `delay_for_attempt(max(consecutive_failures, 1))`. New `consecutive_failures` field surfaced on `MonitorEvidence` + `MonitorSummary` (and CLI summary via `dataclasses.asdict`). 6 tests in `tests/test_market_monitor.py` (+ CLI `_SUMMARY_KEYS` updated).
        - **F3 follow-up (2026-06-10, review-flagged) — heartbeat-only healthy misjudgement CLOSED.** The first F3 fix counted *any* `ApplyStatus.APPLIED` as healthy, but `LatestMarketStateStore` returns APPLIED for heartbeats too — so a heartbeat-only connection (heartbeat APPLIED → drop, repeated) reset `consecutive_failures` to 0 every attempt and **never exhausted**, contradicting the documented "heartbeat-only counts as not-healthy" contract (reproduced: 8 heartbeat-only drops survived `max_attempts=3`; after fix exhausts at attempt 3). Fix: the per-attempt healthy counter (renamed `_market_applied_this_attempt`) increments only when `result.event_type in (TRADE, BEST_BID_ASK)`. Policy: **trade OR quote APPLIED ⇒ healthy** (requiring both would falsely fail illiquid symbols that only quote). 3 tests: heartbeat-only → EXHAUSTED, heartbeat+trade → healthy, heartbeat+quote → healthy. Evidence-timing note: `drop` evidence is emitted before `consecutive_failures` is updated; `backoff`/`exhausted` after — i.e. `drop` carries the pre-update value, `backoff`/`exhausted` the post-update value.
      - **F7 — non-hermetic acceptance/pytest-count gate, CLOSED.** `ops/acceptance_check.sh` Check 1 grepped for a hardcoded `"2799 passed"` string (brittle: normal test growth false-alarms; offsetting failures could hide a real break) and a research-source live-smoke test reached real network. Fix: Check 1 now gates on pytest **exit code** only (`fail` iff exit≠0; pass-count is informational). `test_fetch_research_sources.py` live-smoke rewritten to run `main(...)` in-process with a fake ticker factory + fixed `fetched_at`, asserting stdout stays pure JSON and `verbose:` only on stderr — proven hermetic under a socket-blocking pytest plugin (all of `socket.socket`/`create_connection`/`getaddrinfo` raise → still passes). `docs/RUNBOOK.md` and 6 `pytest_baseline_synchronized*` guard tests updated to enforce the exit-code contract (no hardcoded count anywhere).
      - **F1 — process-lifetime-only trigger idempotency (BLOCKER, RTM-5 PREREQUISITE).** `TriggerEngine` idempotency/fire state is in-memory per process; a restart loses the fire journal, so the same activation could re-fire after a crash/restart. This is acceptable for the trigger-only fast loop today but **MUST be closed with a persistent fire journal before RTM-5 connects PaperBroker/ledger** (else a restart can double-execute an order). **F1a (the journal repository itself) is now landed — see the F1a entry below.** F1 stays OPEN until the journal is wired across the fire→order boundary in a later lane (F1b); do not connect any broker/ledger write path until that bridge lands and `max_fires_per_decision == 1` is enforced at the boundary.
      - **F4 / F5 — deferred to RTM-4b.2** (indicator/condition-layer concerns; opened only with the 4b.2 manual gate). **F6 / F8 / F9 — deferred to a separate follow-up hardening pass** (not blocking RTM-4b.1b). None were silently closed.
      - Gates at closure: focused suites green; full suite 2799→2811 passed; `./ops/acceptance_check.sh` 11 PASS / 0 WARN / 0 FAIL; `git diff --check` clean; `git ls-files runtime` empty; zero network calls (hermetic proof above).
    - **RTM-4b.1b — pure indicators (operator, 2026-06-10) — CLOSED.** `src/market_data/indicators.py` (`tests/test_indicators.py`, 46 tests). All policy is caller-supplied; no hidden defaults; `network/broker/ledger/asyncio/threading` import-free (covered by the existing `market_data` import guard — no allowlist change needed; the new file only uses `hashlib`/`json`/`decimal`/`datetime`/pydantic).
      - **`IndicatorWindowSpec`** (pydantic, `extra=forbid`, `frozen`): `lookback_events:int|None`, `lookback_seconds:Decimal|None`, `min_events:int`, `freshness_max_age_seconds:Decimal`, `max_gap_seconds:Decimal|None`. Contract: at least one lookback required; `min_events>=1`; `lookback_events>=min_events`; durations are finite, >0, whole-microsecond-representable (no `float`); `bool` rejected as int. `window_id` = SHA-256 of canonical JSON over the 5 fields with `Decimal.normalize()` so 60 ≡ 60.0 ≡ same id.
      - **Readiness** `MISSING/WARMING/DISCONTINUOUS/READY/FUTURE/STALE/INSUFFICIENT_RETENTION`. Window selection: anchor = latest sample `trade_at`; time range `(anchor-lookback_seconds, anchor]` (old edge **exclusive**); count = newest `lookback_events`; both ⇒ intersection (tighter suffix). Gap: last `max_gap_seconds`-violating adjacency splits off the trailing continuous suffix. Lookback period and freshness period are never mixed.
      - **Resolved spec gaps (beyond GPT's literal spec):** (1) **explicit fail-closed precedence**: MISSING → static INSUFFICIENT_RETENTION (spec lookback > store hard cap) → empty-after-reset DISCONTINUOUS → dynamic INSUFFICIENT_RETENTION (requested range actually evicted) → suffix<min_events ⇒ WARMING (initial epoch, no internal gap) / DISCONTINUOUS (reset epoch OR internal gap) → FUTURE → STALE → READY. (2) **future tick (now < anchor ⇒ age<0)** is a dedicated `FUTURE` readiness (see hardening below; originally folded into STALE). (3) **count/time coverage** is fail-closed and now judged against the *effective intersection* of both bounds (see hardening below). Truth table is encoded at the top of `tests/test_indicators.py`.
      - **RTM-4b.1b hardening (operator, 2026-06-10) — CLOSED.** GPT's 3-item post-commit review was verified against the code (item 2 via a membership proof, not trusted verbatim) and all 3 applied. Full suite 2860→2864 (+4 net: 4 new combined-window/coherence tests; the future-tick test was re-pointed STALE→FUTURE). (1) **future tick split** — `now < anchor` (age<0) now returns `IndicatorReadiness.FUTURE` instead of STALE, so clock-skew/ordering defects are diagnosed distinctly from genuinely-old data (`age > freshness_max_age_seconds`) for accurate 4b.2 reason mapping (`test_future_latest_tick_is_fail_closed_future_not_stale`). (2) **combined count+time retention via effective intersection** — the prior independent-OR check over-suppressed: it flagged INSUFFICIENT whenever *either* the time edge OR the count edge looked truncated, even when the evicted sample lay outside the two bounds' intersection (the actual effective window). Now: with both bounds present, `insufficient = time_evicted AND count_evicted` (the newest-evicted sample breaks coverage iff it was inside the intersection = within the time edge AND within the latest-N count); single-bound specs keep their original behavior. New regressions: count-binding with time-only eviction → READY, time-binding with count-only eviction → READY, genuine intersection-internal eviction → INSUFFICIENT. (3) **snapshot coherence metadata** — `IndicatorWindowSnapshot` now carries `provider`/`channel`/`latest_sequence`/`latest_received_at` (passed through from the history) so a 4b.2 consumer can self-identify the source stream/sequence without re-reading the store (`test_ready_window_carries_source_coherence_metadata`).
      - **Not in RTM-4b.1b (still 4b.2):** `ConditionClause.window`, `IndicatorContext`, `TriggerEngine.evaluate(..., indicators=)`, `ConditionObservation.window_id`, `rule_set_id` window binding, and the latest/indicator coherence gate that closes audit F4/F5. The indicator layer here is consumed by passing a snapshot; it never reaches into a store or engine.
    - **RTM-4b.2 — rolling-condition integration + F4/F5 closure (operator, 2026-06-10) — CLOSED.** GPT's 4b.2 spec was verified against the real code (the highest-risk item — that the rolling store records `provider`/`channel`/`sequence` from `tick.provider_sequence.*`, `latest_event_time` from `tick.trade_at`, `latest_received_at` from top-level `tick.received_at`, exactly matching the coherence equalities so the gate cannot brick all firing — was checked before implementing). Full suite 2864→2937 (+73: `tests/test_rolling_conditions.py` 35, `tests/test_trigger_engine_rolling.py` 28, `tests/test_monitor_rolling.py` 10). All additive/backward-compatible: price-only RTM-4a plans behave identically with `indicators=None`.
      - **C — conditions** (`src/market_data/conditions.py`): `Metric` extended with rolling `SMA_PRICE/RETURN_BPS/ROLLING_VOLUME/VWAP`; `ConditionClause.window: IndicatorWindowSpec|None` (rolling metric **requires** a window, latest metric **forbids** one, via `model_validator` + `extra=forbid`). Threshold categories: price metrics (`LAST_TRADE_PRICE/BEST_BID_PRICE/BEST_ASK_PRICE/SMA_PRICE/VWAP`) > 0; `SPREAD_BPS/ROLLING_VOLUME` >= 0; `RETURN_BPS` any finite; non-finite rejected. `metric_value(..., indicators=, window=)` computes rolling metrics only from a READY `IndicatorContext` window (`IndicatorNotReadyError`→None fail-closed); `evaluate_clause`/`evaluate_all` gained `indicators=`. `rule_required_slots` forces `needs_trade=True` for any rolling metric (so the engine gates latest-trade freshness first); new `rule_required_windows(rules)` dedupes by `window_id`, deterministically ordered, empty for price-only.
      - **D — frozen `IndicatorContext`** (`src/market_data/indicators.py`): immutable `(market, symbol, windows)`; rejects duplicate `window_id` and market/symbol mismatch; orders windows by `window_id`; `get(window_id)`. `build_indicator_context(history, specs, *, now)` evaluates each unique `window_id` once (store/network never re-read). FUTURE readiness is preserved (no silent drop).
      - **F — TriggerEngine gate** (`src/market_data/trigger_engine.py`): `evaluate(..., indicators=None)`; engine **never reads a store** (frozen inputs only). 8 new `TriggerReason`s (`MISSING_INDICATOR`, `INDICATOR_WARMING/DISCONTINUOUS/STALE/FUTURE/INSUFFICIENT_RETENTION`, `INDICATOR_IDENTITY_MISMATCH`, `INDICATOR_LAGGING`). `_indicator_gate` runs after trade-freshness, before condition evaluation: required-window present → context identity → per-window readiness (`_READINESS_REASON` map) → exact latest↔indicator coherence (`_coherence_reason`). **F4/F5 closed:** coherence requires market/symbol/provider/channel match AND `latest_sequence==seq.sequence` AND `latest_event_time==trade.trade_at` AND `latest_received_at==trade.received_at`; indicator behind (`seq<` or `event_time<`)→LAGGING; ahead or partially inconsistent→IDENTITY_MISMATCH. `ConditionObservation.window_id` carries the source window in evidence (None for latest). `_canonical_rule` includes `canonical_window_payload(clause.window)` so SMA-20tick vs 60tick vs 60s get distinct `rule_set_id`/idempotency (60≡60.0 same). All suppression paths leave state/fire-budget unchanged.
      - **G — reset_rules rolling gate (F2 extension):** in COOLDOWN, `_indicator_gate(plan, plan.reset_rules, ...)` gates re-arm on the reset windows too; any missing/not-ready/incoherent reset window holds COOLDOWN and `_suppress_break` zeroes `_reset_count` (stale input can't re-arm). The **initial** trigger does not require reset-rule indicators (only the COOLDOWN re-arm path does).
      - **I — monitor rolling plumbing** (`src/market_data/monitor.py`): optional `rolling_store: RollingTradeHistoryStore|None`. `None` ⇒ byte-identical RTM-3 behavior. When set: only an APPLIED **trade** mirrors into rolling history (quote/heartbeat/non-APPLIED excluded); latest `apply` and rolling `observe` share one injected `now` (clock read once); on confirmed reconnect, latest is reset first then rolling (both via `reset_stream`); a latest-APPLIED trade that rolling rejects (non-APPLIED) or a rolling observe/reset exception raises `MonitorInternalError` (fail-closed, no silent partial update, no nested locks).
      - **Scope held:** no broker/ledger/PaperLoop/KIS/live-network/runtime-DB/LLM touched. F1 (persistent fire journal, RTM-5 prereq) and the transport-health final policy remain **OPEN** below. RTM-5 not started.
    - **RTM-4b.2 hardening — as-of staleness + bool rejection (operator, 2026-06-10) — CLOSED.** GPT's 2 post-commit blockers were verified against the real code (both confirmed real) and fixed. Full suite 2937→2951 (+14). No prod `TriggerEngine.evaluate` wiring exists yet, so the new as-of gates change no live path; they harden the contract before RTM-5 integration. (1) **Cached-freshness fail-open (BLOCKER)** — `LatestMarketStateSnapshot` carried only `trade_fresh`/`quote_fresh` booleans (computed once at `peek` time) and `IndicatorContext` carried no evaluation time, so a snapshot/context reused after `now` advanced would fire on expired freshness flags. **Fix (Option A — exact as-of equality):** `LatestMarketStateSnapshot` gains `evaluated_at` (set to `now` in `peek`); `IndicatorContext` gains `evaluated_at` (set to `now` in `build_indicator_context`, tz-aware validated in `__post_init__`); engine suppresses `STALE_SNAPSHOT` when `snapshot.evaluated_at != now` (right after permission gates, before market-state freshness) and `INDICATOR_CONTEXT_STALE` when `indicators.evaluated_at != now` (inside `_indicator_gate`, which now takes `now`; both main-rule and reset-rule call sites updated). Chosen over a freshness-deadline scheme because it preserves the authoritative boolean-flag staleness semantics (minimal test churn) while giving the strictest fail-closed guarantee, matching the monitor's "read clock once per tick" pattern. Regressions: reused stale snapshot at `now+1h` within permission window → `STALE_SNAPSHOT`, no fire, state unchanged; fresh-at-exact-now control still fires; stale `IndicatorContext` with fresh snapshot → `INDICATOR_CONTEXT_STALE`; fresh-context control fires. (2) **Boolean threshold accepted** — `ConditionClause._coerce_threshold` had `isinstance(value, (str, int))` and `bool` is an `int` subclass, so `True`/`False` silently coerced to `Decimal(1)`/`Decimal(0)`. **Fix:** reject `bool` before the str/int branch in `_coerce_threshold`; same guard added to `TriggerPlan._coerce_decimal` (`debounce_seconds`/`cooldown_seconds`) for consistency. Regressions: every `Metric` rejects `True`/`False` thresholds; `debounce_seconds`/`cooldown_seconds` reject bools. **Scope held:** same as RTM-4b.2 (no broker/ledger/PaperLoop/KIS/live-network/runtime-DB/LLM). RTM-5/F1 not started.
    - **F1a — persistent trigger fire journal (repository only) (operator, 2026-06-11) — CLOSED.** New `src/execution/` package (did not exist before); journal-only, broker/ledger never connected. GPT's review of the F1 design precheck was verified against the real code before implementing (the three highest-risk corrections were each confirmed against source, not trusted verbatim): (a) `TriggerSignal` exposes neither `activation_epoch` nor `rule_set_id` and `TriggerEngine` has no restore/seed API → multi-fire activation-epoch restoration is **not implementable in v1** and is deferred; the boundary instead relies on `max_fires_per_decision == 1` (enforced in the future F1b bridge, not here); (b) `order_id` stays `order-{decision_id}` and `correlation_id` is the externally-injectable field → no `order_id` redefinition in v1; (c) the journal needs no `market_data` import (the `market_data` import guard never forbade `sqlite3`; the precheck's "guard blocks journal" reasoning was overstated — the real basis is engine purity + the recommended `src/execution` location). Full suite 2960→2991 (+31, `tests/test_trigger_journal.py`).
      - **State machine:** `RESERVED → DISPATCHING → COMMITTED`; `RESERVED → ABORTED` (safe pre-dispatch cancel); `DISPATCHING → UNCERTAIN` (restart/error with unknown order outcome). `COMMITTED/ABORTED/UNCERTAIN` are terminal for auto-execution; `UNCERTAIN` forbids auto-retry (operator-reconciliation-only; no reconcile API in F1a). The earlier `RESERVED→SUBMITTED→COMMITTED/FAILED` shape was corrected per review (SUBMITTED is a misname before the broker call returns; a single FAILED conflated safe-fail vs uncertain).
      - **`src/execution/trigger_journal.py`** — `JournalState` enum, `TERMINAL_STATES`, `ReserveOutcome` (`RESERVED_NEW/EXISTING_TERMINAL/EXISTING_PENDING`), frozen `TriggerJournalRecord`/`ReserveResult`, structural `TriggerFireSignal` Protocol (reads identity fields only; `market_data.TriggerSignal` satisfies it without an import), `TriggerJournal` Protocol (`reserve`/`mark_dispatching`/`mark_committed`/`mark_aborted`/`mark_uncertain`/`get`/`list_nonterminal`), and typed errors (`IllegalTransitionError`/`RecordNotFoundError`/`IdentityCollisionError`/`OrderIdConflictError` under `TriggerJournalError`).
      - **`src/execution/sqlite_trigger_journal.py`** — `SqliteTriggerJournal`. Single-statement compare-and-set updates (`UPDATE ... WHERE idempotency_key=? AND state=?`; `rowcount!=1` → fetch to distinguish `RecordNotFoundError` vs `IllegalTransitionError`). `idempotency_key` PK; `order_id` nullable `UNIQUE` (dup → `OrderIdConflictError`, original row rolled back unchanged). Reserve on PK conflict returns the existing record (`EXISTING_TERMINAL`/`EXISTING_PENDING`) and rejects identity-field mismatch (`IdentityCollisionError`). `CHECK(state IN (...))`; tz-aware ISO-8601 datetimes (naive `now`/`triggered_at` rejected, round-trip stays tz-aware); `bool`/unknown-enum identity values rejected; serialization mirrors `sqlite_ledger.py` helpers. Recommended store path is a **separate** `runtime/paper/trigger_journal.sqlite3` (distinct from the ledger DB); tests use `tmp_path` only.
      - **F1a hardening (operator, 2026-06-11) — 5 review items, all verified against the code then applied.** Full suite 2991→3009 (+18). (1) **timestamp monotonicity** — new `NonMonotonicTimestampError`; `reserve` rejects `triggered_at > reserved_at`; every `mark_*` rejects `now < existing.updated_at` (equal allowed). This makes `triggered_at ≤ reserved_at ≤ dispatching_at ≤ finalized_at` and non-regressing `updated_at` an enforced invariant (audit/restart-reconciliation safe); the guarded update now pre-checks state→monotonicity inside the txn (CAS `WHERE state=?` kept as a race fallback). (2) **typed result status** — `mark_committed` now validates against a new execution-local `JournalResultStatus` enum (`FILLED/PARTIALLY_FILLED/REJECTED/CANCELLED/PENDING`); arbitrary strings/bools rejected; no ledger/broker enum imported (bridge maps later). (3) **real concurrency proof** — added a `threading.Barrier` two-thread same-key reserve test (exactly 1 `RESERVED_NEW` + 1 `EXISTING_PENDING`, no "database is locked"); the prior sequential test was renamed to stop over-claiming concurrency. (4) **UTC-canonical storage** — all datetimes stored as `.astimezone(utc).isoformat()`, so TEXT/ISO-8601 string ordering equals absolute-time ordering even across mixed offsets (`list_nonterminal` correctness); connection sets `busy_timeout`/WAL. (5) **terminal-transition regressions** — added exhaustive rejection tests out of `ABORTED` and `UNCERTAIN` (previously only `COMMITTED`), plus repeated-commit and backward-time rejection.
      - **Scope held:** `TriggerEngine`/`RiskFilter`/`PaperBroker`/`Ledger` unmodified; no broker/ledger/network import in `src/execution` or its tests (grep-verified); no `runtime/` DB touched; `git ls-files runtime` empty; acceptance 11 PASS. **Deferred to F1b (next lane):** the fire→order bridge that calls `reserve` before order generation and `mark_*` around the broker submit, plus `max_fires_per_decision==1` enforcement and the DISPATCHING-after-restart→ledger-reconcile decision (default UNCERTAIN + stop). RTM-5 broker write still forbidden until F1b lands.
    - **F1b — fire→order bridge (`TriggerOrderBridge`) (operator, 2026-06-11) — core COMPLETE; safety-hardening sub-entry below closes the identity-coherence gap.** New `src/execution/trigger_order_bridge.py` orchestrates the fire→order boundary; it *calls* journal/`OrderIntentGenerator`/`QuantityResolver`/broker/ledger via narrow Protocols but modifies none of them, and the journal modules stay broker/ledger/network-import-free (grep-verified — only the bridge imports them). GPT's F1b precheck was verified against the real code before implementing; the four corrections it raised were each confirmed against source (not trusted verbatim) and are reflected in the design: **(1) `QuantityResolver` is in the path** — `PaperBroker.submit_order` rejects `target_weight_percent` intents (`paper_broker.py:100`), so the bridge inserts sizing between `generate` and `submit`; **(2) the bridge takes `DecisionTriggerBundle`(plan+analysis)+`TriggerSignal`** — `TriggerSignal` alone lacks `max_fires_per_decision` and cannot prove plan↔analysis coherence, so preflight checks signal↔plan and signal↔analysis identity fields and asserts `plan.max_fires_per_decision == 1` (the *initial* F1b preflight checked only the signal/plan/analysis-decision triple and `risk_input.analysis_decision`'s `decision_id` — it did **not** assert the full `risk_input.analysis_decision`/`market_price` and built-order symbol/market/side/source identity; that gap was a real BLOCKER, closed in the hardening sub-entry below); **(3) no normal-path auto-resume of persisted nonterminal rows** — `reserve` returning `EXISTING_PENDING` yields `RECONCILE_REQUIRED` (not a resume), and a separate `reconcile_record`/`reconcile_all` is the only way to settle restart leftovers; **(4) ledger durable result is the commit source of truth, not the broker return value** — `PaperBroker` returns an in-memory REJECTED for a duplicate `order_id` *without* writing a row, so the bridge commits from `ledger.get_order_result(order_id)` only.
      - **Execution order (locked):** `preflight` (fail-closed, no journal/broker call on mismatch) → `journal.reserve` (`RESERVED_NEW`→continue; `EXISTING_TERMINAL`→`SKIPPED_TERMINAL`; `EXISTING_PENDING`→`RECONCILE_REQUIRED`) → `generate` (`BLOCKED`→`mark_aborted("risk_blocked")`; `NOOP`→`mark_aborted("hold_noop")`) → `resolve` (`FAILED`→`mark_aborted("sizing_failed")`; `NOOP`→`mark_aborted("no_executable_quantity")`; both before any broker call) → executable-intent coherence (`order_id==order-{decision_id}`, `correlation_id==idempotency_key`, quantity present, `target_weight_percent` removed; violation → `mark_aborted("coherence_failed")` + raise) → **ledger preflight** (`get_order_result(order_id)` already present → no broker call, `mark_dispatching` then commit/uncertain from that durable status) → `mark_dispatching` (DISPATCHING, `order_id` UNIQUE) → `broker.submit_order` (exception → `mark_uncertain("broker_exception")` + stop) → `ledger.get_order_result` truth (FILLED→`COMMITTED(FILLED)`; REJECTED→`COMMITTED(REJECTED)`; missing→`mark_uncertain("dispatch_outcome_missing")`; PENDING/CANCELLED/other→`mark_uncertain("dispatch_outcome_nonterminal")`).
      - **Restart reconcile (separate from the normal path):** RESERVED → `mark_aborted("restart_before_dispatch")` (broker is only called after `mark_dispatching`, so RESERVED proves no order was sent — safe abort). DISPATCHING → if `ledger.has_processed_order(order_id)` and the durable status is FILLED/REJECTED → `mark_committed`; otherwise `mark_uncertain("dispatch_outcome_unknown")` with **no auto-resubmit**. The fail-closed bias prefers a missed execution over a double order.
      - **PENDING in v1:** v1 paper generates MARKET orders only, so paper outcomes are {FILLED, REJECTED}; PENDING is LIMIT-only and therefore unreachable on the normal path, but the bridge still maps any non-{FILLED,REJECTED} durable status to UNCERTAIN+stop rather than guessing.
      - Full suite 3009→3031 (+22, `tests/test_trigger_order_bridge.py`): preflight mismatches (missing plan / signal↔plan / max_fires≠1 / correlation_id) each reserve nothing and never call the broker; FILLED/REJECTED happy paths; the four abort paths; coherence violation; ledger-preflight desync commit-without-broker; broker-exception / missing-durable / pending-durable UNCERTAIN paths; `EXISTING_TERMINAL` skip and `EXISTING_PENDING`→reconcile-required; reconcile RESERVED→ABORTED and DISPATCHING→COMMITTED/UNCERTAIN via `has_processed_order`; terminal-record reconcile rejection; one integration test exercising the real `QuantityResolver` sizing.
      - **Scope held:** `TriggerEngine`/`RiskFilter`/`PaperBroker`/`Ledger`/journal unmodified; bridge depends on Protocols and is tested with fakes + the real SQLite journal; no real broker connected (RTM-5 still forbidden); no `runtime/` DB touched; `git ls-files runtime` empty; `git diff --check` clean; acceptance 11 PASS / 0 WARN / 0 FAIL. **F1 can now be considered closeable once RTM-5 wires this bridge into the live fast loop** — the persistence + idempotency contract it required is in place, but no production caller invokes `TriggerOrderBridge.dispatch` yet.
      - **F1b hardening (operator, 2026-06-11) — 7 review items, each verified against the code before applying (one of GPT's six findings — the integration-test overclaim — was confirmed by reading the test, not trusted verbatim).** Full suite 3031→3043 (+12: 10 new bridge tests, 1 full-stack integration pair, 1 journal concurrency test). The first three items were **real BLOCKERs** that allowed a divergent order to be journaled. (1) **Identity-coherence gap (BLOCKER)** — the order is built from `risk_input.analysis_decision`, but preflight only compared its `decision_id`, and the executable-intent coherence check omitted symbol/market/side/source. Concrete failure: signal `005930 BUY` + `risk_input.analysis_decision` `000660 SELL` sharing one `decision_id` → a `005930 BUY` journal row recorded under a `000660 SELL` order. **Fix:** `_preflight` now asserts `risk_input.analysis_decision == bundle.decision` and `market_price.market`/`symbol == signal`; `_assert_executable_coherence` now asserts the built intent's `symbol`/`market == signal`, `side == _ACTION_TO_SIDE[signal.action]`, `source_decision_id == signal.decision_id`, `order_type == MARKET`, `account_role == PAPER` (violation → `mark_aborted("coherence_failed")` + raise `BridgeCoherenceError`). (2) **Pre-dispatch dependency exceptions orphaned the RESERVED row** — a raise from `generate`/`resolve`/the ledger preflight left a `RESERVED` row with no terminal transition. **Fix:** each is wrapped; on exception the row is `mark_aborted` (`generation_exception`/`sizing_exception`/`ledger_preflight_exception`) and a typed `BridgeDependencyError` is re-raised — no orphan, broker never called. (3) **Broker-exception skipped the ledger re-read** — a broker that wrote a FILLED row and *then* raised was forced to UNCERTAIN, losing a real fill. **Fix:** `_reconcile_after_broker_exception` re-reads `ledger.get_order_result(order_id)`; terminal status → `COMMITTED`, otherwise `mark_uncertain("broker_exception")`. (4) **Integration overclaim** — the original "integration" test used a fake generator. **Fix:** added a true full-stack test (real `OrderIntentGenerator` + `QuantityResolver` + `PaperBrokerAdapter` + `SQLiteLedger` + `SqliteTriggerJournal` sharing one `AnalysisDecision`) → `COMMITTED(FILLED)` + ledger fill present, plus an identity-mismatch variant (divergent risk-input symbol → `BridgePreflightError`, no ledger order, no journal row). (5) **Concurrent `mark_dispatching` race untested** — added a `threading.Barrier` two-connection race: exactly 1 success + 1 `IllegalTransitionError`, no "database is locked", final `DISPATCHING` carries the winner's `order_id`. (6) **TECH_DEBT wording** — corrected the item-(2) "across all identity fields" claim above (it overstated the initial coverage). **Scope held:** same as F1b (no broker/ledger/PaperLoop/journal module modified; grep-confirmed journal modules still import no broker/ledger/network; no `runtime/` DB touched; `git ls-files runtime` empty; `git diff --check` clean; acceptance 11 PASS / 0 WARN / 0 FAIL). RTM-5 broker write still forbidden.
      - **F1b postflight hardening (operator, 2026-06-11) — 1 follow-up item, verified against the code before applying.** The post-broker ledger read that establishes the commit source of truth (`durable_post = ledger.get_order_result(order_id)`, step 8) was unguarded: at that point the journal is already `DISPATCHING` (broker call returned), so a raise from that read propagated as a raw exception out of `dispatch`, leaving the row stuck `DISPATCHING` with no terminal transition. **Fix:** the postflight read is wrapped; on exception the row is `mark_uncertain("ledger_postflight_exception")` and a `BridgeResult(UNCERTAIN)` is returned (no raw exception escapes, broker is **not** re-called, and the commit is **never** taken from the broker return value — result-unknown is fail-closed to UNCERTAIN, not ABORTED, because the broker side-effect may have happened). Regression: broker returns FILLED normally, then the postflight ledger read raises → `UNCERTAIN("ledger_postflight_exception")`, `order_result is None`, journal `UNCERTAIN`, broker called exactly once; re-dispatch → `SKIPPED_TERMINAL`, broker not called again. This closes the last unguarded read on the fire→order path. RTM-5 broker write still forbidden.
    - **RTM-5 — paper execution coordinator (`PaperExecutionCoordinator`) (operator, 2026-06-11) — coordination layer COMPLETE; no default runtime caller yet.** New `src/execution/paper_execution_coordinator.py` is the explicit library API that assembles a validated `DecisionTriggerBundle` + frozen `LatestMarketStateSnapshot` (+ optional `IndicatorContext`) + `TradingPermission` + paper portfolio/risk context + `AllocatorDecision` and drives `TriggerEngine` → `TriggerOrderBridge.dispatch`. It *calls* engine/bridge/position-source via narrow injected deps and modifies none of them; it imports no KIS/live adapter, no network, no LLM, and hardcodes no `runtime/paper` DB path. Full suite 3043→3067 (+24: 1 bridge postflight regression + 23 coordinator unit/integration tests in `tests/test_paper_execution_coordinator.py`). Design choices, each verified against the real APIs before implementing (never trusted verbatim): **(1) idempotent per-tick `replace_bundle`** — the coordinator calls `engine.replace_bundle(bundle, now=)` every tick; an unchanged decision returns `UNCHANGED` (no fire-state reset), a newer decision re-arms, and an older/conflicting decision is `FAILED_CLOSED` (never silently dropped). **(2) coordinator-owned `RiskFilterInput`** — it does not trust a caller-supplied risk input; it constructs its own from the injected `AllocatorDecision` + `bundle.decision` (as `analysis_decision`) + `RiskFilterContext`, with `correlation_id = signal.idempotency_key`, so the bridge's identity-coherence preflight is fed canonical objects. **(3) position from the ledger/broker, not an external scalar** — current PAPER quantity is read via the injected `PositionSource.get_position(symbol, market, PAPER)`; a returned position whose symbol/market/account_role does not match the signal is `FAILED_CLOSED` (`position_identity_mismatch`), so a wrong-instrument quantity can never reach sizing. **(4) `MarketPrice` from the same frozen snapshot** — BUY uses `quote.ask_price`, SELL uses `quote.bid_price`, `as_of = quote.quote_at`, market/symbol from the snapshot; there is no LAST-trade fallback (missing quote → engine `SUPPRESSED`, defensively also a coordinator `quote_unavailable` guard). **(5) exact as-of equality** — `snapshot.evaluated_at == now` and (when present) `indicators.evaluated_at == now` are enforced before the engine runs (`snapshot_as_of_mismatch`/`indicator_as_of_mismatch`), and bundle↔snapshot identity + context↔plan market are checked, all before any journal/broker call. **Typed outcomes:** `SUPPRESSED` (engine non-fire — journal/broker untouched), `FAILED_CLOSED` (coordinator preflight block — journal/broker untouched), and the bridge-mapped `TRIGGERED_ABORTED`/`COMMITTED`/`UNCERTAIN`/`RECONCILE_REQUIRED`/`SKIPPED_TERMINAL` (trigger/bridge reason codes preserved). **Restart recovery** is an explicit `recover(now=)` call delegating to `bridge.reconcile_all` (no constructor side effects, no auto-reorder, no UNCERTAIN auto-clear). **Isolated full-stack write proof:** integration tests use the real `TriggerEngine` + `TriggerOrderBridge` + `OrderIntentGenerator` + `QuantityResolver` + `PaperBrokerAdapter` + `SQLiteLedger(tmp_path)` + `SqliteTriggerJournal(tmp_path)` — trigger-false → SUPPRESSED/no order; valid BUY → `COMMITTED(FILLED)` with exactly one order result + one fill + reduced cash + one journal `COMMITTED` row; same-engine re-fire → `SUPPRESSED(ALREADY_FIRED)` with no extra fill; **recreate coordinator + reopen the same journal/ledger files (restart sim)** → `SKIPPED_TERMINAL`, no duplicate fill; sizing-NOOP → `TRIGGERED_ABORTED`; valid SELL → `COMMITTED(FILLED)`; stale snapshot and position-identity mismatch → no broker write; reserved-row `recover` → `ABORTED`. All test DBs are tmp_path with journal/ledger in separate files; `git ls-files runtime` empty. **Scope held / still OPEN:** the coordinator is a library callable only — **no scheduler/daemon/monitor wires it, no production caller invokes `process`, the real `runtime/paper` DB is untouched, and no KIS/live order path exists.** Authoritative per-tick unattended invocation, process supervision, and market-hours scheduling are **RTM-7**; real KIS WS transport is **RTM-6**; the transport-health final policy below stays **OPEN**. The 1-day unattended paper pilot requires a separate explicit launch/readiness precheck before any real runtime activation. `TriggerEngine`/`RiskFilter`/`PaperBroker`/`Ledger`/journal/bridge unmodified; acceptance 11 PASS / 0 WARN / 0 FAIL.
      - **RTM-5 portfolio-context coherence hardening (operator, 2026-06-12) — CLOSED for the dimensions the coordinator can verify; portfolio-wide coherence remains an RTM-7 launch-precheck OPEN item.** A verified launch-readiness BLOCKER: the coordinator read current PAPER quantity from the ledger but passed the caller-supplied `RiskFilterContext` straight through to `RiskFilterInput`/`QuantityResolver`, so the two could diverge. Concrete failure: empty ledger (0 shares) but a context claiming `current_symbol_market_value = 3,000,000` → coordinator passed `current_position_quantity = None` → `QuantityResolver._resolve_current_value` fell back to the *claimed* 3M instead of ledger truth 0 → for a 4% target of a 100M NAV it bought only ~1M instead of ~4M (undersized). Independently, `RiskFilter` (`filter.py`) consumes `current_symbol_market_value`/`current_symbol_cumulative_buy_cost` (position cap), `cash` (cash band) and `invested_amount` (paper-observation band) directly, so a stale context also shifts BLOCK/ADJUST approvals. **Fix (coordinator-enforced, fail-closed on the verifiable dimensions; the coordinator cannot rebuild multi-asset NAV/cash/invested from a single-symbol snapshot, so those are NOT fabricated):** (1) **ledger-truth sizing** — the current quantity passed to `bridge.dispatch` is now `position.quantity` when a position exists else `Decimal("0")`, never `None`, so the resolver can never fall back to a caller-claimed symbol value; (2) **as-of gate** — `risk_context.created_at == now` is enforced (`context_as_of_mismatch`); (3) **symbol-value coherence gate** — `current_symbol_market_value` must equal `ledger_quantity × reference price` with matching currency, else `context_position_value_mismatch` (a `None` symbol value is read as 0 and must match an empty/zero ledger); (4) **typed exception wrapping** — `position_source.get_position(...)` and `RiskFilterInput(...)` construction are wrapped so a raise becomes `FAILED_CLOSED` (`position_source_error`/`risk_input_build_error`) instead of propagating. `MarketPrice` is now built before the position read (the reference price is needed for the coherence gate). New reason codes: `context_as_of_mismatch`, `context_position_value_mismatch`, `position_source_error`, `risk_input_build_error`. Full suite 3067→3072 (+5 net: 4 new coordinator unit tests — as-of mismatch / symbol-value mismatch / position-source raise / risk-input build raise, each `FAILED_CLOSED` with no `bridge.dispatch`; 1 new integration test — empty ledger + context claiming 3M → `FAILED_CLOSED(context_position_value_mismatch)` with no ledger order, no journal row, position/cash unchanged). The pre-existing integration tests now assert *exact* ledger-truth sizing: empty-ledger BUY → 57 shares (4M/70000 floored) + cash 96,010,000; seeded-100 NOOP and SELL supply a context coherent with `100 × 70000 = 7,000,000`; SELL → 58 shares remaining; the restart-reopen idempotency test supplies a post-fill-coherent `57 × 70000 = 3,990,000` context so the coherence gate is reached before the journal terminal skip. **Still OPEN (not closed here):** portfolio-wide coherence of `cash`/`total_nav`/`invested_amount`/`current_symbol_cumulative_buy_cost` against ledger truth, and a canonical ledger-derived `RiskFilterContext` builder, require a portfolio-valuation service the coordinator does not own — these are the **RTM-7 portfolio-context service + the separate 1-day-pilot launch/readiness precheck**, and RTM-5 launch readiness stays **NO-GO** until that precheck runs. `TriggerEngine`/`RiskFilter`/`PaperBroker`/`Ledger`/journal/bridge unmodified; journal modules still import no broker/ledger/network (grep-verified); `git ls-files runtime` empty; `git diff --check` clean; acceptance 11 PASS / 0 WARN / 0 FAIL.
    - **RTM-7a — canonical paper portfolio-context service + pre-fire coordinator hardening (operator, 2026-06-12) — CLOSED for the single-instrument fast-loop path; portfolio-wide multi-asset valuation authority now exists, scheduler/market-hours/process-supervision still OPEN (RTM-7).** This lane closes the RTM-5 hardening's two named OPEN items: (a) the coordinator no longer trusts a caller-supplied `RiskFilterContext` at all — it owns a canonical valuation service that derives the context from ledger truth + fresh marks; (b) all position/cash/portfolio/dependency validation now runs **before** `TriggerEngine.evaluate()`, so a dependency failure can no longer consume the engine fire budget (the missed-execution bug). Full suite 3072→3090 (+18: `tests/test_paper_portfolio_context.py` 24, plus net coordinator-test delta after the rewrite).
      - **New `src/execution/paper_portfolio_context.py` — `PaperPortfolioContextService` is the single valuation authority.** `build_context(*, symbol, market, proposed_price, policy, now) → PaperPortfolioValuation`. **Valuation policy (v1):** base account `AccountRole.PAPER`, base currency KRW; non-KRW anywhere → typed `portfolio_currency_unsupported` (no FX). Cash from `ledger.get_cash(KRW, PAPER)` (missing → `portfolio_cash_missing`; `as_of` in the future → `portfolio_cash_future`; `as_of != now` is **not** a rejection — cash is a valid projection). Positions = `list_positions()` filtered to PAPER; each position's portfolio **MARK is the quote midpoint `(bid+ask)/2`** from a fresh snapshot (`evaluated_at == now` + quote present + `quote_fresh`, **no trade fallback**; failures → `portfolio_snapshot_missing`/`portfolio_snapshot_stale`/`portfolio_snapshot_identity_mismatch`). `market_value = qty × midpoint`, `cumulative_buy_cost = qty × avg_cost`, `invested = Σ market_values` (CASH asset-class excluded), `total_nav = cash + invested` (`≤0` → `portfolio_nav_invalid`); current-symbol weight = value/nav; asset-class weights bucket by `AssetClass` (KR_EQUITY→kr / US_EQUITY→us / GOLD→gold; CASH excluded; denominator = invested). **Execution price keeps the RTM-5 policy (BUY=ask, SELL=bid) and is NEVER mixed with the portfolio midpoint mark** — `proposed_price` enters the context as `Money` and `reference_prices` carries the midpoint marks (RiskFilter slippage compares proposed vs the symbol's midpoint, currency-matched). A `proposed_price` whose identity/currency disagrees → `portfolio_proposed_price_mismatch`.
      - **Coordinator rewrite (`paper_execution_coordinator.py`):** constructor is now `__init__(*, engine, bridge, portfolio_context_service)` and `process(*, bundle, snapshot, permission, allocator_decision, portfolio_policy, now, indicators=None)` — **`risk_context` is no longer a parameter** (a regression test asserts `risk_context` absent / `portfolio_policy` present in the signature). **Pre-fire ordering (the fix):** tz/as-of gates → (if `bundle.plan` present) snapshot identity + quote-present → build execution `MarketPrice` (ask/bid via `plan.action`, available *before* evaluate) → `build_context(...)` (typed `PaperPortfolioContextError.reason_code` → `FAILED_CLOSED`; any other raise → `portfolio_context_build_error`) → structural `RiskFilterInput` build check (`risk_input_build_error`) → `replace_bundle` → **only then** `evaluate()`. Non-TRIGGERED → `SUPPRESSED`; TRIGGERED → rebuild `RiskFilterInput(correlation_id=signal.idempotency_key)` and `bridge.dispatch(current_position_quantity=valuation.position_quantity)` (ledger-truth qty, never the caller's). **Removed reason codes** (now subsumed by the service): `context_market_mismatch`, `context_as_of_mismatch`, `context_position_value_mismatch`, `position_source_error`, `position_identity_mismatch` (and the `PositionSource` dep). **Added:** `portfolio_context_build_error`.
      - **Pre-fire regression proof (the headline test):** first tick with a **stale portfolio mark** → `FAILED_CLOSED(portfolio_snapshot_stale)`, broker 0, no journal row, `signal is None` — and because the failure precedes `evaluate()`, the fire budget is untouched; the **same engine/coordinator** on a second tick with a fresh mark → `COMMITTED(FILLED)` 57 shares. Under the old order this would have consumed the fire on tick 1 and suppressed as `ALREADY_FIRED` on tick 2 (the missed-execution bug). Integration suite (real `TriggerEngine`+`TriggerOrderBridge`+`OrderIntentGenerator`+`QuantityResolver`+`PaperBrokerAdapter`+`SQLiteLedger`+`SqliteTriggerJournal`, `tmp_path`, mode REBALANCING so an empty 0%-invested account WARNs not ERRORs): empty-account BUY → 57 shares + cash 96,010,000 with NAV identity proven by **rebuilding the context post-fill** (cash 96,010,000 + invested 3,990,000 = nav 100,000,000); seeded-20 BUY → 57; seeded-100 SELL → 58; duplicate → `ALREADY_FIRED`; restart-reopen → `SKIPPED_TERMINAL`; reserved-row `recover` → `ABORTED`.
      - **Scope held / still OPEN:** the service + coordinator are library callables only — **no scheduler/daemon wires `process`, the real `runtime/paper` DB is untouched, no KIS/live path exists.** KRW-only valuation (multi-currency/FX deferred); a NORMAL-mode 70% invested low-band vs empty-account bootstrap tension is noted (REBALANCING/EMERGENCY/MDD downgrade it to WARNING — the pilot profile must pick the mode deliberately). Authoritative per-tick unattended invocation, process supervision, and market-hours scheduling remain **RTM-7**; real KIS WS transport is **RTM-6**; the transport-health final policy below stays **OPEN**. RTM-5/RTM-7a launch readiness stays **NO-GO** until the separate 1-day-pilot launch/readiness precheck runs. `TriggerEngine`/`RiskFilter`/`PaperBroker`/`Ledger`/journal/bridge unmodified; journal modules still import no broker/ledger/network (grep-verified); `git ls-files runtime` empty; `git diff --check` clean; acceptance 11 PASS / 0 WARN / 0 FAIL.
      - **RTM-7a hardening (operator, 2026-06-12) — 2 review items applied + 2 policy clarifications, each verified against the code before acting.** A post-commit review of `bf98938` flagged a coordinator↔valuation snapshot-coherence BLOCKER, a market-qualified mark-key correctness bug, a cost-basis terminology question, and a deep-immutability gap. Full suite 3090→3095 (+5: 6 new portfolio-context tests minus 1 removed redundant `snapshot_missing` direct test). (1) **Snapshot coherence (BLOCKER, CLOSED)** — the coordinator built the execution price (`BUY=ask`/`SELL=bid`) from the caller's frozen `snapshot`, but the service re-fetched the *same target symbol's* mark via a separate `market_state_source.get_snapshot()`, so `TriggerEngine`/execution price could run on snapshot A while NAV/current-symbol value ran on snapshot B (different quote, same `evaluated_at==now`) — RiskFilter slippage and `QuantityResolver` sizing could then reference divergent market states. **Fix:** `build_context` now takes `current_snapshot` and computes the **target symbol's** midpoint mark from that exact snapshot (refactored `_mark_for` → `_mark_from_snapshot`); the coordinator passes its frozen `snapshot`. A defensive `proposed_price.as_of == snapshot.quote_at` equality is enforced (`portfolio_proposed_price_mismatch`). Only *other* held positions' marks still come from `market_state_source`. Now execution price, slippage reference, and the target portfolio mark are provably one quote. (2) **Market-qualified mark cache (CLOSED)** — the internal `marks` cache and `PaperPortfolioValuation.marks` were keyed by `symbol` alone, but ledger position PK is `(symbol, market, account_role)`, so two same-string symbols in different markets could reuse the wrong mark. **Fix:** internal cache + `valuation.marks` are now keyed `(market, symbol)`; `RiskFilterContext.reference_prices` stays **symbol-keyed** (RiskFilter looks it up by `analysis_decision.symbol`) with the target symbol's reference written last as the authoritative value. (KRW-only v1 means cross-market same-symbol can't actually occur yet — US is USD and rejected first — but the data structure is now correct for RTM-6+.) (3) **`current_symbol_cumulative_buy_cost` semantics (CLARIFIED, no code change)** — it is `quantity × avg_cost` = **remaining cost basis** of the live position, not a historical cumulative including already-sold shares. For RiskFilter's single-position 5%-NAV *concentration* cap (`cumulative_after = remaining_cost_basis + additional_buy_cost`), remaining cost basis is the correct input; historical cumulative would over-count and wrongly block legitimate rebuys. Documented in the service docstring; the cross-cutting `RiskFilterContext` field name is intentionally **not** renamed in this lane (out of scope; would touch `risk/`). (4) **Deep immutability (CLOSED)** — `PaperPortfolioValuation.marks` is now a `MappingProxyType`, and `PaperPortfolioPolicy.metadata` is frozen to a `MappingProxyType` in `__post_init__`, so the "immutable snapshot" claim holds for the nested mappings (read-only views; regression tests assert `TypeError` on mutation). **Scope held:** `TriggerEngine`/`RiskFilter`/`PaperBroker`/`Ledger`/journal/bridge unmodified; service/coordinator remain library-only with no production caller; `git ls-files runtime` empty; `git diff --check` clean; acceptance 11 PASS / 0 WARN / 0 FAIL. **Remaining for RTM-7 launch precheck:** per-tick runtime wiring must pass the coordinator's canonical snapshot through to the valuation (now structurally enforced via `current_snapshot`), transport-health policy below, and the multi-currency/FX deferral.
    - **RTM-6 — KIS domestic real-time websocket read-only transport + operator smoke (operator, 2026-06-12) — transport/parser/smoke layer COMPLETE; no automatic caller, no real network exercised in this lane.** This closes the long-standing "official KIS WebSocket transport and field mapping remain UNVERIFIED" deferral (line 14): the real KRX real-time contract is now recorded and a network source feeds the existing `MarketEventSource`/`MarketMonitor` without any second reconnect loop. Full suite 3095→3162 (+67). The official KIS contract was verified field-by-field against the raw upstream source (not a lossy summary) and recorded in `docs/KIS_WS_CONTRACT.md` (approval `POST /oauth2/Approval` body `{grant_type, appkey, secretkey}` → `approval_key`; subscribe/unsubscribe envelope `header{approval_key, custtype:"P", tr_type:"1"/"2", content-type:"utf-8"}` + `body.input{tr_id, tr_key}`; `PINGPONG` control frame; market-data frame `flag|tr_id|data_count|^-delimited body` with flag `"0"` plaintext only and `"1"` encrypted rejected fail-closed; `H0STASP0` 59-field quote and `H0STCNT0` 46-field trade verbatim field indices; KST timestamp + receive-order sequence + health-separation policies).
      - **Pure parser `src/market_data/kis_official_ws_parser.py` (+ `tests/test_kis_official_ws_parser.py`).** `KisOfficialWsFrameParser.parse_frame(raw, *, received_at)` is network/env/fs-free (lives in `market_data`, allowed `zoneinfo` only) and builds `NormalizedTradeTick`/`NormalizedBestBidAsk` via the RTM-1 models. Fail-closed on: encrypted/unknown flag, unknown `tr_id`, field-count ≠ `record_len × data_count`, malformed `HHMMSS`/`BSOP_DATE`, naive `received_at`, crossed book, and any computed event time in the future (quote uses the received_at KST date + `BSOP_HOUR`; trade uses `BSOP_DATE + STCK_CNTG_HOUR`). Per-`(tr_id, symbol)` receive-order sequence starts at 1 and resets with a fresh parser per connection; `channel = "<tr_id>|<symbol>"`, `provider="kis"`. `ValidationError` is sanitized to `loc`+`msg` only — no raw frame/credential ever surfaces (sentinel test).
      - **Approval provider `src/data/kis_ws_auth.py` (+ `tests/test_kis_ws_auth.py`).** `KisWsApprovalProvider.issue_approval_key()` posts the approval body via an **injected structural HTTP transport Protocol** (no broker import); validates `status==200` + JSON object + non-empty `approval_key`; every failure path is sanitized (`from None`, no credential in the message), blank creds / non-positive timeout rejected at construction.
      - **Network source `src/data/kis_ws_source.py` (+ `tests/test_kis_ws_source.py`).** `KisWsMarketEventSource` implements `MarketEventSource` with the boundary "connect once → subscribe → yield events → disconnect" and **no internal reconnect/backoff/heartbeat loop** — `MarketMonitor` stays the single reconnect owner; a disconnect/iterator error reaches the monitor as a drop and a fresh source (new sequence epoch) resubscribes. `PINGPONG` → WebSocket `pong` echo + `MarketHeartbeat(channel="PINGPONG")`; subscription ack `rt_cd != 0` fails closed; non-JSON non-data control frame fails closed; `asyncio.CancelledError` re-raised after best-effort unsubscribe+close. Transport-health evidence (`KisWsTransportEvent`: connected/subscription_sent/ack/ping_received/pong_sent/unsubscribe_sent/disconnect) is **separate** typed evidence from market-data health, and carries no raw frame/token. `websockets` is imported lazily inside `open_kis_websocket` only (so `--validate-only` needs no socket stack). The source imports no broker/ledger/paper path (`websockets>=15.0` added to `pyproject.toml`; 16.0 installed).
      - **Config `KisWsReadOnlySettings` (`src/config/settings.py`, `config/config.toml.example`, `tests/test_settings.py`).** New `[broker.kis_ws_read_only]` section (default `enabled=false`, prod approval/ws URLs, `app_key_env`/`app_secret_env` are **variable names only**, bounded `max_subscriptions`, and a `confirmation_env_var`/`confirmation_phrase` gate that is **independent of `allow_live_trading`**). Read-only WS is allowed in paper mode because there is no order/balance/ledger path.
      - **Operator smoke `ops/run_kis_ws_readonly_smoke.py` (+ `tests/test_run_kis_ws_readonly_smoke.py`).** `--validate-only` is the default and emits a plan summary with **no credentials, no network, no fs writes**; `--run` is an explicit manual opt-in that requires, before any socket is opened, all of: `enabled=true`, the confirmation env phrase, and a bound (`--max-events` and/or `--duration-seconds`). The bounded core `execute_run(...)` is injectable (`connect` + `approval_transport`) so the full wiring (approval → `KisWsMarketEventSource` → `MarketMonitor` bounded run → transport-health aggregation → optional evidence under `runtime/`) is proven with a fake websocket + fake approval transport and zero real network. Evidence paths are constrained to `runtime/`. This lane **does not auto-run a real network smoke**; a real `--run` against KIS remains a separate, manually-gated operator action.
      - **Integration + isolation guards.** `tests/test_kis_ws_monitor_integration.py` drives the real `KisWsMarketEventSource` through `MarketMonitor` via `source_factory` including a drop→reconnect epoch (fresh source resubscribes, stream reset before the new epoch's first event, restarted sequence accepted, two connects observed). `tests/test_kis_ws_transport_isolation.py` are AST guards: the two `src/data` transport modules import no `broker`/`ledger`/`decision`/`paper_loop`/`paper_execution`/`llm`; `websockets` is not a module-top import in the source; the ops CLI calls `execute_run`/`open_kis_websocket` only inside `main()` and keeps an `if __name__ == "__main__"` guard so import alone never runs the network path. All RTM-6 tests use fakes — no socket/DNS/`getaddrinfo`/`websockets.connect()` is exercised.
      - **Inherited section K — deep-immutable `PaperPortfolioPolicy.metadata`.** The shallow `MappingProxyType(dict(...))` left nested dict/list values mutable and aliased to the caller's source; `__post_init__` now `_deep_freeze`s recursively (Mapping→`MappingProxyType`, list/tuple→tuple, set→frozenset, scalars as-is). Regression tests assert `TypeError` on nested mutation and that mutating the original source dict after construction does not affect the policy snapshot.
      - **RTM-6 live-smoke readiness hardening (operator review, 2026-06-12) — 6 items, each verified TRUE against the code before acting.** The post-`551602b` review found the implementation foundation complete but live-smoke readiness NO-GO. (1) **ACK barrier (was BLOCKER, CLOSED)** — `KisWsMarketEventSource` entered the receive loop right after sending subscribes and yielded market frames with no ACK confirmation; ACK only failed on an explicit non-zero string `rt_cd` (missing `rt_cd`, unknown `tr_id`, duplicate ACK all passed). The source now tracks `(tr_id, tr_key)` per requested subscription, **rejects any market frame until every subscription has a success ACK** (`pending_acks` empty), and fail-closes on missing/non-string/non-zero `rt_cd`, missing `tr_id`/`tr_key`, ACK for an unrequested pair, or a duplicate ACK. The verified official ACK schema (`header.tr_id`/`header.tr_key`, `body.rt_cd`/`msg1`, optional `body.output.iv/key`) from upstream SHA `33e0e1e6` is recorded in `docs/KIS_WS_CONTRACT.md §2.1`. (2) **Unsubscribed-symbol frame (was BLOCKER, CLOSED)** — parsed events are now checked against a `(tr_id|symbol)` channel allowlist derived from the active subscriptions; a frame for any other channel is fail-closed. (3) **Heartbeat-only false PASS (was BLOCKER, CLOSED)** — the CLI PASS gate was `summary.applied > 0`, which a `PINGPONG`→`MarketHeartbeat`→APPLIED satisfied with zero market data. The smoke now tallies trade/quote parsed·applied + heartbeat separately via a `MarketMonitor` `on_evidence` sink and **PASS = all-subscriptions-ACKed (`all_subscribed`) AND `quote_applied >= 1`**; heartbeat-only → FAIL. (4) **Cancellation cleanup (hardening, CLOSED)** — unsubscribe/close/disconnect now run exactly once via a single `finally` path (`except CancelledError` only marks `cancelled=True` and re-raises); a regression asserts close-count == 1 and one `disconnect` evidence with `detail="cancelled"`. (5) **Health-evidence completeness (hardening, CLOSED)** — every `KisWsTransportEvent` now carries a source-clock `at` timestamp; the run summary reports `connection_duration_seconds`, `subscriptions_expected`/`subscriptions_acked`, and a `market_health` block (trade/quote parsed·applied, `heartbeat_count`, `last_market_event_at`). (6) **Config hardening (CLOSED)** — `KisWsReadOnlySettings.__post_init__` validates `environment ∈ {prod, vps}`, `approval_base_url` scheme=`https`+host, `websocket_url` scheme∈`{ws,wss}`+host, and positive timeouts/`max_subscriptions`; the CLI `_resolve_symbols` enforces a 6-digit KRX code and de-dupes symbols, and `build_subscriptions` de-dupes `(tr_id, symbol)` pairs. Regression coverage added per the operator list (data-before-ACK reject, partial-ACK reject, unknown/duplicate/missing-`rt_cd` ACK reject, unsubscribed-symbol reject, heartbeat-only FAIL, quote-1-건+all-ACK PASS, cancel-cleanup-once, duplicate-symbol dedupe, env/URL/scheme settings rejection).
      - **Scope held / still OPEN:** the transport + parser + smoke are library/CLI callables only — **no scheduler/daemon wires them, no automatic `--run`, no real KIS network exercised in this lane, no broker/ledger/paper-execution path touched.** Real per-tick unattended invocation, process supervision, and market-hours scheduling remain **RTM-7**; the transport-health final policy below stays **OPEN** (RTM-6 supplies the transport-vs-market-data evidence separation it needs). Live-smoke readiness remains gated on the operator precheck + 장중 bounded `--run` + evidence review before any pilot. `git ls-files runtime` empty; `git diff --check` clean; acceptance 11 PASS / 0 WARN / 0 FAIL.
    - **Transport-health final policy — PARTIALLY CLOSED (RTM-7b hardening, 2026-06-13).** Separated transport/market-data health policy, typed supervisor actions, and offline full-day integration are implemented in RTM-7b hardening (see above). **Remaining OPEN:** production threshold calibration from live-smoke evidence, real KRX calendar provider, runtime wiring of adapter→supervisor→coordinator, and unattended pilot activation.
    - **RTM-7b correctness hardening (operator, 2026-06-13) — runtime correctness REVISE items CLOSED offline; runtime wiring still NO-GO.** Closes eight correctness defects on the 7b foundation without new scheduler/execution wiring. Full suite 3243→3231 (net refactor of overlapping tests). See `docs/MARKET_SUPERVISOR_CONTRACT.md`. **(C1) ACK completion** — adapter maps per-subscription `ack`/`subscribed` as informational only; only `all_subscribed` sets tracker completion; unknown kinds → `AdapterError`. **(C2) Subscription grace** — explicit `subscription_grace_seconds`; connected+incomplete → `WARMING` (no restart); grace exceeded → `UNHEALTHY`. **(C3) Rolling restart budget** — `_total_restarts` (lifetime) separated from `_restart_times` deque (window); `restarts_in_current_window` in evidence/summary; session close clears window only. **(C4) Sticky FAILED_CLOSED** — factory/sink failures terminal; no next-tick overwrite; cancellation preserves `CancelledError`. **(C5) Starvation vs transport exit** — `MonitorExitReason` typed classification; STARVED+running monitor → `HOLD_EXECUTION_ONLY` (restart 0); transport exit restart reason = `transport_session_absent`. **(C6) Epoch isolation** — new `connected` resets pong/subscription counters; delayed old-epoch events rejected. **(C7) Completed-epoch flapping** — `ConnectionEpochResult` bounded deque; short-unstable = not stable; flapping before WARMING. **(C8) Full-day long-running integration** — `test_full_day_long_running_exact_counts` with exact start/restart/cancel counts. **(C9) Rehearsal CLI** — `is_relative_to(runtime)` path guard; actual transition aggregation; long-running fixture mode. **Still OPEN:** real KRX holiday provider, production threshold calibration from live-smoke, KIS live `--run`, broker/ledger/coordinator wiring, scheduler/daemon, unattended pilot, runtime default activation.
    - **RTM-7b hardening (operator, 2026-06-13) — offline layer HARDENED; superseded for correctness details by RTM-7b correctness hardening above.** Builds on the 7b.1–7b.4 foundation with typed policies, evidence adapter, full-day supervisor orchestration, and offline rehearsal CLI. See `docs/MARKET_SUPERVISOR_CONTRACT.md`. **(H1) Calendar** — `ExplicitMarketScheduleProvider` (production contract: schedule-missing weekday → `UNKNOWN`/`CALENDAR_MISSING`/`WAIT_FOR_CALENDAR`; weekend-without-schedule → `CLOSED`; defensive copy/freeze); `FixtureMarketCalendar` marked test-only (fail-open weekdays no longer production). **(H2) Health timestamp + strictness** — `record_*` rejects naive/future/out-of-order/unknown-kind with typed `RecordResult` and state immutability on reject; `TransportHealthStatus`/`MarketDataHealthStatus` enums; strict `is_healthy` (= transport HEALTHY ∧ market_data HEALTHY); `is_execution_ready` conservative; ping/pong timeout and flapping in transport verdict. **(H3) Transport vs market-data action split** — `SupervisorAction.HOLD_EXECUTION_ONLY` for OPEN starvation/stale (monitor kept, no restart); `RESTART_TRANSPORT` only for transport FLAPPING/UNHEALTHY; regression `test_starvation_hold_execution_no_restart`. **(H4) Evidence adapter** — `src/data/market_supervisor_adapter.py` maps `KisWsTransportEvent`/`MonitorEvidence` → neutral signals; `market_data→data` import guard tested; unknown events fail-closed. **(H5) Supervisor restart semantics** — `monitor_initial_starts` vs `monitor_restarts` separated; `max_restarts_in_window` counts restarts only; session close resets restart budget. **(H6) Full-day integration** — `test_full_day_supervisor_orchestration` runs real `MarketSupervisor` + `ExplicitMarketScheduleProvider` + scripted monitor (not health-only replay). **(H7) Offline rehearsal CLI** — `ops/rehearse_market_supervisor.py` (fixture JSON, fake clock, deterministic summary stdout, optional `runtime/` evidence). **Still OPEN:** real KRX holiday provider, production threshold calibration, KIS live smoke, broker/ledger/coordinator wiring, scheduler/daemon, unattended pilot, runtime default activation.
    - **RTM-7c.2 — fast-loop paper execution orchestration (offline library wiring) — CLOSED offline; runtime activation NO-GO.** `MarketMonitor` gains neutral `AppliedMarketUpdate` + optional `on_applied_update` hook (APPLIED trade/quote only; exact shared `now`; hook failure → `MonitorInternalError`, supervisor sticky `FAILED_CLOSED`, transport restart 0). New `src/orchestration/fast_loop_execution.py` (`FastLoopExecutionOrchestrator`) and `src/orchestration/execution_gate.py` wire post-apply → session/health gate → `ActiveDecisionStore.read_active` → frozen snapshot/indicator → `PaperExecutionCoordinator.process()` synchronously (no queue/coalescing). `docs/FAST_LOOP_EXECUTION_CONTRACT.md`. **Still OPEN:** KIS live `--run`, production runtime DB, scheduler daemon, throughput/latency validation, unattended pilot, default runtime activation.
    - **RTM-7c.2 correctness hardening — CLOSED offline.** Five defects closed without new features: **(A)** COMMITTED + evidence sink failure now sets `_global_terminal=True` while preserving current `COMMITTED` result (no order rollback, subsequent coordinator 0); **(B)** complete malformed `AppliedMarketUpdate` preflight (typed reject, no dependency calls, no external exceptions); **(C)** `StaticExecutionInputsProvider` allocator `universe`/`created_at` binding; **(D)** orchestrator-level rolling context regression (READY pass-through, WARMING readiness, `rolling_store=None` → `indicators=None`); **(E)** `RECONCILE_REQUIRED` symbol halt + cross-symbol isolation + new-instance reset; monitor hook future/stream-mismatch callback 0; `orchestration` package lazy export of gate/fast-loop public API. RTM-7c.3 full-day two-loop rehearsal **NOT STARTED**. Runtime activation remains **NO-GO**.
    - **RTM-7c.3 — deterministic full-day offline two-loop rehearsal — CLOSED offline; runtime activation NO-GO.** `tests/test_full_day_two_loop_rehearsal.py` wires slow loop (`DecisionRefreshScheduler` + scripted runner + separate `ActiveDecisionStore` writer/reader on one SQLite file) with fast loop (`MarketMonitor` → adapter/health → `FastLoopExecutionOrchestrator` → paper stack) under fake clock for `2026-06-15` with explicit four slots. Proves: one BUY fill (57 sh / 96,010,000 KRW cash), duplicate prevention across repeated ticks / reconnect epoch / fast-loop restart / scheduler restart, HOLD replacement, runner failure preserving prior active, false-condition s4 publication with order 0, supervisor POST_CLOSE stop, and failure paths (corrupt active store, health starvation gate, scheduler evidence sink, COMMITTED evidence sink latch). Preconditions: real malformed `AppliedMarketUpdate` field branches; real rolling READY/WARMING/`rolling_store=None` integration via `PaperExecutionCoordinator`. `docs/TWO_LOOP_REHEARSAL_CONTRACT.md`. Orchestrator maps `CoordinatorResult.trigger_reason` into `FastLoopExecutionResult.reason_code` when `reason_code` is absent. **Hardening (operator, 2026-06-13) — `9e43f13`:** the rehearsal now asserts the *actual* duplicate-prevention mechanism at each layer rather than only "no extra fill": (a) `on_applied` no longer double-records the market event into the health tracker — the `on_evidence` apply→`MarketSupervisorAdapter`→tracker path is the single source (apply evidence is emitted before the post-apply hook, so the gate sees the latest state); (b) `run_monitor_reconnect` returns the `MonitorSummary` and the test asserts the **per-stream** epoch reset (trade stream reset → new `sequence=1` APPLIED; unobserved quote stream not reset → its `sequence=1` correctly `OUT_OF_ORDER`); (c) the **fast-loop restart duplicate is blocked by portfolio-state quantity resolution (`TRIGGERED_ABORTED`/`no_executable_quantity`), NOT journal dedup** — the idempotency key embeds in-memory `activation_epoch` (reset on restart → different key → journal gains an `ABORTED` row, final rows = COMMITTED 1 + ABORTED 1 = 2); the *within-arming* repeated-tick duplicate is blocked earlier by the engine fire budget (`max_fires_reached`), so the journal `SKIPPED_TERMINAL` path is not exercised in this rehearsal. Journal-based cross-process exactly-once is proven separately in the F1b integration suite (identical-key `SKIPPED_TERMINAL`); persistent activation-epoch restore stays deferred (F1). The `docs/TWO_LOOP_REHEARSAL_CONTRACT.md` "journal blocks duplicate" line was corrected accordingly. **Still OPEN:** production runtime daemon composition (RTM-7c.4+), KIS live `--run`, throughput/latency validation, unattended pilot, default runtime activation.
    - **RTM-7c.4a — offline paper fast-loop composition root + operator verification tooling — CLOSED offline; runtime activation NO-GO.** New package `src/composition/` is the only place allowed to wire broker/ledger/execution/orchestration into a runnable stack; `src/orchestration` purity is preserved. Three offline capabilities in `src/composition/paper_fast_loop.py`: (1) `build_paper_fast_loop_plan` validate-only — loads + validates the on-disk execution-inputs snapshot and runs single-symbol (KR / 6-digit / PAPER / KRW) preflight against any *existing* ledger positions (read-only scan; ledger not created if absent); typed reason codes `snapshot_file_missing`/`snapshot_not_yet_valid`/`snapshot_expired`/loader codes + `unsupported_market`/`unsupported_account_role`/`unsupported_currency`/`foreign_position_present`. (2) `inspect_paper_fast_loop` read-only summaries of ledger/journal/active-store via `src/composition/sqlite_inspector.py` (`file:...?mode=ro` URI + `PRAGMA query_only=ON`; never constructs the stores, never writes/migrates, sanitized counts + non-secret `decision_id`/`plan_id` + integer-quantity strings; reports `missing_databases`). (3) `replay_offline` deterministic replay of the real execution stack against built-in fixtures `("buy_fill","hold_noop")` using **caller-provided temp paths only** — never the configured `runtime/` paths; `buy_fill` → 1 `committed`, position `"57"`, 1 terminal journal row; `hold_noop` → no fill. Replay primes health/quote preconditions (connect/all_subscribed at decision time for uptime; a `best_bid_ask` market event for market-data HEALTHY; a seeded `NormalizedBestBidAsk` quote so the coordinator can price) to reach a deterministic COMMITTED. Operator CLI `ops/run_paper_fast_loop.py` exposes four mutually-exclusive modes (default `--validate-only` exit 0/1; `--inspect-existing` exit 0; `--replay FIXTURE` OS-temp-dir exit 0/1; `--run` **REFUSED** → `outcome=NO_GO`/`reason_code=live_run_not_implemented` with non-zero exit **before** any credential read/network socket/production-DB write/filesystem path creation; `--json` sanitized — no credentials/raw frames/exception reprs/tracebacks/DB dumps). Import guard `tests/test_composition_import_guard.py` forbids `socket`/`websocket(s)`/`http`/`httpx`/`urllib`/`requests`/`data`/`llm` over all `src/composition/*.py` + the CLI, while allowing the composition-root first-party packages (broker/ledger/execution/orchestration/market_data/risk/paper_loop/domain/allocator/decision/analysis/config/composition). **Documented Section-17 deviation:** `execution` is allowlisted at the composition boundary by design — the keystone snapshot module imports `execution` for `PaperPortfolioPolicy` and the composition root must construct the real `PaperExecutionCoordinator`; the repo already allowlists `execution` for `orchestration/fast_loop_execution.py`. Network/credential roots stay hard-forbidden regardless. `config/config.toml.example` gains `[runtime.paper_fast_loop]` (enabled=false). `docs/PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md`. **Still OPEN:** KIS live `--run` implementation, production runtime daemon composition, scheduler/process supervision wiring, throughput/latency validation, unattended pilot, default runtime activation.
    - **RTM-7c.4a correctness hardening — operator-tooling fixes (offline; runtime activation still NO-GO).** Six correctness defects in the verification tooling are resolved: (B1) `build_paper_fast_loop_plan` validate-only no longer opens any database — it is pure config + snapshot validation and never scans/creates the ledger; single-symbol position/account-role/currency preflight moved into `inspect_paper_fast_loop`. (B2) `inspect_paper_fast_loop` is now fail-closed: it returns a typed `PaperFastLoopInspection.outcome` (`InspectionOutcome.OK`/`NO_GO`) with sanitized `reasons`, emitting `NO_GO` (CLI non-zero exit) on a missing DB (`missing_database:<x>`), an invalid required schema (`<db>_missing_table:`/`<db>_missing_column:` via read-only `PRAGMA`), an unreadable file (`<db>_unreadable:<code>`), a dangling active pointer (`dangling_active_pointer`, LEFT-JOIN detection distinguishing corruption from "no active"), a non-terminal journal row (`nonterminal_journal_entries`), or a position-preflight failure. (B3) ledger summary counts the real `OrderStatus` enum (`order_result_count` + per-status `filled`/`rejected`/`pending`/`cancelled`) instead of a non-existent `COMMITTED` status that was always 0. (B4) replay now feeds the **real** `ValidatedExecutionInputsProvider` loaded from a canonical hash-stamped snapshot through the orchestrator; tampered (load fail → `snapshot_loaded=false`), stale, and universe-mismatch snapshots all yield zero fills (`first_status=execution_inputs_unavailable`), proven by E2E tests, while a valid snapshot fills once. (B5) replay drives three phases on the same on-disk DBs and exposes `first_status`/`repeat_status`/`restart_status` + `order_result_count`/`fill_count`/`journal_state_counts`/final cash/position: first event → `committed`, repeat on the same stack → `suppressed` (max-fires), and a composition restart against the same DBs → `skipped_terminal` (journal idempotency dedup) — net exactly 1 fill, 1 terminal journal row, 0 nonterminal. (Additional) `main()` wraps `inspect`/`replay` in `try/except` that converts any internal/sqlite failure into a sanitized `outcome=FAIL`/`reason_code=<inspect|replay> error: <ExceptionType>` with non-zero exit — no traceback or raw sqlite text escapes. Docs `docs/PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` updated; `--run` early-refusal, config path safety, snapshot schema/loader, and the composition import boundary are unchanged.

    - **RTM-7c.4b correctness hardening & readiness completion — operator-tooling fixes (offline; runtime activation still NO-GO).** Closes the RTM-7c.4a review findings as one hardening lane, no new runtime features: **(A) active pointer identity integrity.** `inspect_active_decision` now verifies the configured `(market, symbol)` pointer against the referenced version's columns, the bundle's internal `decision.market`/`decision.symbol`/`decision_id`/`created_at`, and (when present) the plan's `market`/`symbol`/`decision_id`; a pointer referencing a foreign-but-internally-consistent version is `identity_mismatch` → `active_pointer_identity_mismatch` NO_GO, kept distinct from `dangling_active_pointer` (pointer to a missing version). **(B) validity fail-closed.** `valid_from`/`expires_at` are treated as integrity, not best-effort: a new `_parse_required_aware_datetime` requires present + ISO-parseable + timezone-aware + `valid_from <= expires_at`; missing/unparseable/naive/reversed → `corrupt` → `active_bundle_corrupt` with `currently_valid=False` (a parse failure is never downgraded to "currently valid"). `inspect_paper_fast_loop` now requires a timezone-aware `now` (naive → `ValueError`). **(C) partial-construction cleanup.** `_build_stack` initializes the three SQLite handles to `None` and wraps the whole construction; if any later store constructor or in-memory dependency raises, every already-opened handle is closed in reverse order (active-store → journal → ledger) and the original exception is re-raised — a cleanup-time `close` failure is swallowed so it never masks the original. **(read-only evidence)** `sqlite_inspector.open_read_only` appends `immutable=1` only when the DB is already quiescent (no live `-wal`/`-shm`/`-journal` sidecar), so a WAL-mode reader no longer materializes a `-shm`/`-wal` sidecar; a present sidecar fails closed with `database_not_quiescent:<db>` and keeps a faithful `mode=ro` read. New regression coverage: active-decision integrity matrix (`tests/test_paper_fast_loop_inspector.py`), end-to-end OK readiness + read-only byte-hash/size/user_version/sidecar invariance + quiescence + identity-mismatch + constructor-spy proof that inspect never builds the stores (`tests/test_paper_fast_loop_composition.py`), resource-lifecycle file (`tests/test_paper_fast_loop_resource_lifecycle.py`: exactly-once/idempotent close, partial-construction reverse-order cleanup, cleanup-error tolerance, restart, temp-dir deletable), and CLI exit-code/sanitization cases (`tests/test_run_paper_fast_loop.py`). `docs/PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` updated with snapshot/active/quiescence reasons, pointer↔version↔bundle↔plan identity, validity-as-integrity, the `immutable=1`-when-quiescent read policy, and the `PaperFastLoopStack` lifecycle. **Still OPEN:** KIS live `--run` implementation, production runtime daemon composition, scheduler/process supervision wiring, throughput/latency validation, unattended pilot, persistent activation-epoch restore, default runtime activation.
    - **RTM-7c.4b additional closure — independent-review findings P1-A/P1-B/P2-C (offline; runtime activation still NO-GO).** **[SUPERSEDED IN PART by the RTM-7c.4b second-round closure entry below — history retained, not rewritten: the "BUY-without-plan → corrupt" inspector test contract recorded in this entry was corrected in the second round. The current stable inspect reason for a *recognized* BUY/SELL action carrying no plan (and for a HOLD carrying one) is `active_plan_consistency_mismatch`, not `active_bundle_corrupt`; `corrupt` is now reserved for unknown/malformed actions and JSON/hash/publication-id/model/validity corruption. The references below to "BUY without a plan" being rejected by the runtime reader remain accurate; only the *inspect reason code* for that case changed.]** Three follow-up correctness items from the independent review, no new runtime features. **(P1-A) inspector ↔ runtime model-validation parity.** The read-only `inspect_active_decision` previously reconciled hash/publication-id/identity/validity but accepted any payload whose `decision`/`plan` *dicts* carried the checked identity fields — it never restored the pydantic models, so a bundle the runtime reader (`ActiveDecisionStore._row_to_active_bundle`) would reject (incomplete `AnalysisDecision`, BUY without a plan, plan/decision time-binding violation) was reported integrity-OK. A new shared pure helper `orchestration.active_decision_store.deserialize_validated_bundle` performs the model restoration; both the runtime reader and the inspector call it (single source → no drift). The inspector runs it as the final integrity gate (after identity/validity so those more-specific reasons still win), failing closed to `active_bundle_corrupt` otherwise. The inspector's required `decision_bundle_versions` schema also gains the two columns the runtime reader reads (`source_payload_hash`, `published_at`); a store missing them now fails the schema check instead of being treated as inspectable. The inspector module stays free of store construction (it imports only the pure helper). **(P1-B) single-reason for a dangling pointer.** A configured pointer to a missing version row emitted *both* `dangling_active_pointer` (store-summary LEFT-JOIN) and `active_bundle_corrupt` (the active-decision inspector's `else` branch). `_inspect_active_decision` no longer adds `active_bundle_corrupt` for the dangling integrity reason, so a missing-version pointer surfaces as exactly one `dangling_active_pointer`; `active_bundle_corrupt` is reserved for JSON/hash/publication-id/model/validity corruption and `active_pointer_identity_mismatch` for foreign identity. **(P2-C) teardown-order proof + unified close order.** `PaperFastLoopStack.close()` now closes in construction-reverse order (active-store → journal → ledger), matching the partial-construction cleanup path (previously `close()` closed forward). The resource-lifecycle tests record a single shared ordered close-event log across all three real SQLite handles and assert the exact teardown sequence on every path (normal close, body-exception close, each partial-construction failure, cleanup-error tolerance), not merely a per-handle close count. New/updated coverage: `tests/test_paper_fast_loop_inspector.py` (model-invalid → corrupt, BUY-without-plan → corrupt, runtime-reader schema columns required, valid-buy now uses full real models), `tests/test_paper_fast_loop_composition.py` (dangling pointer → exactly one reason), `tests/test_paper_fast_loop_resource_lifecycle.py` (ordered close-event log). `docs/PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` updated with the model-restoration parity, the single-reason dangling rule, and the unified reverse teardown order. **Candidate next lane (RTM-7c.4c) — NOT started, explicit Operator approval required, runtime activation NO-GO:** before any runtime activation precheck the following stay OPEN — confirm all DB writers/processes stopped, verify pre/post quiescence, and obtain explicit Operator approval; no lock/daemon/launchd/systemd/cron/scheduler/process-supervision/kill/activation work is in scope until then.
    - **RTM-7c.4b second-round closure — independent-review REVISE (offline; runtime activation still NO-GO).** Three correctness regressions/gaps the independent review found after the first-round closure above. **(P1-A redux) the stable `active_plan_consistency_mismatch` reason had become unreachable.** Placing the shared `deserialize_validated_bundle` model-restoration gate at the end of `_verify_active_version` meant `DecisionTriggerBundle`'s own BUY/SELL-needs-plan / HOLD-forbids-plan validation collapsed a wrong plan presence into the generic `active_bundle_corrupt`, and `paper_fast_loop._plan_consistent` (which only runs on the integrity-OK path) became dead code — contradicting the documented contract that BUY/SELL-without-plan and HOLD-with-plan emit `active_plan_consistency_mismatch`. Fix: the inspector now checks plan-presence consistency **before** model restoration, emitting a dedicated internal integrity reason `plan_consistency_mismatch` that `_inspect_active_decision` maps to the stable `active_plan_consistency_mismatch`; a *recognized* action with the wrong plan presence yields exactly that reason, while an *unknown/malformed* action still falls through to `active_bundle_corrupt`. The dead `_plan_consistent` path was removed (the integrity layer now owns the classification). **(P1-B) `published_at` runtime-reader parity.** The inspector's required schema added the `published_at` column but never read or value-validated it, while the runtime reader parses it as a timezone-aware datetime — so a malformed/naive `published_at` (not part of the hashed payload) passed inspect yet would be unreadable at activation time. Fix: the inspector now selects `published_at` and requires it to be present, ISO-parseable, and timezone-aware, failing closed to `active_bundle_corrupt` otherwise (`source_payload_hash` is left at column-existence parity only — the runtime reader does not validate its value, so no over-strict rule is added). **(P2-C) runtime stored-datetime fail-closed.** `ActiveDecisionStore._row_to_active_bundle` parsed `valid_from`/`expires_at`/`decision_created_at`/`published_at` with an unguarded `_parse`, so a malformed/naive stored value leaked a raw `ValueError`/timezone error instead of the documented `PublicationError`. Fix: a `_parse_stored_datetime` helper normalizes every read-path datetime parse into `PublicationError` (original exception chained, message sanitized to the field name, no fallback, no state change). New/updated coverage: `tests/test_paper_fast_loop_inspector.py` (BUY-without-plan → `plan_consistency_mismatch`, HOLD-with-plan → `plan_consistency_mismatch`, unknown action → corrupt, malformed/naive `published_at` → corrupt; fixture schema gains the runtime columns and a legacy-schema variant proves the schema check), `tests/test_paper_fast_loop_composition.py` (HOLD-with-plan E2E → exactly one `active_plan_consistency_mismatch`, never `active_bundle_corrupt`), `tests/test_active_decision_store.py` (malformed/naive stored datetimes → `PublicationError`). `docs/PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` and the store docstrings updated for the plan-consistency-before-restoration ordering, `published_at` value validation, and read-path datetime fail-closed. RTM-7c.4c remains candidate-only, explicit-Operator-approval-required, runtime activation NO-GO.
    - **RTM-7c.4c — attended bounded fast-loop runtime precheck (read-only; runtime activation still NO-GO).** Adds a read-only operator precheck that defines a mechanical readiness check and *proves it is read-only*; it activates no runtime, opens no KIS, reads no credential, touches no network, places no order, starts no daemon/scheduler, and writes no operational DB. `composition.paper_fast_loop.precheck_runtime(*, settings, now, base_dir)` (tz-aware `now` required; naive → `ValueError`) fingerprints all four configured artifacts (execution-inputs JSON snapshot + ledger / trigger-journal / active-decision SQLite DBs), reuses the RTM-7c.4b `inspect_paper_fast_loop` body verbatim (constructs no store, no schema create/migrate, no reconcile), then re-fingerprints. The artifact fingerprint is side-effect-free — present / is_regular_file / size / byte SHA-256 / SQLite `user_version` read from the **header bytes (offset 60, big-endian)** rather than by opening a connection (a WAL-mode read could otherwise materialize a `-shm`/`-wal` sidecar) / sidecar-suffix set (`composition.sqlite_inspector.ArtifactFingerprint` + `fingerprint_artifact`). **Four distinct gates, never conflated:** the typed `RuntimePrecheckResult.machine_outcome` (`MachineCheckOutcome.PASS`/`NO_GO`) is the *only* thing decided here — `PASS` iff the reused inspection is OK **and** every fingerprint is byte-identical before/after; the three activation fields are constants that always hold (`activation_authorized=False`, `runtime_activation_outcome="no_go"`, `explicit_operator_approval_required=True`, `writers_stopped_manual_confirmation_required=True`). A machine PASS is necessary-but-not-sufficient and is **never** an activation authorization. Two precheck-specific reasons append to the reused inspection reasons: `precheck_artifact_changed:<name>` (before≠after → the precheck mutated state, fail-closed even when the inspection says OK) and `precheck_artifact_not_regular_file:<name>` (present but directory/socket/fifo — covers the JSON snapshot, which inspect's `open_read_only` does not open). **Drift-avoidance decision:** a *missing* artifact is reported only by the reused inspect layer (`missing_database:<db>` / `missing_execution_inputs_snapshot`); precheck deliberately does **not** emit a `precheck_artifact_missing` code (follows the P1-B single-reason-per-condition precedent). **Quiescence semantic limit (documented):** sidecar absence proves only *momentary* quiescence at the instant of the check — there is no process scan / PID inspection / OS-lock acquisition in scope — so writer-stop stays a machine-unverified MANUAL requirement, never inferred from a machine PASS. Operator CLI gains a fifth mutually-exclusive mode `--precheck-runtime` (read-only; machine `PASS`→exit 0, `NO_GO`→exit 1; JSON carries `machine_check_outcome`, the four activation fields, `reasons`/`inspection_*`/`missing_databases`, `fingerprints_before`/`_after`, and the no-side-effect attestations `network_called`/`credential_read`/`broker_called`/`production_db_written`/`runtime_file_created` all false; internal failures sanitized to `reason_code=precheck error: <ExceptionType>`). **Carry-over hardening (3 items) folded in at lane start:** (H1) the RTM-7c.4b first-round TECH_DEBT entry is marked SUPERSEDED-IN-PART (history retained, not rewritten) for its outdated BUY-without-plan inspect-reason contract; (H2) `tests/test_active_decision_store.py` replaces the asymmetric stored-datetime test with a symmetric 4×2 matrix (`decision_created_at`/`valid_from`/`expires_at`/`published_at` × malformed/naive → `PublicationError`); (H3) `tests/test_paper_fast_loop_composition.py` adds the BUY-without-plan E2E arm symmetric to the existing HOLD-with-plan arm (exactly one `active_plan_consistency_mismatch`, never `active_bundle_corrupt`). New coverage: precheck unit/E2E/isolation in `tests/test_paper_fast_loop_composition.py` (machine PASS on a fully-seeded quiescent stack with byte-identical fingerprints and zero new sidecars; missing-DB / non-quiescent / monkeypatched-mutation-detected `precheck_artifact_changed:ledger` / not-regular-file fail-closed; constructor-spy proves precheck builds no store; read-only/no-sidecar invariant; tz-aware-now guard) and precheck CLI in `tests/test_run_paper_fast_loop.py` (machine PASS exit 0 with all activation fields false/no_go/true/true, missing-DB NO_GO exit 1, mode exclusivity, non-quiescent fail-closed + sanitized output). New `docs/PAPER_FAST_LOOP_RUNTIME_PRECHECK_CONTRACT.md`; `docs/PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` CLI table updated to five modes. **Still OPEN (no change to runtime posture):** KIS live `--run` implementation, production runtime daemon composition, scheduler/process-supervision wiring, manual writer-stop confirmation, explicit Operator activation approval, throughput/latency validation, unattended pilot, persistent activation-epoch restore, default runtime activation.
    - **RTM-7c.4c safety closure — independent-review P1/P2/P3 follow-up (offline; runtime activation still NO-GO).** Closes the independent-review safety findings on top of the RTM-7c.4c precheck lane without expanding runtime scope. **(P1) precheck CLI credential/env isolation.** The shared `load_settings(..., environ=None)` path reads `os.environ` for `${ENV}` substitution and live-confirmation/credential gates; precheck mode now injects an explicit empty environ (`load_settings(args.config, environ={})`) so the process environment is never touched — a literal config prechecks with zero env access; `${KIS_LIVE_APP_KEY}` and live-mode configs fail closed with sanitized `config error: ConfigEnvironmentError` / `RuntimeGateError` and never resolve or read secrets. Real env-access spy tests replace `config.settings.os.environ` with a shim that raises on any access and prove zero reads on the normal, env-placeholder, and live-config paths. **(P2) irregular-artifact single canonical aggregate reason.** When `precheck_artifact_not_regular_file:<name>` fires, the inspection layer's generic unreadable/invalid reason for the *same* artifact is dropped from aggregate `reasons` (kept verbatim in `inspection.reasons` for diagnostics) via `_IRREGULAR_ARTIFACT_OWNED_INSPECTION_REASONS`; missing artifacts still add no duplicate precheck reason. Parameterized matrix covers all four artifacts (`execution_inputs_snapshot` → `execution_inputs_invalid`; `ledger` → `ledger_unreadable:sqlite_not_a_file`; `trigger_journal` → `journal_unreadable:sqlite_not_a_file`; `active_decision_store` → `active_store_unreadable:sqlite_not_a_file`). **(P3) memory-bounded fingerprint + semantic limits.** Artifact SHA-256 streams in 1 MiB chunks (no `Path.read_bytes()` whole-file load); SQLite `user_version` parses from the first 100 header bytes without opening a connection (non-SQLite/absent/short/non-magic → `None`). `ArtifactFingerprint` / precheck contract docstrings corrected: before/after equality proves no *net* observable drift across the window, not concurrent-writer absence; a difference proves observable state changed during the window, not necessarily that precheck itself wrote; mutate-then-restore within the window is undetectable. CLI module docstring corrected to five modes; `--run` early refusal remains before settings loading. **Still OPEN (unchanged posture):** KIS/network/credential/broker/order/daemon/scheduler/process-supervision wiring, operational DB write, schema migration, journal reconcile, manual writer-stop confirmation, explicit Operator activation approval, throughput/latency validation, unattended pilot, persistent activation-epoch restore, default runtime activation.
    - **RTM-7c.4d — ephemeral precheck state receipt (stdout-only; runtime activation still NO-GO).** Binds mechanical precheck outcome to exact observed fingerprints, reasons, market/symbol, and `checked_at` via `RuntimePrecheckReceipt` + `receipt_sha256` (canonical JSON via existing `decision.canonical_json.payload_sha256`; no duplicate canonicalizer, no signing key/HMAC, no file persistence, no approval input). `RuntimePrecheckResult.receipt` is emitted for both `PASS` and `NO_GO`; activation fields on outer result and receipt always match (`_precheck_activation_posture()` single source). CLI `--precheck-runtime --json` adds nested `precheck_receipt` (top-level fields retained). Carry-over: quiescence doc corrected (equality = net drift absent, not read touched nothing / writer absence); live-config env-isolation test strengthened (`RuntimeGateError`, zero env access, sanitized stdout/stderr). New `docs/PAPER_FAST_LOOP_PRECHECK_RECEIPT_CONTRACT.md`. **Still OPEN (unchanged posture):** KIS live `--run`, daemon/scheduler wiring, manual writer-stop confirmation, explicit Operator activation approval, receipt file persistence, TTL/expiry, default runtime activation.
    - **RTM-7c.4e — ephemeral precheck receipt verification (stdin-only; runtime activation still NO-GO).** Pure `composition.precheck_receipt_verifier.verify_runtime_precheck_receipt_payload` validates strict schema, semantic consistency, and `receipt_sha256` recomputation (`VALID` ≠ authenticity/approval/freshness/activation). Builder hardening: `PrecheckReceiptError` on malformed `checked_at`, wrong fingerprint count/order/names. CLI `--verify-precheck-receipt` (stdin-only receipt object, 1 MiB bound, before `load_settings`; no config/env/DB/fs write/network/clock read). Tests: builder matrix, verifier tampering/schema, emitted nested receipt hash recomputation (PASS/NO_GO), stdin bound, isolation spies. New `docs/PAPER_FAST_LOOP_PRECHECK_RECEIPT_VERIFICATION_CONTRACT.md`. **Still OPEN (unchanged posture):** signing/HMAC, receipt file I/O, envelope extraction, TTL, approval input, activation token, KIS `--run`, daemon wiring, default runtime activation.
    - **RTM-7c.4e safety closure — untrusted JSON parser + builder/verifier parity (offline; runtime activation still NO-GO).** Closes independent-review P1/P2 without expanding scope. **(P1) stdin JSON fail-closed.** `composition.precheck_receipt_stdin_json.parse_receipt_stdin_json` replaces bare `json.loads`: catches `ValueError` (oversized int), `RecursionError` (deep nest), duplicate keys via `object_pairs_hook`, non-standard `NaN`/`Infinity` via `parse_constant`; sanitized reasons `receipt_input_not_json` / `receipt_input_too_deep` / `receipt_input_duplicate_key`; no traceback escape. **(P2) shared schema.** `composition.precheck_receipt_schema` is single source for builder + verifier; invariant tested: builder success → verifier VALID; market/symbol/outcome/fingerprint semantic parity; removed unreachable `receipt_fingerprint_identity_mismatch`. **Exact 1 MiB bound** tested with whitespace-padded valid receipt at `len==limit`, `limit+1` read probe spy. **Still OPEN (unchanged posture):** signing/HMAC, receipt file I/O, TTL, approval, activation, KIS `--run`, daemon wiring.
    - **RTM-7c.4e final parity closure — strict fingerprint types + ASCII symbol + stdin read fail-closed (offline; runtime activation still NO-GO).** Closes independent-review strict-type parity gap: builder and verifier now share `validate_fingerprint_semantics` via `_strict_fingerprint_fields_from_object` / `parse_fingerprint_dict` normalization — int-as-bool, float size, absent+sidecar, absent+regular all fail-closed at builder with same reasons as verifier. Absent canonical state matches generator output (`sidecar_suffixes=[]`). KRX symbol tightened to ASCII `[0-9]{6}` in `precheck_receipt_schema` and `config.settings` (full-width / Arabic-Indic digits rejected). Stdin `OSError` on buffer read → `receipt_input_read_error` (exit 1, sanitized, no traceback). Parameterized builder/verifier parity matrix + valid-path matrix tests. Docs updated across verification/receipt/composition contracts. **Still OPEN (unchanged posture):** signing/HMAC, receipt file I/O, envelope extraction, TTL, approval input, activation token, KIS `--run`, daemon wiring, default runtime activation.
    - **RTM-7c.4f — attended one-shot activation design freeze (offline; runtime activation still NO-GO).** Freezes the attended activation contract without implementing any activation caller. New `docs/PAPER_FAST_LOOP_ATTENDED_ACTIVATION_CONTRACT.md` defines six logical stages (`composition.attended_activation.AttendedActivationStage`), machine vs manual future prerequisites (unset policy stays OPEN), runtime surface inventory, and the hard invariant `activation_authorized=false` / `runtime_activation_outcome="no_go"` on every path. Carry-over hardening: **(H1)** stdin `ValueError` on buffer read (e.g. closed file) → `receipt_input_read_error` alongside `OSError`; **(H2)** absent-main orphan-sidecar fingerprint limit documented and regression-tested (`present=false`, `sidecar_suffixes=[]` even when `-wal`/`-shm`/`-journal` orphan exists; `sidecar_files` observes orphans separately; missing main stays machine NO_GO). `--run` JSON adds explicit activation posture fields; early refusal before `load_settings` proven with env/DB/network/thread/subprocess spies. New `tests/test_attended_activation_contract.py`. **Still OPEN:** activation CLI/caller, fast-loop `--run`, KIS live connection, daemon/scheduler wiring, approval input/persistence, receipt signing/HMAC, TTL/freshness defaults, activation epoch persistence, orphan-sidecar fingerprint binding, process-ownership/single-instance policy.
    - **RTM-7c.4f semantic closure — PASS receipt drift + observation semantics (offline; runtime activation still NO-GO).** Closes independent-review P1: builder and verifier previously accepted machine `PASS` receipts when `fingerprints_before != fingerprints_after`. New shared `validate_observation_semantics` / `observation_semantics_valid` in `composition.precheck_receipt_schema` — single source for builder object path and verifier dict path. **PASS rule:** `inspection_outcome=ok`, empty `reasons`, canonical normalized before/after equality on all seven fingerprint fields. **NO_GO drift rule:** each before/after drift requires exactly one `precheck_artifact_changed:<artifact>` reason; spurious/duplicate/unknown changed reasons fail closed; other inspection/precheck reasons unchanged. Verifier evaluates semantic mismatch before hash mismatch. Tests: P1 drift reproduction, builder semantic matrix, verifier precedence, `precheck_runtime` PASS/NO_GO receipt VALID regression. Docs: approval-time comparison target is `fingerprints_after`; `VALID` ≠ no-drift proof without semantic rules. **Still OPEN (unchanged posture):** activation CLI/caller, fast-loop `--run`, approval input/persistence, receipt signing/HMAC, TTL/freshness, activation epoch, orphan-sidecar binding, daemon wiring, default runtime activation.
    - **RTM-7c.4g — approval-time activation candidate revalidation preflight (read-only; runtime activation still NO-GO).** Pure `composition.activation_candidate_revalidation.revalidate_activation_candidate` re-checks verifier-VALID machine PASS receipt against current config binding and on-disk artifact fingerprints (two current passes vs `receipt.fingerprints_after`; drift reason owns revalidation-window change). CLI `--revalidate-activation-candidate` (stdin receipt + `load_settings(..., environ={})`; no clock/DB connection/fs write/network). Stable reasons: `candidate_receipt_invalid`, `candidate_receipt_not_pass`, `candidate_config_disabled`, `candidate_market_mismatch`, `candidate_symbol_mismatch`, `candidate_enabled_mismatch`, `candidate_current_artifact_drift:<artifact>`, `candidate_receipt_artifact_mismatch:<artifact>`. `freshness_evaluated=false` always; mechanical PASS ≠ activation. New `docs/PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_REVALIDATION_CONTRACT.md`. **Still OPEN (unchanged posture):** Operator approval input/consumption, receipt signing/HMAC, TTL/freshness policy, activation CLI/caller, fast-loop `--run`, KIS/daemon wiring, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4h — time-aware final activation candidate preflight (read-only; runtime activation still NO-GO).** Pure `composition.activation_candidate_final_preflight.final_preflight_activation_candidate` composes the 4g byte-state revalidation with a fresh caller-time `precheck_runtime`, so a byte-identical snapshot / active-decision whose validity window has since opened or closed is caught even when every fingerprint matches the receipt. Steps: naive/malformed `now` → `candidate_invalid_now` (precheck never reached); 4g `NO_GO` short-circuits with reasons preserved; fresh-precheck machine `NO_GO` → `candidate_current_precheck:<reason>`; per-artifact `candidate_post_revalidation_artifact_drift:<artifact>` between the two reads; else mechanical PASS. Evaluates **current-time validity only** — `current_validity_evaluated=true`, `receipt_age_evaluated=false`, `freshness_policy_evaluated=false`; activation posture constant NO-GO. No clock read of its own (caller supplies `now`); composed precheck opens the configured DBs **read-only** (`mode=ro` + `PRAGMA query_only`) — no operational connection/write/network/broker/runtime artifact. CLI `--final-preflight-activation-candidate` (8th mutually-exclusive mode; stdin receipt + `load_settings(..., environ={})`; `now=datetime.now(tz=KST)`; JSON `read_only_databases_opened` reflects the read-only precheck honestly, no `database_opened=false` claim). Carry-overs: **(H1)** `activation_candidate_revalidation` fingerprint reads are fail-closed on `OSError` → `candidate_artifact_unreadable:<artifact>` (canonical order, no raw exception/path leak); **(H2)** single shared `composition.paper_fast_loop_artifacts.PAPER_FAST_LOOP_ARTIFACT_SPECS` is the source for precheck / receipt schema / revalidation / final preflight artifact set + order. New `docs/PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_FINAL_PREFLIGHT_CONTRACT.md`. **Still OPEN (unchanged posture):** receipt age/TTL/max-age policy, receipt freshness, Operator approval input/consumption, receipt signing/HMAC, activation CLI/caller, fast-loop `--run`, KIS/daemon wiring, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4h truthful-state closure — execution-state fields now reflect the real path.** Supersedes two misleading result-contract claims in the prior 4h entry. **(P1)** `current_validity_evaluated` (previously a constant `true` on every path, including short-circuits that never ran the fresh precheck) is **replaced** by the per-call `fresh_precheck_executed`: `true` only when the composed `precheck_runtime` actually ran (PASS, fresh-precheck machine NO_GO, or post-revalidation drift), `false` for naive `now` and every 4g revalidation NO_GO. A `_posture(*, fresh_precheck_executed)` helper sets it per return site; no global constant masks it. **(P2)** the CLI `read_only_databases_opened` field (set from `precheck is not None`, i.e. not real connection telemetry) is **removed**; the CLI emits no `database_opened` / connection-count claim. Honest SQLite contract for this lane: read-only inspection may open the configured DBs (`mode=ro`) on fresh-precheck paths; write-capable connections / writes / schema changes / reconcile / store constructors / new sidecars all `0`. Tests add a precheck-call-count matrix (short-circuit ⇒ 0 calls + `fresh_precheck_executed=false`; PASS/expired/not-yet-valid ⇒ exactly 1 call + `true`) and assert the removed fields are absent from CLI JSON; the read-only connection spy asserts ≥1 connection, all `mode=ro`, none write-capable. Docs synced (final-preflight / attended-activation contracts). **Still OPEN (unchanged posture):** receipt age/TTL/max-age, receipt freshness, Operator approval input/consumption, receipt signing/HMAC, activation CLI/caller, fast-loop `--run`, KIS/daemon wiring, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4i — policy-neutral receipt time observation (read-only; runtime activation still NO-GO).** Pure `composition.receipt_time_assessment.assess_receipt_time(*, receipt_payload, now)` observes the time relationship between a verifier-`VALID` precheck receipt's `checked_at` and a caller-supplied tz-aware `now`: it records the **exact integer** receipt age (`(delta.days*86_400 + delta.seconds)*1_000_000 + delta.microseconds` — never float `total_seconds`) and **fail-closes a future `checked_at`** (`checked_at > now` → `NO_GO` / `receipt_time_in_future`). It selects **no** max-age / TTL / freshness threshold (`freshness_policy_evaluated` constant `false`); computing an age is an **observation**, not a verdict, not authenticity, not approval, not writer-stop proof, not activation. Outcome model `ReceiptTimeAssessment{outcome, reasons, receipt_checked_at, receipt_age_microseconds: int|None, receipt_age_evaluated, freshness_policy_evaluated}`. Processing order: strict `now` guard (non-datetime / naive / `None`-offset / `utcoffset`-raising → `receipt_time_invalid_now`, verifier never reached) → verifier reuse (`verify_runtime_precheck_receipt_payload`; not VALID → `receipt_time_receipt_invalid`; **no new canonical verifier or JSON parser built**) → defensive aware parse of the verified `checked_at` (unparseable/naive → `receipt_time_invalid_checked_at`) → future fail-close → exact age. `receipt_age_microseconds` is `>= 0` on VALID, `None` otherwise (incl. future receipt). Raw `checked_at` strings / payload values / exception types / tracebacks never appear in reasons. **Integrated into the 4h final preflight** as Step 3, interposed **between** the 4g byte-state revalidation and the fresh caller-time precheck: a 4g `NO_GO` short-circuits first; then a future receipt fail-closes as `candidate_receipt_time_in_future` (any other receipt-time `NO_GO` → `candidate_receipt_time_invalid`, not reachable after a 4g PASS) **before** the fresh precheck runs — so a `now` earlier than the receipt `checked_at` reports `candidate_receipt_time_in_future` with `fresh_precheck_executed=false`, never reaching a `candidate_current_precheck:*` reason. The final-preflight result/dataclass gains `receipt_age_microseconds: int|None` and a per-call `receipt_age_evaluated` (`true` once the comparison ran — VALID age or future fail-close — `false` for pre-comparison short-circuits); `freshness_policy_evaluated` stays constant `false`. **Carry-over hardening folded in at lane start (3 items):** **(H1)** `_final_preflight_summary` is path-free — the `"config": config_path` field is removed (no absolute config path / username / repo path / config contents in CLI JSON); the CLI also emits `receipt_age_microseconds`. **(H2)** `final_preflight_activation_candidate`'s `now` strictly fail-closes via `_now_is_invalid` (rejects `None` / `str` / `int` / naive datetime / `utcoffset`-raising tzinfo / `None`-offset → `candidate_invalid_now`, `fresh_precheck_executed=false`, both composed results `None`, no raw exception/type/repr leak; aware UTC/KST accepted) — revalidation and `precheck_runtime` never reached on an invalid `now`. **(H3)** `test_final_preflight_short_circuit_paths_do_not_run_precheck` rebuilt with independent per-case settings (no shared mutation), asserting exact single reason + `precheck_runtime` call count `0` + `fresh_precheck_executed=false`. New `composition.receipt_time_assessment`, `tests/test_receipt_time_assessment.py`, and `docs/PAPER_FAST_LOOP_RECEIPT_TIME_ASSESSMENT_CONTRACT.md`; final-preflight / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** receipt-age **threshold** / TTL / max-age selection, receipt freshness verdict, Operator approval input/consumption + approval-receipt binding, receipt signing/HMAC, activation CLI/caller, fast-loop `--run`, KIS/daemon wiring, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4j — immutable verified receipt snapshot (read-only; runtime activation still NO-GO).** Closes a real correctness defect: `final_preflight_activation_candidate` previously passed the **same raw `receipt_payload` dict** to both `revalidate_activation_candidate` (4g) and `assess_receipt_time` (4i), each of which independently re-verified the payload and re-read the raw mutable dict — so a cross-stage mutation could yield a **mixed observation** (hash from one read, age from another). New `composition.verified_precheck_receipt.verify_and_snapshot_precheck_receipt(payload)` verifies once (reusing `verify_runtime_precheck_receipt_payload` — **no new verifier / hash / JSON parser**) and freezes every retained field into an immutable `VerifiedPrecheckReceipt` (`@dataclass(frozen=True)`: aware `checked_at` + exact verified source `checked_at_iso` bound into the receipt hash, `receipt_sha256`, `market`/`symbol`/`enabled`/`machine_outcome`/`inspection_outcome`, `reasons` tuple, frozen `ArtifactFingerprint` tuples for before/after with tuple `sidecar_suffixes`, constant NO-GO activation posture). Non-VALID verification or any post-VALID structural surprise fails closed to the single stable `receipt_snapshot_invalid`; no raw key/value/exception/path is retained or surfaced. The 4g and 4i lanes gain snapshot-based cores (`revalidate_verified_activation_candidate(*, settings, receipt, base_dir=None)`, `assess_verified_receipt_time(*, receipt, now)`) that take a `VerifiedPrecheckReceipt` and call **no** verifier and never read the raw payload; their public raw-payload entrypoints are now thin wrappers that build a snapshot once and delegate (the 4i wrapper still guards `now` first, preserving the 4i precedence that an invalid `now` fail-closes before the receipt is verified). The 4i `receipt_time_invalid_checked_at` reason is removed — a verifier-VALID `checked_at` is already aware/parseable, so a malformed value is absorbed at snapshot-build time. `final_preflight_activation_candidate` now builds the snapshot **once** after its `now` guard (non-VALID → `candidate_receipt_invalid`) and feeds that same frozen snapshot to both verified cores, so a single preflight calls the receipt verifier **exactly once** and a raw-payload mutation between stages cannot change the verdict. New `composition.verified_precheck_receipt`, `tests/test_verified_precheck_receipt.py` (immutable build, frozen dataclass, raw + nested-collection mutation isolation, aware `checked_at`/`checked_at_iso` identity, fail-closed INVALID/non-dict, no clock read), and `tests/test_activation_candidate_final_preflight.py` adds a single-verifier-call spy + cross-stage mutation-isolation test. New `docs/PAPER_FAST_LOOP_VERIFIED_RECEIPT_SNAPSHOT_CONTRACT.md`; verification / revalidation / receipt-time / final-preflight / composition contracts synced. No CLI mode added; `--final-preflight-activation-candidate` JSON fields unchanged. **Still OPEN (unchanged posture):** receipt-age threshold / TTL / max-age selection, receipt freshness verdict, Operator approval input/consumption + approval-receipt binding, receipt signing/HMAC, activation CLI/caller, fast-loop `--run`, KIS/daemon wiring, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4j atomic snapshot closure — verify/copy TOCTOU (read-only; runtime activation still NO-GO).** Closes the independent-review P1 finding that the initial 4j snapshot builder verified the caller-owned payload then re-read the **same mutable dict** for field extraction — a caller mutation between verify return and snapshot read could yield a **mixed observation** (verifier-confirmed `receipt_sha256` from receipt A, `checked_at`/`symbol`/`fingerprints_*` from receipt B). Fix: `verify_and_snapshot_precheck_receipt` now deep-copies the caller payload to a private detached JSON tree (`_clone_receipt_payload` via `copy.deepcopy`) **before** any verifier or extraction step; the verifier and `_snapshot_from_verified_payload` both read the **same detached tree** with identity separate from the caller input — the caller object is never passed directly to the verifier and is not re-read after clone. Clone failure (including custom `__deepcopy__` exceptions) is fail-closed to `receipt_snapshot_invalid` with no raw exception/key/value/path leak. Doc correction: `checked_at_iso` is the exact verified source string bound into the receipt hash (not "original canonical string" — the verifier validates parseable timezone-aware ISO but does not canonicalize datetime strings). New regression coverage in `tests/test_verified_precheck_receipt.py`: verify-return caller mutation isolation (hash + fields stay on pre-mutation detached observation), verifier/snapshot object-identity spy (same detached id, nested list/dict ids differ from caller), nested mutation during verify (reasons/fingerprints/sidecars), clone-failure sanitization, verifier call-count = 1 per `verify_and_snapshot`. Existing final-preflight single-verifier-call spy + cross-stage mutation test retained. Docs synced (`PAPER_FAST_LOOP_VERIFIED_RECEIPT_SNAPSHOT_CONTRACT.md` atomic order section + verify/copy TOCTOU closure; verification / revalidation / receipt-time / final-preflight contracts). **Still OPEN (unchanged posture):** receipt-age threshold / TTL / max-age selection, receipt freshness verdict, Operator approval input/consumption + approval-receipt binding, receipt signing/HMAC, activation CLI/caller, fast-loop `--run`, KIS/daemon wiring, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4j strict detached-tree closure — no deepcopy hooks (read-only; runtime activation still NO-GO).** Closes independent-review P1 that `copy.deepcopy` accepted caller-defined `__deepcopy__` hooks: a top-level or nested self-returning container left the caller object aliased into the supposed detached tree, so a post-VALID caller mutation could still mix verifier-confirmed `receipt_sha256` with mutated fields. Fix: `_clone_receipt_payload` now calls `_clone_json_tree`, which accepts **exact built-in** JSON types only (`type(value) is dict|list|str|int|bool|None` — no `isinstance`, no custom container/scalar subclass, no `float`), builds new built-in dict/list nodes with no caller reference retention, and fail-closes cycles and `RecursionError` (no raw exception/value/path leak). Clone failure ⇒ `receipt_snapshot_invalid`, verifier call count `0`. Processing order unchanged: caller → strict clone → verifier(detached) → snapshot extraction(same detached). **Concurrency contract (precise):** does not claim point-in-time atomic snapshot across concurrent caller mutation during clone; no locks/thread sync in scope. Guarantee is post-clone isolation — verifier and extraction share the same detached built-in tree. New regression coverage in `tests/test_verified_precheck_receipt.py`: top-level/nested subclass matrix, cyclic input, deep nesting, scalar strictness, mixed-observation reproduction blocked, identity separation, no `copy.deepcopy` import. Docs synced (verified snapshot / verification / revalidation / receipt-time / final-preflight contracts). **Still OPEN (unchanged posture):** receipt-age threshold / TTL / max-age selection, receipt freshness verdict, Operator approval input/consumption + approval-receipt binding, receipt signing/HMAC, activation CLI/caller, fast-loop `--run`, KIS/daemon wiring, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4k — explicit receipt freshness policy evaluation (read-only; runtime activation still NO-GO).** Pure `composition.receipt_freshness_policy.evaluate_receipt_freshness(*, time_assessment, policy)` compares an already-computed `ReceiptTimeAssessment` against a **caller-supplied** `ReceiptFreshnessPolicy(max_age_microseconds: int)` — exact built-in int, bool rejected, `>= 0`, inclusive boundary (`age <= max_age` → `FRESH`; `age > max_age` → `STALE` / `receipt_age_exceeds_policy`). Invalid policy → `NO_GO` / `freshness_policy_invalid`; invalid/unusable time assessment → `NO_GO` / `freshness_time_assessment_invalid`. No clock read, no receipt payload/snapshot re-read, no verifier re-call, no config/env/CLI binding, no module-level default threshold, and no final-preflight integration (`freshness_policy_evaluated` there stays `false`). Outcome model `ReceiptFreshnessEvaluation{outcome, reasons, receipt_age_microseconds, max_age_microseconds, freshness_policy_evaluated, activation_authorized=false, runtime_activation_outcome="no_go"}`. **Carry-over hardening (H1):** clone-stage container-iteration `RuntimeError` (e.g. dict mutated during iteration) is now fail-closed to `receipt_snapshot_invalid` alongside `RecursionError` — no raw exception/type/repr/traceback leak, verifier call count `0`. New `composition.receipt_freshness_policy`, `tests/test_receipt_freshness_policy.py`, and `docs/PAPER_FAST_LOOP_RECEIPT_FRESHNESS_POLICY_CONTRACT.md`; receipt-time / final-preflight / attended-activation / composition / verified-snapshot contracts synced. **Still OPEN (unchanged posture):** max-age **value selection** / operational calibration, config/env/CLI threshold binding, final-preflight freshness integration, Operator approval input/consumption + approval-receipt binding, receipt signing/HMAC, activation CLI/caller, fast-loop `--run`, KIS/daemon wiring, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4l — explicit freshness-qualified activation candidate preflight (read-only; runtime activation still NO-GO).** API-only `composition.activation_candidate_freshness_preflight.freshness_qualify_activation_candidate(*, settings, receipt_payload, now, policy, base_dir=None)` requires an explicit `ReceiptFreshnessPolicy` argument (no default/config/env/CLI binding). Processing: shared policy validation → snapshot once (verifier call `1`) → `final_preflight_verified_activation_candidate` on frozen snapshot (verifier `0`) → on final PASS only, `evaluate_receipt_freshness` on the **same** `ReceiptTimeAssessment` object (`freshness_policy_evaluated=true` for FRESH/STALE; `candidate_receipt_stale` on STALE). Final-preflight `NO_GO` preserves existing reasons verbatim without calling the freshness evaluator. `final_preflight_activation_candidate` wrapper and `--final-preflight-activation-candidate` CLI remain policy-neutral (`freshness_policy_evaluated=false`). Verified core `final_preflight_verified_activation_candidate` extracted; result adds `receipt_time_assessment` for reuse. **Carry-over hardening (4k):** strict `type(policy) is ReceiptFreshnessPolicy` / `type(time_assessment) is ReceiptTimeAssessment` (subclass/wrong-object fail-closed, no `AttributeError` escape); contradictory VALID assessment matrix; single-read local field pattern in evaluator. New `composition.activation_candidate_freshness_preflight`, `tests/test_activation_candidate_freshness_preflight.py`, `docs/PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_FRESHNESS_PREFLIGHT_CONTRACT.md`; receipt-freshness / final-preflight / receipt-time / verified-snapshot / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** max-age value selection / operational calibration, config/env/CLI threshold binding, new CLI mode for freshness-qualified preflight, Operator approval input/consumption + approval-receipt binding, receipt signing/HMAC, activation CLI/caller, fast-loop `--run`, KIS/daemon wiring, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4l closure — now-precedence and policy-snapshot (read-only; runtime activation still NO-GO).** Closes independent-review findings on qualified-preflight ordering and policy double-observation. Processing order fixed: `snapshot_receipt_freshness_policy` → shared `final_preflight_now_is_invalid` → receipt snapshot → verified final core → `evaluate_receipt_freshness(snapshot policy)`. Invalid policy beats invalid `now` (`candidate_freshness_policy_invalid`); valid policy + invalid `now` → `candidate_invalid_now` with zero receipt snapshot / verifier / final core / evaluator (`freshness_policy_evaluated=false`). Shared `final_preflight_now_is_invalid` exported from final-preflight module; verified core direct-call also fail-closes invalid `now` before revalidation. `snapshot_receipt_freshness_policy` freezes caller `max_age_microseconds` once (not default/persistence/config); evaluator receives snapshot instance, not caller object — caller mutation during final core or after result does not change verdict. Per valid qualified call: policy snapshot builds `1`, receipt snapshot builds `1`, verifier calls `1`. Regression coverage: invalid-now matrix, precedence table, policy mutation during/after final core, verified-core direct invalid-now, isolation spies. Docs synced. **Still OPEN (unchanged posture):** max-age value selection, config/env default binding, Operator approval, signing/HMAC, activation caller, `--run`, KIS/daemon, writer-stop proof, default runtime activation.
    - **RTM-7c.4m — explicit freshness-qualified CLI preflight (read-only; runtime activation still NO-GO).** Adds 9th mutually-exclusive CLI mode `--freshness-preflight-activation-candidate` with **required** `--max-age-microseconds` (strict ASCII decimal parser in `composition.freshness_policy_cli_input`; stable reasons `freshness_policy_input_missing` / `freshness_policy_input_invalid`; no default/config/env threshold). Processing: args/mode → `--run` refusal → max-age applicability → max-age parse → stdin receipt → `load_settings(..., environ={})` → `now=datetime.now(tz=KST)` once → `freshness_qualify_activation_candidate`. Invalid max-age: stdin/config/env/clock/DB/filesystem access `0`. Other modes + `--max-age-microseconds` → `freshness_policy_argument_not_applicable` (existing modes unchanged). Path-free JSON; mechanical FRESH PASS ≠ activation (`activation_authorized=false`, `runtime_activation_outcome="no_go"`). `--final-preflight-activation-candidate` stays policy-neutral. **Carry-over hardening:** **(H1)** malformed exact `ReceiptFreshnessPolicy` (field deleted) → `snapshot_receipt_freshness_policy` returns `None` / `candidate_freshness_policy_invalid` (no `AttributeError` escape); **(H2)** `receipt_freshness_policy_is_valid` documented as sharing snapshot operation. New `composition.freshness_policy_cli_input`, `tests/test_freshness_policy_cli_input.py`, CLI matrix in `tests/test_activation_candidate_freshness_preflight.py`. Docs synced. **Still OPEN (unchanged posture):** max-age value selection, config/env default threshold field, Operator approval, signing/HMAC, activation caller, `--run`, KIS/daemon, writer-stop proof, default runtime activation.
    - **RTM-7c.4m strict max-age input closure — parser fail-closed hardening (read-only; runtime activation still NO-GO).** Closes three independent-review findings in `parse_max_age_microseconds_cli_input` without expanding runtime scope. **(P1a) over-limit integer conversion.** The parser called `int(raw)` after a regex check but never guarded the conversion; a token longer than the Python integer-string-conversion digit limit raised `ValueError` that escaped to a CLI traceback. Conversion is now wrapped so **only** `ValueError` is normalized to `freshness_policy_input_invalid` (`MemoryError`/`KeyboardInterrupt`/`SystemExit` are deliberately not caught). A rejected over-long token is a CLI-input-invalid event, **not** a selection of any freshness max-age upper bound. **(P1b) trailing newline.** Validation used `re.match(r"^[0-9]+$", raw)`, but `$` matches just before a trailing `\n`, so `"1\n"` parsed as `1`; validation now uses `re.fullmatch(r"[0-9]+", raw)`, rejecting a trailing newline and every other whitespace form (`\r`, `\r\n`, `\t`, `\v`, `\f`, leading/embedded whitespace). **(P2) wrong-object input.** The parser typed `raw: str | None` and called `re.match`/`int` directly, so a non-`str` (`int`, `bytes`, arbitrary object) raised `TypeError` and a `str` subclass silently passed; the signature is now `raw: object` with a `type(raw) is str` guard (exact built-in only) so wrong objects and `str` subclasses fail closed to `freshness_policy_input_invalid` with no exception escape. The raw token, exception type/message, and any traceback never appear in reasons or stdout/stderr. CLI processing order is unchanged: an invalid max-age still fails closed before any stdin read, config load, env access, clock read, DB/filesystem access, or freshness evaluation. New coverage: parser whitespace/newline matrix, wrong-object matrix, over-long-token fail-close, deterministic conversion-failure monkeypatch (`ValueError` normalized; `MemoryError`/`KeyboardInterrupt`/`SystemExit` not swallowed) in `tests/test_freshness_policy_cli_input.py`; CLI invalid-token zero-side-effect matrix + conversion-failure spy + raw-token/exception sanitization in `tests/test_activation_candidate_freshness_preflight.py`. `docs/PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_FRESHNESS_PREFLIGHT_CONTRACT.md` / `PAPER_FAST_LOOP_RECEIPT_FRESHNESS_POLICY_CONTRACT.md` / `PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` updated for entire-token ASCII-decimal validation, trailing-newline/whitespace rejection, integer-conversion `ValueError` fail-close, wrong-object fail-close, and the runtime-integer-limit-is-not-a-policy clarification. **Still OPEN (unchanged posture):** max-age value selection, config/env default threshold field, Operator approval, signing/HMAC, activation caller, `--run`, KIS/daemon, writer-stop proof, default runtime activation.
    - **RTM-7c.4n — canonical freshness-qualified candidate evidence (read-only; runtime activation still NO-GO).** Read-only `composition.activation_candidate_evidence.build_activation_candidate_evidence(*, qualified_result, evaluated_at)` freezes **one** freshness-qualified mechanical PASS into a single immutable canonical `ActivationCandidateEvidence` payload (schema version `1`) that a *future* Operator-approval stage can reference. Evidence is created **only** when the qualified outcome is PASS / freshness FRESH / final-preflight PASS with `activation_authorized=false` + `runtime_activation_outcome="no_go"`; `NO_GO`/`STALE` produce no digest (`evidence=None`) — there is no generic failure evidence. Strict eligibility: exact `type(qualified_result) is ActivationCandidateFreshnessPreflightResult` (subclass/wrong-object → `INVALID`); shared `final_preflight_now_is_invalid(evaluated_at)` tz-aware guard (invalid → `INVALID`); well-formed non-PASS (`outcome != PASS`) → `NOT_ELIGIBLE`; a PASS with any broken invariant (non-empty reasons, missing nested results, `freshness_policy_evaluated` not `True`, final outcome not PASS, `fresh_precheck_executed`/`receipt_age_evaluated` not `True`, freshness not FRESH, non-hex64 `receipt_sha256`, empty `market`/`symbol`, non-int/negative ages, `age > max_age`, or contradictory `activation_authorized=True` / `runtime_activation_outcome != "no_go"`) → `INVALID`, never a silent digest; a malformed exact-type instance (deleted field) fail-closes via `AttributeError` catch → `INVALID`. The canonical digest reuses `decision.canonical_json.payload_sha256` over **every evidence field except `evidence_sha256`** (14 fields, including the constant NO-GO posture, so a tampered posture changes the hash); independent recomputation = `asdict(evidence)` minus `evidence_sha256` → `payload_sha256(...)`. Single-observation builder: each caller-owned nested object/scalar is read once into a local and reused for validation + the hash payload. **Never stored:** raw receipt payload, artifact path, config path, fingerprint raw payload, secret/env data, approval fields, signature fields — the receipt hash already binds the receipt and its artifact observation. Composition wrapper `freshness_qualify_and_build_candidate_evidence(*, settings, receipt_payload, now, policy, base_dir=None)` runs the existing qualified preflight then invokes the builder **only** on a qualified PASS with `evaluated_at = now` (the same caller `now`, no second qualified run, no extra clock read; per call receipt verifier `1`, snapshot `1`, fresh precheck `1`, freshness evaluator `1`, builder `1`, clock read `1`). The existing CLI mode `--freshness-preflight-activation-candidate` gains two optional PASS-only fields `candidate_evidence_sha256` (lowercase hex64) + `candidate_evidence_schema_version` (`1`); `NO_GO`/`STALE`/input-failure → both `null`; output stays path-free (no full evidence raw payload, config/artifact path, fingerprint body, raw receipt, raw `checked_at`, secret/env identifier). CLI exit codes unchanged (PASS 0, NO_GO 1, `--run` 2). A digest on a PASS is documented as **not** approval — `activation_authorized=false`, `runtime_activation_outcome="no_go"` always. New `composition.activation_candidate_evidence`, `tests/test_activation_candidate_evidence.py`, `docs/PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_EVIDENCE_CONTRACT.md`; freshness-preflight / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** Operator approval input/storage/consumption + approval-evidence binding, evidence signing/HMAC, evidence persistence/file output, max-age value selection / config/env default threshold field, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4n evidence consistency and fail-closed closure (read-only; runtime activation still NO-GO).** Closes independent-review P1 findings that the initial 4n builder trusted the outer qualified result without cross-checking the nested observations, and that the combined wrapper/CLI surfaced a qualified PASS as PASS even when no digest was created. Reproduced (all returned `CREATED`/CLI-PASS before the fix): (1) outer/final `receipt_sha256`·`market`·`symbol` identity mismatch; (2) `final.receipt_age_microseconds` vs `freshness.receipt_age_microseconds` vs `time_assessment.receipt_age_microseconds` mismatch (evidence froze the freshness age while the final/assessment age differed); (3) a nested GO posture (`final`/`freshness` `activation_authorized=True`/`runtime_activation_outcome="go"`, or `explicit_operator_approval_required`/`writers_stopped_manual_confirmation_required=False` on outer/final); (4) an `evaluated_at` three hours after the `now` the qualified result was computed from. Fix — the builder now requires, on a PASS, exact nested types (`type(final_preflight_result) is ActivationCandidateFinalPreflightResult`, `type(freshness_evaluation) is ReceiptFreshnessEvaluation`, `type(receipt_time_assessment) is ReceiptTimeAssessment`; subclass/arbitrary object fail-closed) and full **observation consistency**: outer↔final identity equality; `final.freshness_policy_evaluated is False` (policy-neutral) with `freshness.freshness_policy_evaluated is True` + `freshness.outcome == FRESH` + `freshness.reasons == ()`; `time_assessment.outcome == VALID` + `reasons == ()` + `receipt_age_evaluated is True`; constant NO-GO posture (`activation_authorized False`, `runtime_activation_outcome "no_go"`) on outer/final/freshness plus `explicit_operator_approval_required`/`writers_stopped_manual_confirmation_required True` on outer/final; one agreed non-negative-int receipt age across final/freshness/time-assessment with `age <= max_age`. **Exact `evaluated_at` time binding** — `time_assessment.receipt_checked_at` is parsed as a strict timezone-aware datetime (malformed/naive/`None`-offset → `INVALID`), and the **exact integer** microseconds `evaluated_at - checked_at` (`days*86_400_000_000 + seconds*1_000_000 + microseconds`, never float `total_seconds`) must equal the agreed age; an `evaluated_at` before `checked_at` or off by one microsecond fails closed, while UTC/KST representations of the same instant are accepted. Raw `checked_at`/datetime/type/exception never appear in reasons. **Combined fail-closed outcome** — new `FreshnessQualifiedEvidenceOutcome{PASS, NO_GO}`; `FreshnessQualifiedEvidenceResult` gains `outcome` + `reasons`. The wrapper is combined `PASS` only when the qualified verdict is PASS *and* evidence is `CREATED`; a qualified PASS whose evidence is `INVALID`/`NOT_ELIGIBLE`/`None` is combined `NO_GO` with the single stable reason `candidate_evidence_generation_invalid` (no upstream stage rerun: snapshot/precheck/evaluator each still `1`); a qualified `NO_GO`/`STALE` preserves its exact reasons verbatim and produces no digest (builder `0`). The CLI `--freshness-preflight-activation-candidate` `outcome`/exit now follow the combined verdict — a qualified PASS without a `CREATED` digest emits `outcome=NO_GO`, `reasons=["candidate_evidence_generation_invalid"]`, `candidate_evidence_sha256`/`candidate_evidence_schema_version` `null`, exit 1, constant NO-GO posture; existing qualified NO_GO reasons are not augmented. New consistency/time-binding/combined/CLI coverage in `tests/test_activation_candidate_evidence.py` and `tests/test_activation_candidate_freshness_preflight.py`; `docs/PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_EVIDENCE_CONTRACT.md` / `PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_FRESHNESS_PREFLIGHT_CONTRACT.md` / `PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` / `PAPER_FAST_LOOP_ATTENDED_ACTIVATION_CONTRACT.md` synced. **Still OPEN (unchanged posture):** Operator approval input/storage/consumption + approval-evidence binding, evidence signing/HMAC, evidence persistence/file output, max-age value selection / config/env default threshold field, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4n nested PASS semantic closure (read-only; runtime activation still NO-GO).** Closes two further independent-review P1 findings that the consistency builder still trusted two nested scalars without cross-checking their semantic role. Reproduced against the committed builder (both returned `CREATED` before the fix): (1) a nested final preflight with `outcome == PASS` but a **non-empty `reasons`** tuple (e.g. `("candidate_current_precheck:expired",)`) — a contradictory PASS that carries failure reasons — was frozen into evidence; (2) a nested `time_assessment` with `freshness_policy_evaluated=True` — claiming the policy-neutral age-observation lane evaluated a freshness policy — was frozen into evidence, overlapping the nested semantic roles (the policy verdict belongs only to the freshness evaluation). Fix — the single-read nested snapshot now also captures `final_result.reasons` and `time_assessment.freshness_policy_evaluated` once, and the PASS path requires `final.reasons == ()` (a non-empty tuple, a `list`, `None`, or an arbitrary object all differ from `()` → `INVALID`; the raw reason content is never surfaced in the stable `candidate_evidence_invalid_input` reason) and `time_assessment.freshness_policy_evaluated is False` (a `True`/`0`/`1`/`None`/string flag → `INVALID`; a deleted field fail-closes via the `AttributeError` catch). The combined wrapper/CLI fail-closed path is unchanged: a builder `INVALID` from either contradiction yields combined `NO_GO` with `candidate_evidence_generation_invalid`, both digest fields `null`, exit 1, constant NO-GO posture; no upstream stage reruns. New `final.reasons` and `time_assessment.freshness_policy_evaluated` matrices in `tests/test_activation_candidate_evidence.py`; `docs/PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_EVIDENCE_CONTRACT.md` / `PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_FRESHNESS_PREFLIGHT_CONTRACT.md` / `PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` / `PAPER_FAST_LOOP_ATTENDED_ACTIVATION_CONTRACT.md` synced. **Still OPEN (unchanged posture):** Operator approval input/storage/consumption + approval-evidence binding, evidence signing/HMAC, evidence persistence/file output, max-age value selection / config/env default threshold field, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4n fresh machine-proof binding closure — evidence binds the *actual* revalidation + fresh-precheck result objects, not a boolean (read-only; runtime activation still NO-GO).** Closes an independent-review P1 finding that the builder accepted a freshness-qualified PASS whose final-preflight carried only `fresh_precheck_executed=True` while the real machine-proof result objects `revalidation_result` / `current_precheck_result` were `None` (or arbitrary/subclass objects) — a boolean-only PASS was frozen into a `CREATED` digest. Reproduced against the committed builder (returned `CREATED` before the fix with `revalidation_result=None, current_precheck_result=None, fresh_precheck_executed=True`). Fix — the PASS path now requires the bound, exact-typed machine proof and validates it against the same single canonical artifact observation. **Revalidation** (`type(final.revalidation_result) is ActivationCandidateRevalidationResult`): `outcome == PASS`, `reasons == ()`, `receipt_sha256`/`market`/`symbol` equal to outer/final, constant NO-GO posture (`activation_authorized False`, `runtime_activation_outcome "no_go"`, `explicit_operator_approval_required`/`writers_stopped_manual_confirmation_required True`), `freshness_evaluated is False`, and `current_fingerprints_before == current_fingerprints_after` over the canonical 4-artifact tuple (`execution_inputs_snapshot`, `ledger`, `trigger_journal`, `active_decision_store`; each element exact `ArtifactFingerprint` in order — non-tuple / wrong length / wrong-or-subclass element → `INVALID`). **Current precheck** (`type(final.current_precheck_result) is RuntimePrecheckResult`): `machine_outcome == PASS`, `reasons == ()`, `market`/`symbol` equal to outer, constant NO-GO posture, an `inspection` of exact `type PaperFastLoopInspection` with `outcome == OK` / `reasons == ()` / matching `market`·`symbol`, and `fingerprints_before == fingerprints_after`. **Fresh precheck receipt** (`type(precheck.receipt) is RuntimePrecheckReceipt`): `machine_outcome == "pass"`, `inspection_outcome == "ok"`, `reasons == ()`, matching `market`·`symbol`, `enabled is True`, constant NO-GO posture, `checked_at == evaluated_at.isoformat()` (the single caller `now`), and canonical fingerprint tuples. **Exact state relationship:** `reval before == reval after`, `precheck before == precheck after`, `reval after == precheck after`, and `receipt before/after == precheck before/after` — one canonical observation held identical from candidate revalidation through the fresh precheck (frozen-observation comparison only; the final-preflight drift check is not re-implemented). The fresh receipt `receipt_sha256` is **independently recomputed** by reusing the canonical `precheck_receipt_schema.build_receipt_hash_payload(...)` + `compute_receipt_sha256(...)` (no hash reimplementation, no filesystem/network re-verify); a stored sha that is not lowercase hex64 or that disagrees with the recomputation → `INVALID`. **Schema v2:** `ACTIVATION_CANDIDATE_EVIDENCE_SCHEMA_VERSION` is bumped to `2` and the evidence gains a `fresh_precheck_receipt_sha256` field that is folded into the canonical digest payload, so the digest binds **both** the original candidate `receipt_sha256` and the fresh-precheck receipt hash — changing either changes the digest. The CLI emits `candidate_evidence_schema_version=2` on PASS (read straight from the evidence; `null` on NO_GO/STALE/input-failure). **P2 fail-closed:** `evaluated_at` is required to be exact `type datetime` (a `datetime` subclass — including one whose `isoformat`/subtraction raises — is rejected before any computation) without any broad `BaseException` catch, so `MemoryError`/`KeyboardInterrupt`/`SystemExit` are never swallowed; raw fingerprint bodies, paths, datetimes, and exception text never appear in a reason. Test fixtures no longer use `None`-nested synthetic results: they build consistent real `ActivationCandidateRevalidationResult` / `RuntimePrecheckResult` / `RuntimePrecheckReceipt` objects (with a recomputed receipt hash), and a genuinely seeded freshness-qualified PASS regression asserts `CREATED` evidence at schema v2. New machine-proof absence / revalidation / current-precheck / fresh-receipt / schema-v2 / datetime-subclass matrices in `tests/test_activation_candidate_evidence.py`; `docs/PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_EVIDENCE_CONTRACT.md` / `PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_FRESHNESS_PREFLIGHT_CONTRACT.md` / `PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` / `PAPER_FAST_LOOP_ATTENDED_ACTIVATION_CONTRACT.md` synced. **Still OPEN (unchanged posture):** Operator approval input/storage/consumption + approval-evidence binding, evidence signing/HMAC, evidence persistence/file output, max-age value selection / config/env default threshold field, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4n fresh receipt verifier-parity closure (read-only; runtime activation still NO-GO).** Closes three independent-review P1 findings that the machine-proof binding path accepted fresh receipts the standalone JSON verifier would reject. Reproduced against the committed builder (all returned `CREATED` before the fix; standalone verifier returned `INVALID`): (1) **unsupported fresh-receipt schema** — `schema_version = PRECHECK_RECEIPT_SCHEMA_VERSION + 1` with a hash recomputed from the tampered schema still produced `CREATED` evidence while `verify_runtime_precheck_receipt_payload` returned `receipt_unsupported_schema`; (2) **invalid market/symbol** — outer/final/receipt identity aligned on `market="US"`, empty market, or non-ASCII-6-digit symbols (`ABC`, full-width, Arabic-Indic, mixed) still produced evidence while the verifier rejected `receipt_invalid_market` / `receipt_invalid_symbol`; (3) **semantically invalid fingerprints with matching hash** — canonical name/order and exact `ArtifactFingerprint` type preserved but field semantics violated (`size=-1`, absent-with-size/sha/sidecar, irregular-with-size/sha/user_version, uppercase/invalid sha, JSON snapshot `user_version`, negative SQLite `user_version`, unknown/duplicate/noncanonical sidecars, bool-as-int size/user_version) still produced `CREATED` while the verifier returned `receipt_invalid_fingerprint`. Fix — new shared `precheck_receipt_schema.validate_runtime_precheck_receipt_object(...)` reuses the single-source receipt schema helpers (`PRECHECK_RECEIPT_SCHEMA_VERSION`, `validate_checked_at`, `validate_market`, `validate_symbol`, `validate_receipt_fingerprints`, `validate_observation_semantics`, `build_receipt_hash_payload`, `compute_receipt_sha256`, `PrecheckReceiptError`) and returns the verified sha256 on success; `_machine_proof_receipt_sha256` replaces hash-only recomputation with this shared operation; outer `market`/`symbol` now require verifier-parity (`KR` + ASCII `[0-9]{6}`); revalidation/precheck fingerprint pairs gain exact `ArtifactFingerprint` type guard + semantic validation. Hash equality alone is insufficient — unsupported schema or semantically malformed fingerprints with a matching hash fail closed to `INVALID` / `candidate_evidence_invalid_input` / `evidence=None`; combined qualified PASS → `NO_GO` with `candidate_evidence_generation_invalid`. New schema/identity/fingerprint semantic/verifier-parity/combined matrices in `tests/test_activation_candidate_evidence.py`; object-validator parity assertion in `tests/test_precheck_receipt_verifier.py`; `docs/PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_EVIDENCE_CONTRACT.md` / `PAPER_FAST_LOOP_ACTIVATION_CANDIDATE_FRESHNESS_PREFLIGHT_CONTRACT.md` / `PAPER_FAST_LOOP_ATTENDED_ACTIVATION_CONTRACT.md` / `PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md` synced. **Still OPEN (unchanged posture):** Operator approval input/storage/consumption + approval-evidence binding, evidence signing/HMAC, evidence persistence/file output, max-age value selection / config/env default threshold field, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4o — canonical Operator approval intent binding (read-only API-only; runtime activation still NO-GO).** Adds `composition.operator_approval_intent.build_operator_approval_intent(*, combined_result, declared_at, operator_approval_declared, writers_stopped_manually_confirmed, live_orders_forbidden_confirmed)` — a pure function that freezes one combined freshness-qualified `PASS` whose evidence is `CREATED` into an immutable `OperatorApprovalIntent` (`OPERATOR_APPROVAL_INTENT_SCHEMA_VERSION = 1`; `approval_intent_sha256` via `decision.canonical_json.payload_sha256` over 12 non-digest fields; fixed `approval_scope="attended_paper_fast_loop_candidate"`). Eligibility: exact `type(combined_result) is FreshnessQualifiedEvidenceResult`; combined `PASS` + `reasons == ()`; exact `ActivationCandidateEvidenceResult` with `CREATED` + `reasons == ()`; exact `ActivationCandidateEvidence` with schema v2, lowercase hex64 `evidence_sha256` (independent `asdict` minus digest recomputation match), `market == KR`, ASCII `[0-9]{6}` symbol, constant NO-GO posture; three manual declarations each exact built-in `True`; `declared_at` exact built-in tz-aware `datetime` with `declared_at >= evidence.evaluated_at` (strict parse of evidence `evaluated_at`). Combined well-formed `NO_GO` → `NOT_ELIGIBLE` (`approval_intent_not_eligible`); contradictory combined `PASS`, malformed/deleted fields, evidence hash mismatch, invalid market/symbol/posture, non-exact declarations, naive/`datetime` subclass/invalid `declared_at`, or `declared_at` one microsecond before evidence → `INVALID` (`approval_intent_invalid_input`). Single-observation builder; no clock read; no receipt verifier / precheck / evaluator / evidence-builder re-invocation; no persistence/file output; no CLI wiring (existing `--freshness-preflight-activation-candidate` unchanged). Intent is **not** identity, signature, writer-stop machine proof, approval consumption, replay prevention, or activation authorization. New `composition.operator_approval_intent`, `tests/test_operator_approval_intent.py`, `docs/PAPER_FAST_LOOP_OPERATOR_APPROVAL_INTENT_CONTRACT.md`; evidence / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** approval-intent CLI input, approval consumption, intent persistence/file output, signing/HMAC, Operator identity authentication, replay/nonce/idempotency, approval pre-consumption revalidation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4o approval-intent evidence semantic-parity closure (read-only API-only; runtime activation still NO-GO).** Closes independent-review P1 finding that approval intent accepted semantically invalid evidence when `evidence_sha256` alone matched a recomputed digest — e.g. invalid receipt hashes, `final_preflight_outcome="no_go"`, `freshness_outcome="stale"`, false observation flags, or `receipt_age_microseconds > max_age_microseconds` with a matching hash still returned `CREATED`. Fix — shared `validate_activation_candidate_evidence_scalars` / `validate_activation_candidate_evidence_object` in `composition.activation_candidate_evidence` enforce the full schema-v2 semantic contract (exact types, lowercase hex64 receipt hashes, `"pass"`/`"fresh"` outcomes, exact-`True` observation flags, non-negative int ages with `age <= max_age`, constant NO-GO posture, independent hash parity via public `activation_candidate_evidence_hash_payload`). `build_operator_approval_intent` single-reads every evidence scalar into locals, validates via the shared helper, and builds the intent from validated locals only — production path does **not** use `dataclasses.asdict` or `copy.deepcopy` on caller evidence; caller-defined `__deepcopy__` hooks are never invoked. Hash equality alone is insufficient. New semantic-invalid matching-hash matrix, deepcopy/custom-object matrix, shared-validator, and production-`asdict` absence tests in `tests/test_operator_approval_intent.py`; evidence / approval-intent / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** approval-intent CLI input, approval consumption, intent persistence/file output, signing/HMAC, Operator identity authentication, replay/nonce/idempotency, approval pre-consumption revalidation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4o declared-time snapshot and combined-PASS qualified consistency closure (read-only API-only; runtime activation still NO-GO).** Closes independent-review P1 findings that (1) stateful/custom `tzinfo` on caller `declared_at` could raise `RuntimeError` after the first `utcoffset()` during comparison/`isoformat()` — exception escaped to the caller; and (2) combined `PASS` with contradictory `qualified_result` (e.g. qualified `NO_GO` with empty reasons, receipt-hash/market/symbol mismatch, wrong object/subclass, or posture flag mismatch) still returned `CREATED` because the builder never read `qualified_result`. Reproduced before fix: `_StatefulTz` with `fail_at=2` raised `POISON_DECLARED_AT` on comparison; combined `PASS` + qualified `NO_GO` + `CREATED` evidence returned `CREATED`. Fix — new `snapshot_declared_at(value)` freezes caller `declared_at` via detached single observation: exact built-in `datetime` only; `isoformat()` exactly once; `fromisoformat()` re-parse; timezone-aware check on parsed datetime; comparison uses `parsed_datetime` only; intent model/hash use snapshot ISO only — no post-snapshot caller datetime/tzinfo access. Combined `PASS` now also requires `type(qualified_result) is ActivationCandidateFreshnessPreflightResult` with qualified `PASS`/`reasons==()`/identity match to validated evidence (`receipt_sha256`, `market`, `symbol`) and minimum posture contract (`freshness_policy_evaluated` exact `True`, `activation_authorized` exact `False`, `runtime_activation_outcome=="no_go"`, `explicit_operator_approval_required`/`writers_stopped_manual_confirmation_required` exact `True`); evidence full semantic contract remains owned by shared `validate_activation_candidate_evidence_scalars`. Single-read locals for combined + qualified + evidence; all invalid paths → `approval_intent_invalid_input` with no raw exception/repr/traceback/sentinel escape (`MemoryError` re-raised; `KeyboardInterrupt`/`SystemExit` not swallowed). New stateful-tzinfo matrix, declared-snapshot call-count, qualified-consistency matrix, and source-guard tests in `tests/test_operator_approval_intent.py`; approval-intent / attended-activation / composition contracts synced. Intent remains unauthenticated, unconsumed, unpersisted; activation remains NO-GO. **Still OPEN (unchanged posture):** approval-intent CLI input, approval consumption, intent persistence/file output, signing/HMAC, Operator identity authentication, replay/nonce/idempotency, approval pre-consumption revalidation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4o strict result-scalar comparison closure (read-only API-only; runtime activation still NO-GO).** Closes independent-review P1 findings that (1) custom always-equal objects injected into qualified `receipt_sha256`/`market`/`symbol`/`runtime_activation_outcome` bypassed identity checks via `__eq__` returning `True` and still returned `CREATED`; and (2) custom raising comparators on `combined.reasons`, `evidence_result.reasons`, `qualified.reasons`, or qualified identity/runtime scalars escaped `RuntimeError("POISON_QUALIFIED_COMPARE")` to the caller because validation used `value == ()` / `value != ()` / direct scalar equality. Reproduced before fix: `_AlwaysEqual()` on all four qualified identity/runtime fields → `CREATED`; `_PoisonCompare()` on all seven targets → raw `RuntimeError` escape. Fix — new `_is_exact_empty_reasons(value)` requires `type(value) is tuple and len(value) == 0` for `combined_reasons`, `er_reasons`, and `qr_reasons` (no `== ()` / `!= ()`); qualified identity/runtime scalars require `type(value) is str` before built-in string equality (`receipt_sha256`, `market`, `symbol` vs validated evidence; `runtime_activation_outcome == "no_go"`). Str subclass, `None`, `int`, `bytes`, always-equal, and poison-comparator objects → `INVALID` with stable `approval_intent_invalid_input` and zero exception/sentinel escape. Enum identity and exact-bool posture helpers unchanged; evidence shared semantic validator unchanged. New always-equal matrix, poison-comparator matrix, exact-empty-reasons matrix, qualified exact-string matrix, and source-guard tests in `tests/test_operator_approval_intent.py`; approval-intent / attended-activation / composition contracts synced. Intent remains unauthenticated, unconsumed, unpersisted; activation remains NO-GO. **Still OPEN (unchanged posture):** approval consumption, intent persistence/file output, signing/HMAC, Operator identity authentication, replay/nonce/idempotency, approval pre-consumption revalidation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4p — Operator approval-intent CLI input (read-only stdout generation; runtime activation still NO-GO).** Adds 10th mutually-exclusive CLI mode `--build-operator-approval-intent` with **required** `--max-age-microseconds` (same strict parser as freshness mode) plus three **explicit** manual confirmation flags (`--operator-approval-declared`, `--writers-stopped-manually-confirmed`, `--live-orders-forbidden-confirmed` — each must be present; absence is not approval). Processing order: args/mode → `--run` refusal (exit 2) → confirmation applicability → max-age applicability → max-age parse → confirmation presence → stdin receipt → `load_settings(..., environ={})` → `now=datetime.now(tz=KST)` once → `freshness_qualify_and_build_candidate_evidence` once → `build_operator_approval_intent` once (combined PASS only). Early isolation: invalid/missing max-age or missing confirmation → `FAIL` before stdin/config/env/clock/DB/filesystem; wrong-mode confirmation → `approval_intent_argument_not_applicable`; wrong-mode max-age unchanged (`freshness_policy_argument_not_applicable`). Aggregate outcomes: combined PASS + intent CREATED → `PASS` exit 0 with evidence/intent digests; upstream mechanical NO_GO → exact upstream reasons, null intent digest, exit 1; intent generation failure → `approval_intent_generation_invalid`, null digest, exit 1; CLI input failure → `FAIL` exit 1. Same KST `now` binds `evidence.evaluated_at` and `declared_at`. Constant NO-GO / unauthenticated posture on every path. No persistence/file output/consumption/identity/signing/activation. Existing `--freshness-preflight-activation-candidate` unchanged. New `tests/test_paper_fast_loop_operator_approval_intent_cli.py`, `docs/PAPER_FAST_LOOP_OPERATOR_APPROVAL_INTENT_CLI_CONTRACT.md`; approval-intent / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** approval consumption, intent persistence/file output, signing/HMAC, Operator identity authentication, replay/nonce/idempotency, approval pre-consumption revalidation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4p explicit CLI boundary and outcome-classification closure (read-only stdout generation; runtime activation still NO-GO).** Closes independent-review P1 findings that (1) missing `--json` still ran the pipeline and emitted human-readable text; (2) missing `--config` silently fell back to `config/config.toml.example`; (3) stdin input failures were classified as mechanical `NO_GO`; and (4) config load/validation failures were classified as mechanical `NO_GO` with exception-type leakage. Fix — argparse `--config` default is now `None`; after mode resolution, approval mode requires explicit `--json` (`approval_intent_json_required`) and explicit `--config` (`approval_intent_config_required`) before any external access (json checked before config when both missing); other modes retain `config/config.toml.example` fallback. Approval mode is **stdout JSON only** via `_approval_intent_cli_emit` — no human-readable intent output. Stable envelope on every approval path: `mode="build-operator-approval-intent"`, `reasons` list only (no `reason_code` mixing). Outcome taxonomy: invocation/input/config failures → `FAIL` (stdin `receipt_input_*`, config `approval_intent_config_invalid`); valid input with mechanical rejection → `NO_GO`. New explicit-json/config tests, stdin/config error matrices, envelope assertions, and early-isolation spies in `tests/test_paper_fast_loop_operator_approval_intent_cli.py`; CLI / approval-intent / attended-activation / composition contracts synced. Intent remains unauthenticated, unconsumed, unpersisted; activation remains NO-GO. **Still OPEN (unchanged posture):** approval consumption, intent persistence/file output, signing/HMAC, Operator identity authentication, replay/nonce/idempotency, approval pre-consumption revalidation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4q — standalone Operator approval-intent verification (stdin-only; runtime activation still NO-GO).** Promotes shared helpers in `composition.operator_approval_intent` (`operator_approval_intent_hash_payload`, `validate_operator_approval_intent_scalars`, `validate_operator_approval_intent_object`) and adds pure `composition.operator_approval_intent_verifier.verify_operator_approval_intent_payload` — strict 13-field schema v1, semantic posture, canonical hash parity (VALID ≠ authentication/signature/evidence revalidation/consumption/replay/freshness/activation). 11th CLI mode `--verify-operator-approval-intent`: explicit `--json`; forbidden `--config` / `--max-age-microseconds` / confirmation flags; stdin-only 1 MiB bound with receipt JSON parser internally and mapped `approval_intent_input_*` reasons; verifier called exactly once; zero config/env/clock/DB/fs write/network/builder/pipeline. Approval build/verify mode conflicts normalize to approval JSON envelopes. Build-mode clock/builder exceptions sanitized (`MemoryError`/`KeyboardInterrupt`/`SystemExit` not swallowed). New `tests/test_operator_approval_intent_verifier.py`, verify CLI tests in `tests/test_paper_fast_loop_operator_approval_intent_cli.py`, `docs/PAPER_FAST_LOOP_OPERATOR_APPROVAL_INTENT_VERIFICATION_CONTRACT.md`; builder→verifier invariant + tamper/hash/stdin/isolation matrices. Intent remains unauthenticated, unconsumed, unpersisted; activation remains NO-GO. **Still OPEN (unchanged posture):** approval consumption, intent persistence/file output, signing/HMAC, Operator identity authentication, replay/nonce/idempotency, approval pre-consumption revalidation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4q strict hash-string and detached payload snapshot closure (stdin-only verification; runtime activation still NO-GO).** Closes independent-review P1 findings that (1) `str` subclasses with valid hex64 content bypassed `is_hex64`/`isinstance(str)` on `evidence_sha256`/`approval_intent_sha256`; and (2) non-exact dict keys (str subclass / custom key objects) bypassed `set(payload.keys())` field-set checks and could escape via key `__hash__`/`__eq__` during verifier validation. Fix — shared `_is_exact_hex64` (`type(value) is str and is_hex64`) in `composition.operator_approval_intent` for both digest fields (builder output unchanged); verifier `_snapshot_operator_approval_intent_payload` observes `tuple(payload.items())` once, requires `type(key) is str` before any membership/hash on keys, copies into detached built-in dict, and never re-reads caller payload; exact built-in dict root only (`dict` subclass → `not_object`). Field-set precedence: not exact dict → `not_object`; bad key → `unknown_field`; missing canonical key → `missing_field`. RuntimeError/KeyError during observation → stable `unknown_field`; `MemoryError`/`KeyboardInterrupt`/`SystemExit` re-raised. New digest exact-type matrix, key-type/custom-key matrix, detached mutation isolation, source-guard, and sanitization tests in `tests/test_operator_approval_intent_verifier.py`; verification / approval-intent / attended-activation / composition contracts synced. VALID remains schema/semantic/hash only — not authentication/persistence/consumption/freshness/activation. **Still OPEN (unchanged posture):** approval consumption, intent persistence/file output, signing/HMAC, Operator identity authentication, replay/nonce/idempotency, approval pre-consumption revalidation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4r — immutable verified Operator approval-intent snapshot (API-only; runtime activation still NO-GO).** Adds `verify_and_snapshot_operator_approval_intent` → `VerifiedOperatorApprovalIntentResult` with frozen 13-field `VerifiedOperatorApprovalIntent` on VALID (exact built-in scalars only; raw payload/dict not retained). Refactors shared `_verify_detached_operator_approval_intent` so verifier and snapshot API each perform exactly one payload snapshot + one schema/semantic/hash pass — snapshot API does not re-call public verifier or re-read caller payload. INVALID → single stable reason + `snapshot=None`; verifier ↔ snapshot parity on outcome/reason_codes/digest metadata. Caller mutation after snapshot cannot change verdict or snapshot values. Public APIs fail closed on unexpected `Exception` (`approval_intent_invalid_field`); `MemoryError`/`KeyboardInterrupt`/`SystemExit` re-raised. Carry-over H1: CLI `--verify-operator-approval-intent` no longer swallows those three via bare `except Exception`. No new CLI mode, persistence, consumption, signing, authentication, or activation. New `tests/test_verified_operator_approval_intent.py`, `docs/PAPER_FAST_LOOP_VERIFIED_OPERATOR_APPROVAL_INTENT_CONTRACT.md`; verification / approval-intent / attended-activation / composition contracts synced. Builder `CREATED` intent → VALID snapshot with 13-field equality. **Still OPEN (unchanged posture):** approval consumption, intent persistence/file output, signing/HMAC, Operator identity authentication, replay/nonce/idempotency, approval pre-consumption revalidation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4s — Operator approval consumption eligibility preflight (API-only; runtime activation still NO-GO).** Closes carry-over H1: shared `validate_operator_approval_intent_scalars_detailed` owns field-level stable reason classification + full semantic validation + validated scalar snapshot; `_verify_detached_operator_approval_intent` calls it exactly once (no duplicate declared-at / evidence-binding / identity / declaration / posture checks). Adds `assess_operator_approval_consumption_eligibility(*, intent_payload, evidence, now)` → `OperatorApprovalConsumptionEligibilityResult` with outcomes `ELIGIBLE` / `NO_GO` / `INVALID`. Processing: strict `now` snapshot → `verify_and_snapshot_operator_approval_intent` once → evidence scalar snapshot + `validate_activation_candidate_evidence_scalars` once → digest/identity binding (schema v2, `evidence_sha256`, `market`, `symbol`) → time ordering (`evidence.evaluated_at ≤ intent.declared_at ≤ now` only — no TTL/freshness re-evaluation). Eligibility is **not** consumption — no consumed marker, replay protection, persistence, authentication, or activation authorization; constant `activation_authorized=false` / `runtime_activation_outcome="no_go"`. Per ELIGIBLE call: now snapshot `1`, intent payload snapshot `1`, intent semantic validation `1`, intent hash `1`, evidence scalar snapshot `1`, evidence semantic validation `1`, evidence hash `1`; zero upstream pipeline / clock read / config/env / DB/fs write / network / broker. Caller mutation after intent/evidence snapshot cannot change verdict. New `composition.operator_approval_consumption_eligibility`, `tests/test_operator_approval_consumption_eligibility.py`, `docs/PAPER_FAST_LOOP_OPERATOR_APPROVAL_CONSUMPTION_ELIGIBILITY_CONTRACT.md`; verified / approval-intent / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** actual approval consumption, consumed marker, replay/nonce/idempotency, signing/HMAC, Operator identity authentication, intent persistence/file output, intent TTL/freshness policy, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4t — canonical Operator approval consumption eligibility artifact + carry-over closure (API-only; runtime activation still NO-GO).** Adds pure `composition.operator_approval_consumption_eligibility_artifact.build_operator_approval_consumption_eligibility_artifact(eligibility_result)` → `OperatorApprovalConsumptionEligibilityArtifactResult` with outcomes `CREATED` / `NOT_ELIGIBLE` / `INVALID`. Only a completed 4s `ELIGIBLE` result (exact `OperatorApprovalConsumptionEligibilityResult`, `reasons == ()`, exact nested `OperatorApprovalConsumptionEligibility`) is frozen into a 13-field canonical immutable artifact (schema_version `1`; `approval_intent_schema_version`/`candidate_evidence_schema_version` imported from owning modules, **not** redefined locally; `market="KR"`, `activation_authorized=false`, `runtime_activation_outcome="no_go"`) with `eligibility_artifact_sha256` over the 12 content fields via `decision.canonical_json.payload_sha256`. Strict nested revalidation (lowercase hex64 digests, `KR`, `[0-9]{6}` symbol, aware ISO timestamps, exact `False`/`"no_go"` posture, `evidence ≤ intent ≤ checked_at` ordering). **Malformed `NO_GO` (empty/list/subclass reasons, non-None eligibility) maps to `INVALID`, not `NOT_ELIGIBLE`** — only `NO_GO` + tuple-reasons `len≥1` + `eligibility is None` is `NOT_ELIGIBLE`. The builder reads the completed result only: zero upstream eligibility rerun / intent verifier / evidence validator / clock / config/env / DB/fs / network; canonical hash payload `1` and `payload_sha256` `1` on `CREATED`. Single-read locals make post-capture caller mutation non-effective on verdict/artifact (no concurrent-atomicity claim). Normal/unexpected `Exception` → sanitized `INVALID` (no raw value/path leak); `MemoryError`/`KeyboardInterrupt`/`SystemExit` re-raised. The artifact is a canonical eligibility **observation**, **not** consumption: no consumed marker, replay prevention, persistence/file output, signing/HMAC, authentication, TTL/freshness re-evaluation, or activation authorization. **Carry-over test precision closure (4s):** H1 exact built-in `datetime` + stateful custom `tzinfo` (first-read failure → INVALID; caller tzinfo not re-read after first `isoformat()` observation); H2 `_binding_matches` single-field root-cause unit matrix (intent/evidence schema version, evidence digest, intent/evidence market, intent/evidence symbol); H3 ELIGIBLE-path evidence canonical hash measured (`validate_activation_candidate_evidence_scalars` / `activation_candidate_evidence_hash_payload` / evidence-module `payload_sha256` each `1`). New `tests/test_operator_approval_consumption_eligibility_artifact.py`, carry-over tests appended to `tests/test_operator_approval_consumption_eligibility.py`, `docs/PAPER_FAST_LOOP_OPERATOR_APPROVAL_CONSUMPTION_ELIGIBILITY_ARTIFACT_CONTRACT.md`; eligibility / verified / approval-intent / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** actual approval consumption, consumed marker, replay/nonce/idempotency, signing/HMAC, Operator identity authentication, artifact persistence/file output, intent/evidence lookup, TTL/freshness re-evaluation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4u — standalone serialized eligibility-artifact verification + immutable verified snapshot (API-only; runtime activation still NO-GO).** Adds pure `composition.operator_approval_consumption_eligibility_artifact_verifier.verify_operator_approval_consumption_eligibility_artifact_payload(payload)` → `OperatorApprovalConsumptionEligibilityArtifactVerification` and `verify_and_snapshot_operator_approval_consumption_eligibility_artifact(payload)` → `VerifiedOperatorApprovalConsumptionEligibilityArtifactResult` (frozen 13-field `VerifiedOperatorApprovalConsumptionEligibilityArtifact` on VALID). Verifies an untrusted **serialized** 4t artifact (JSON round-trip): strict `type(payload) is dict` root (dict subclass → `eligibility_artifact_not_object`), exact 13-field set, exact scalar types, semantic constants, aware-timestamp `evidence ≤ intent ≤ checked_at` ordering, and `eligibility_artifact_sha256` recomputed over the **actual 12 serialized content fields** (not auto-inserted constants) via `decision.canonical_json.payload_sha256`. Builder and verifier share two semantic owners exported from the 4t module: `validate_operator_approval_consumption_eligibility_artifact_scalars_detailed` (scalar/semantic/timestamp/ordering) and `operator_approval_consumption_eligibility_artifact_hash_payload_from_scalars` (canonical 12-field hash; the existing builder `..._hash_payload` now delegates to it, output unchanged). Stable single reason codes: `eligibility_artifact_not_object` / `_unknown_field` / `_missing_field` / `_unsupported_schema` / `_invalid_field` / `_invalid_timestamp` / `_invalid_binding` / `_invalid_activation_posture` / `_invalid_time_ordering` / `_hash_mismatch` (no raw value/hash/datetime/path/exception leak). One detached `tuple(payload.items())` observation into a built-in dict (non-exact/custom key → `unknown_field`, no hash/eq hook); caller mutation after snapshot cannot change verdict/snapshot. Per VALID call: payload snapshot `1`, scalar/semantic validation `1`, canonical hash payload `1`, `payload_sha256` `1`; zero upstream builder/eligibility/intent-verifier/evidence rerun, clock, config/env, DB/fs, network. Snapshot API uses the shared detached core directly (no public-verifier recall). Normal/unexpected `Exception` → sanitized `INVALID` / `eligibility_artifact_invalid_field`; `MemoryError`/`KeyboardInterrupt`/`SystemExit` re-raised. VALID/snapshot means schema·semantic·hash consistency only — **not** actual consumption, consumed marker, replay prevention, persistence, authentication/signature, TTL/freshness re-evaluation, or activation authorization. No new CLI mode (stdin/file CLI deferred to the persistence/external-file lane). New `composition.operator_approval_consumption_eligibility_artifact_verifier`, `tests/test_operator_approval_consumption_eligibility_artifact_verifier.py`, `docs/PAPER_FAST_LOOP_VERIFIED_OPERATOR_APPROVAL_CONSUMPTION_ELIGIBILITY_ARTIFACT_CONTRACT.md`; artifact / eligibility / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** actual approval consumption, consumed marker, replay/nonce/idempotency, signing/HMAC, Operator identity authentication, artifact persistence/file output, intent/evidence lookup, TTL/freshness re-evaluation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4u — verification-semantics and shared-owner closure (API-only; runtime activation still NO-GO).** Closes two defects in the committed 4u verifier without adding new behavior. (P1-A verification semantics) The earlier "each of the 13 serialized fields tampered → every tamper stays `INVALID`" wording overclaimed: the verifier is a **consistency checker, not an authenticator** (no signature/HMAC/provenance). Tampers now fall into three documented categories — (A) malformed / semantic-invalid → `INVALID` + single reason; (B) a semantically valid content change with a **stale** stored digest → `INVALID` / `eligibility_artifact_hash_mismatch`; (C) a semantically valid content change with a **correctly recomputed** digest → `VALID` **by design** (observational consistency, not authenticity/provenance). The misleading test `test_stale_intent_hash_recomputed_still_mismatch` is renamed to `test_semantically_valid_digest_change_with_recomputed_artifact_hash_is_valid` and explicit category tests added (`..._symbol_change_with_recomputed_hash_is_valid`, `..._checked_at_change_with_recomputed_hash_is_valid`, `..._change_with_stale_hash_is_hash_mismatch`) plus a builder→verifier parity matrix. (P1-B shared owner) Builder and verifier now share a **single content semantic owner** `validate_operator_approval_consumption_eligibility_artifact_content_scalars_detailed` (12 content-field schema / binding / digest-shape / `KR` market / `[0-9]{6}` symbol / aware-timestamp / posture / `evidence ≤ intent ≤ checked_at` ordering), wrapped by the full `validate_..._scalars_detailed` (content owner exactly once + stored-digest shape). The builder ELIGIBLE path drops its inline `_is_lower_hex64` / market / symbol / posture / timestamp-parse / ordering checks and calls the content owner exactly once; builder 13-field output and `eligibility_artifact_sha256` digest stay byte-equivalent to the pre-refactor result (existing 4t builder hash-payload `1` / `payload_sha256` `1` spies and mutation-isolation test still hold). Call-count discipline asserted: builder content-owner `1`, hash `1`, sha `1`; verifier VALID path full-owner `1` + content-owner `1` (no duplicate semantic validation, no builder↔verifier cross-call). Six contracts (verified-artifact / artifact / eligibility / attended-activation / composition + this entry) re-synced to the consistency-not-authenticity wording. Full suite green by exit code. **Still OPEN (unchanged posture):** actual approval consumption, consumed marker, replay/nonce/idempotency, signing/HMAC, Operator identity authentication, artifact persistence/file output, intent/evidence lookup, TTL/freshness re-evaluation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4v — operator-facing eligibility-artifact verification CLI (stdin-only, read-only; runtime activation still NO-GO).** Exposes the 4u `verify_operator_approval_consumption_eligibility_artifact_payload` API (called **exactly once**) as a new mutually-exclusive CLI mode `ops/run_paper_fast_loop.py --verify-approval-consumption-eligibility-artifact --json`. Stdin-only via the existing bounded strict JSON parser (1 MiB, `read(limit+1)`, exact JSON-object root); parser receipt-namespace reasons are mapped into the `eligibility_artifact_input_*` namespace (`_input_empty` / `_not_utf8` / `_not_json` / `_too_large` / `_too_deep` / `_duplicate_key` / `_read_error`). Argument contract: required `--json` (missing → `eligibility_artifact_verification_json_required`); forbidden `--config` / `--max-age-microseconds` / the three confirmation flags (→ `eligibility_artifact_verification_argument_not_applicable`); `--run` refused with **exit 2 before** mode resolution / applicability / stdin read. Verify mode does **not** call `load_settings` / read `os.environ` / read the clock (`datetime.now` / `time.time`) / open SQLite / construct stores / run precheck / evidence builder / eligibility API / intent verifier / broker / network / fs write. Per VALID call: stdin read `1`, JSON parse `1`, verifier `1`; everything else `0`. Outcome taxonomy: VALID exit 0 reasons `[]` (metadata `schema_version` / `approval_intent_schema_version` / `approval_intent_sha256` / `candidate_evidence_schema_version` / `candidate_evidence_sha256` / `eligibility_artifact_sha256`, verified exact lowercase hex64 only); INVALID artifact exit 1 with verifier stable reason codes; input/argument failure exit 1 with one stable CLI reason. Every path emits the constant posture `activation_authorized=false` / `runtime_activation_outcome="no_go"` / `artifact_authenticated=false` / `artifact_persisted=false` / `approval_consumed=false` / `replay_prevented=false`. Consistency-not-authenticity carried verbatim (Category C semantically valid change + recomputed digest → VALID by design; not presented as authentication/provenance/consumption-ready). Sanitization: no raw stdin / config path / repo path / secret / `Traceback` / exception repr / SQLite raw error; malformed digests never echoed. Verifier raising an ordinary `Exception` → exit 1 / `eligibility_artifact_invalid_field`; `MemoryError` / `KeyboardInterrupt` / `SystemExit` re-raised. **Carry-over:** the 4t builder ELIGIBLE path now hashes and constructs the artifact from the validated content snapshot (`content.validated`) returned by the shared content owner — one identical observation source for validation, hashing, and construction — with byte-equivalent 13-field output and `eligibility_artifact_sha256` digest and unchanged caller-mutation isolation. New `tests/test_paper_fast_loop_eligibility_artifact_verifier_cli.py`, `docs/PAPER_FAST_LOOP_OPERATOR_APPROVAL_CONSUMPTION_ELIGIBILITY_ARTIFACT_VERIFICATION_CLI_CONTRACT.md`; verified-artifact / artifact / eligibility / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** actual approval consumption, consumed marker, replay/nonce/idempotency, signing/HMAC, Operator identity authentication, origin/provenance verification, artifact persistence / external-file CLI / file output, intent/evidence lookup, TTL/freshness re-evaluation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4v — CLI outcome taxonomy and conflict-envelope closure (stdin-only, read-only; runtime activation still NO-GO).** Closes two CLI-boundary defects in the committed 4v verify mode without adding behavior. (P1-A taxonomy) CLI/argument/input boundary failures were misclassified `outcome="INVALID"` because `_verify_eligibility_artifact_input_fail` minted an `OperatorApprovalConsumptionEligibilityArtifactVerification(INVALID)` and ran it through the verifier summary. Three disjoint outcomes now hold: **FAIL** (verification *not started* — `--json` missing / forbidden argument / stdin boundary: empty/not-utf8/not-json/too-large/too-deep/duplicate-key/read-error / mode conflict; exit 1; all metadata digests/schemas `null`; verifier called `0`); **INVALID** (payload reached the verifier and was rejected — schema/semantic/hash, exit 1, verifier stable reason); **VALID** (schema·semantic·hash consistency only, exit 0). A new stable-key-set helper `_verify_eligibility_artifact_cli_envelope` is the single envelope owner; the verifier model is no longer reused to represent CLI failures. The post-verifier defensive `Exception` catch stays **INVALID** / `eligibility_artifact_invalid_field` (payload had reached the verifier). (P1-B conflict envelope) a mutually-exclusive conflict that includes `--verify-approval-consumption-eligibility-artifact` now emits a dedicated artifact FAIL envelope (`reason_codes=["eligibility_artifact_verification_mode_conflict"]`, full stable key set + constant NO-GO posture, metadata `null`) instead of the generic/approval-intent envelope. Precedence: `--run` (exit 2) > mode conflict > `--json` required > argument not applicable > stdin; the artifact conflict envelope takes precedence over the approval-intent conflict envelope when both flags participate. Stable envelope key set on every outcome: `outcome` / `mode` / `schema_version` / `approval_intent_schema_version` / `approval_intent_sha256` / `candidate_evidence_schema_version` / `candidate_evidence_sha256` / `eligibility_artifact_sha256` / `reason_codes` (list only — no singular `reason_code`) + the six constant-posture fields. Parser-vs-verifier root responsibility documented: the bounded parser only requires syntactically valid JSON; the **verifier** requires an exact object root (list/string/null → `INVALID` / `eligibility_artifact_not_object`). Mode-inventory count removed from the CLI module docstring and composition contract ("Mutually-exclusive modes"), and the new flag added to the `_resolve_mode` conflict message. Tests: `test_missing_json_is_fail` / `test_forbidden_arguments_are_not_applicable` / `test_stdin_input_errors` assert exact `FAIL` + full key set + null metadata + constant posture; the mode-conflict matrix is parameterized across all conflicting flags (+ `--run` precedence + artifact-over-approval-intent precedence); the ambiguous `assert outcome in ("FAIL","INVALID")` is removed. **No new scope:** no persistence, external-file input, consumption, consumed marker, replay/nonce, signing/HMAC, identity/provenance authentication, intent/evidence lookup, TTL/freshness re-evaluation, activation caller/token, `--run`, KIS/network, broker/order, operational DB/fs write, daemon/scheduler. **Still OPEN (unchanged posture):** actual approval consumption, consumed marker, replay/nonce/idempotency, signing/HMAC, Operator identity authentication, origin/provenance verification, artifact persistence / external-file CLI / file output, intent/evidence lookup, TTL/freshness re-evaluation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4w — canonical eligibility-artifact persistence-payload encode/decode (API-only, no file I/O; runtime activation still NO-GO).** Fixes the canonical persistence **byte format** and strict round-trip ahead of any actual writer/reader. New pure module `composition.operator_approval_consumption_eligibility_artifact_persistence_payload` with `encode_verified_operator_approval_consumption_eligibility_artifact(snapshot)` → `EligibilityArtifactPersistencePayloadResult` (`CREATED`/`INVALID`) and `decode_operator_approval_consumption_eligibility_artifact_payload(payload_bytes)` → `EligibilityArtifactPersistencePayloadVerification` (`VALID`/`INVALID`). The payload **is** the existing 13-field artifact JSON object (no new wrapper/envelope, no separate persistence hash); encoding is `decision.canonical_json.canonical_json_dumps` → UTF-8 with no trailing newline and no BOM, so an identical snapshot is byte-for-byte identical. Encoder: exact `type(snapshot) is VerifiedOperatorApprovalConsumptionEligibilityArtifact` (subclass/`asdict`-corrupted/arbitrary object → `INVALID` / `eligibility_persistence_payload_invalid_snapshot`), 13 scalars read once into a built-in dict, re-validated through the existing artifact verifier **exactly once**, canonical bytes only on VALID; `payload_bytes`/`eligibility_artifact_sha256` are `None` on INVALID; caller mutation after capture cannot change the result. Decoder: exact `type(payload_bytes) is bytes` (`bytearray`/`memoryview`/bytes-subclass/str → `_not_bytes`), empty → `_empty`, 1 MiB bound → `_too_large`, UTF-8 → `_not_utf8`, then the existing bounded strict JSON parser (`_not_json` / `_too_deep` / `_duplicate_key`; NaN/Infinity/-Infinity → `_not_json`), then `verify_and_snapshot_...` **exactly once**; root list/string/null parse fine then the verifier returns `eligibility_artifact_not_object`, and a verifier rejection preserves the artifact reason verbatim (`eligibility_artifact_missing_field` / `_invalid_field` / `_hash_mismatch`). Per call: encoder verify `1` + `canonical_json_dumps` `1`; decoder UTF-8 decode `1` + parse `1` + verify-and-snapshot `1`; zero filesystem/path/clock/config/env/DB/network/builder/eligibility/intent-verifier rerun. Ordinary `Exception` → sanitized INVALID (no raw field/hash/timestamp/path/exception); `MemoryError`/`KeyboardInterrupt`/`SystemExit` re-raised in both. Decode VALID = schema·semantic·hash **consistency** only — a freshly minted semantically valid payload with a recomputed digest decodes VALID by design (Category B); it is **not** proof that the payload was persisted, of payload origin/provenance, Operator identity, signature/HMAC, approval consumption, replay prevention, or activation authorization. **Carry-over CLI hardening (H1):** the 4v verify CLI now runs the verifier call *and* result validation/summary inside one defensive boundary; `_verify_eligibility_artifact_summary` validates `type(result) is OperatorApprovalConsumptionEligibilityArtifactVerification` and the exact VALID/INVALID invariants (VALID: `reason_codes==()`, three schema versions exact `int`, three digests lowercase hex64; INVALID: `reason_codes` a length-1 tuple) **before** reading metadata, so a malformed verifier return (None/object/dict/subclass/wrong-outcome/property-raising/invariant-violating) fails closed to exit 1 / `INVALID` / `eligibility_artifact_invalid_field` / null metadata with no raw object/value/property leak. **Carry-over (H2):** spies assert applicability/mode FAIL reads stdin `0` and calls the verifier `0`, a stdin-boundary FAIL reads stdin `1` / verifier `0`, and an artifact INVALID/VALID calls the verifier `1`. New `tests/test_operator_approval_consumption_eligibility_artifact_persistence_payload.py` + carry-over tests in the 4v CLI test; new `docs/PAPER_FAST_LOOP_OPERATOR_APPROVAL_CONSUMPTION_ELIGIBILITY_ARTIFACT_PERSISTENCE_PAYLOAD_CONTRACT.md`; artifact / verified-artifact / verification-CLI / attended-activation / composition contracts synced. **Still OPEN (unchanged posture):** actual file persistence (atomic writer/reader CLI deferred to a follow-on lane after this format passes review), actual approval consumption, consumed marker, replay/nonce/idempotency, signing/HMAC, Operator identity authentication, origin/provenance verification, intent/evidence lookup, TTL/freshness re-evaluation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.
    - **RTM-7c.4w — result-invariant and canonical-decode closure (API-only, no file I/O; runtime activation still NO-GO).** Closes independent-review findings on the committed 4w persistence lane without adding file I/O, consumption, replay, signing, or activation. (P1-A dependency result invariants) Shared pure helpers exported from `composition.operator_approval_consumption_eligibility_artifact_verifier`: `validate_operator_approval_consumption_eligibility_artifact_verification_invariants`, `operator_approval_consumption_eligibility_artifact_verification_metadata_matches_payload`, `validate_verified_operator_approval_consumption_eligibility_artifact_result_invariants`, `is_lower_hex64`. Persistence encoder now requires exact verifier return type + VALID/INVALID invariants + six-field metadata binding against the captured payload before `canonical_json_dumps`; malformed verifier dependency results (None/object/dict/subclass/wrong-outcome/property-raising/invariant-violation/metadata-mismatch) fail closed to `INVALID` / `eligibility_persistence_payload_invalid_snapshot` with `payload_bytes=None`. Persistence decoder validates exact `VerifiedOperatorApprovalConsumptionEligibilityArtifactResult` invariants before returning `VALID`; malformed snapshot dependency results fail closed to `eligibility_persistence_payload_invalid_artifact`. The 4v verify CLI `_verify_eligibility_artifact_summary` delegates to the same verification invariant helper (no duplicated rules; `composition` does not import `ops`). (P1-B canonical decode enforcement) Decoder rebuilds canonical bytes from the verified snapshot and requires **exact** equality with input bytes; semantically/hash-valid but noncanonical representations (pretty JSON, reordered keys, leading/trailing whitespace, trailing newline) → `INVALID` / `eligibility_persistence_payload_not_canonical`. Reason precedence: parser reasons (`_not_json` / `_too_deep` / `_duplicate_key`) before artifact verifier reasons before malformed dependency (`_invalid_artifact`) before `_not_canonical`. (P2 exception taxonomy) Parser/UTF-8 ordinary exceptions → `_not_json` (or mapped parser reason); verify-and-snapshot and canonical re-encode ordinary exceptions → `_invalid_artifact`; encoder ordinary exceptions → `_invalid_snapshot`; fatal exceptions re-raised. Tests: encoder/decoder malformed dependency matrices, metadata mismatch, canonical/noncanonical matrix, reason precedence, round-trip digest/canonical equality, exception sanitization, call-count isolation; 4v CLI regression retained. Contracts synced (persistence-payload / artifact / verified-artifact / verification-CLI / attended-activation / composition + this entry). **Still OPEN (unchanged posture):** actual file persistence (atomic writer/reader CLI deferred to follow-on lane), actual approval consumption, consumed marker, replay/nonce/idempotency, signing/HMAC, Operator identity authentication, origin/provenance verification, intent/evidence lookup, TTL/freshness re-evaluation, activation caller/token, `--run`, KIS/network, broker/order, operational DB write / schema migration / reconcile, daemon/scheduler/process lock, unattended pilot, writer-stop machine proof, default runtime activation.

### AccountRole / legacy runtime data
- If old runtime SQLite databases contain legacy `account_role` values (`ISA`, `GENERAL`, `CMA`), add an explicit migration script or discard those runtime DBs. Current semantic values are `KR_TAX_ADVANTAGED`, `US_REGULAR`, `CASH_BUFFER`, `PAPER`.

### Emergency / MDD
- Add optional DailySummary projection for emergency/MDD events without changing Phase 12 store semantics.
- Consider settings/config override for emergency thresholds. Current implementation uses module constants.
- Review MDD liquidation overshoot behavior. Loss-position-first liquidation can exceed target cash; partial sell sizing may be needed before execution integration.
- Define explicit detectors for `false_positive_suspected_count` and `missed_risk_suspected_count`. Phase 16 currently keeps them at default 0.

### Paper review / report
- ~~Run `ops/build_paper_review_report.py` with a valid `PaperReviewInput` bundle once a safe bundle exists.~~ Done: dev synthetic builder + manual smoke via `ops/dev/build_synthetic_paper_review_input.py`.
- Implement a `PaperReviewInput` collector/exporter that explicitly assembles NAV snapshots, DailySummary records, Postmortem records, Emergency events, OrderIntent records, and Fill records.
- Avoid running `--store` and `--markdown-out` together until partial-side-effect behavior is either accepted or hardened. Recommended operation: generate markdown first, then save to JSONL store after review.

### Paper operation entrypoints
- ~~Add a safe `PaperLoopInput` validated bundle builder for manual paper one-shot testing.~~ Done: `ops/dev/build_synthetic_paper_loop_input.py` (dev-only SYNTH fixtures).
- ~~Run `ops/run_paper_once.py` against a valid bundle after the builder exists.~~ Done after datetime JSON roundtrip hardening in domain validators.

### Paper pilot workflow (Foundation 8A–8I)
- ~~Document 30-trading-day paper pilot daily workflow skeleton.~~ Done: `docs/PAPER_PILOT_WORKFLOW.md`.
- ~~Foundation 8B: Research Source Intake + Date.md Export.~~ Done: `ops/research_source_intake.py`.
- ~~Foundation 8C: Universe v0 + Date.md prompt-reference smoke.~~ Done: `config/universe.paper.toml.example`, `ops/run_date_md_smoke.py`.
- ~~Foundation 8D: Scout Once manual LLM call packet.~~ Done: `ops/build_scout_manual_packet.py`.
  - **P2 hardening candidate (post-Foundation):** 8D/8E/8F/8G/8H/8I ops scripts write three output files sequentially via `write_text()` after preflight existence checks. Mid-sequence failure can leave partial artifacts; recovery is `--force` re-run or manual cleanup. Apply atomic-write pattern before 30-trading-day pilot start as a separate ops-script hardening pass.
  - **P3 static-guard hardening candidate (post-Foundation):** Replace coarse substring-based forbidden-token static guards in ops tests with AST/import/call-based guards. Context: 8I `ops/rehearse_paper_loop_no_write.py` currently builds log strings such as `PaperLoopRunner.run: NOT CALLED` via runtime string concatenation to avoid false positives from source-level substring scans. This is functionally safe but a code smell caused by over-broad static tests. Handle in post-Foundation ops hardening; do not change 8I behavior now.
- ~~Foundation 8E: Manual LLM JSON Intake Validator (Scout).~~ Done: `ops/validate_scout_raw_json.py`.
- ~~Foundation 8F: Portfolio state snapshot + Allocator Once.~~ Done: `ops/build_allocator_manual_packet.py`, `ops/validate_allocator_raw_json.py`, `docs/examples/portfolio_state.paper.example.json`.
- ~~Foundation 8G: Analysis Once (per-symbol).~~ Done: `ops/build_analysis_manual_packet.py`, `ops/validate_analysis_raw_json.py`.
- ~~Foundation 8H: Production PaperLoopInput Assembler (per-symbol).~~ Done: `ops/assemble_paper_loop_input.py`, `docs/examples/paper_loop_context.paper.example.json`.
- ~~Foundation 8I: End-to-End no-write rehearsal.~~ Done: `ops/rehearse_paper_loop_no_write.py`.
- ~~Controlled Day 1 Readiness 0A — runbook contract / no-write boundary smoke.~~ Done: `tests/test_controlled_day1_readiness.py` (static docs-contract only; no 8B–8I execution). **Next human/operator action:** actual Controlled Day 1 manual walk-through per `docs/RUNBOOK.md` § Controlled Day 1 paper walk-through (stops at 8I no-write; broker/PaperLoop/KIS/write-mode out of scope).
- **Evidence-based follow-ups (not pre-implemented):**
  - DailySummary writer helper — trigger: template omission ≥ 2 times per week
  - Postmortem weekly template helper — trigger: tag summary format errors ≥ 2 times

### Date-ID / Date.md
- ~~Add a controlled Date.md export/update helper only after the manual Date-ID workflow is stable.~~ Done in Foundation 8B (`ops/research_source_intake.py`); real API fetchers remain deferred.
- ~~Design Real Research Source Intake v1 (read-only fetch → snapshot → DateIdSourceRecord → 8B).~~ Done: `docs/REAL_RESEARCH_SOURCE_INTAKE.md` (design-only; FRED first; G1–G4 guards).
- ~~Implement Real Intake 1A — FRED replay/fixture-only staging (`ops/fetch_research_sources.py --replay`).~~ Done: `src/data/research_source_fetcher.py`, `src/data/fred_source_fetcher.py`, fixtures + tests; JSONL round-trips through 8B `--validate-only`.
- ~~Implement Real Intake 1B — FRED live-smoke HTTP client~~ Done: `src/data/fred_http_client.py` (urllib isolated), `--live-smoke`, API-key leakage hardening + tests.
- ~~Implement Real Intake 2A — generic PRICE snapshot replay (`--replay --source price`)~~ Done: `src/data/price_source_fetcher.py`, fixtures + tests; JSONL round-trips through 8B and satisfies 8C symbol coverage for matching universe symbols.
- ~~Implement Real Intake 2B — yfinance PRICE live-smoke (`--live-smoke --source price`)~~ Done: `src/data/price_live_client.py` (lazy yfinance import), immutable generic PRICE snapshot → 2A replay; tests use injected ticker factory only.
- ~~Implement Real Intake 3A — DART DISCLOSURE replay/fixture (`--replay --source dart`)~~ Done: `src/data/dart_source_fetcher.py`, store-seeded Date-ID batch allocation, fixtures + tests; JSONL round-trips through 8B; does not satisfy 8C symbol coverage.
- ~~Implement Real Intake 3A.1 — Scout packet context for symbol-matched DART DISCLOSURE~~ Done: `ops/build_scout_manual_packet.py` symbol-only selection; `market=None` preserved; 8C coverage semantics unchanged.
- ~~Real Intake **3B1** — DART live snapshot normalizer + fake HTTP transport tests~~ Done: `src/data/dart_live_client.py`, `tests/test_dart_live_client.py`; injected transport only; snapshot→3A replay→8B validate-only; collision + no-leak guards.
- ~~Real Intake **3B2** — DART operator `--live-smoke`~~ Done: `dart_http_client.py`, `run_live_smoke_dart`; env API key read only in DART live branch; snapshot→3A replay→JSONL.
- Real Intake **3B3** — DART hardening (pagination, error schema, corp-code cache, retry/backoff) — optional after 3B2.
- ~~Real Intake **3C1** — DART corp-code resolver fixture-first (stock_code → corp_code)~~ Done: `src/data/dart_corp_code_resolver.py`, `ops/resolve_dart_corp_code.py`, fixtures + tests; no network/env.
- ~~Real Intake **3C2** — live OpenDART corpCode master download~~ Done: `dart_corp_code_http_client.py`, `dart_corp_code_live_client.py`, `--live-fetch` in `ops/resolve_dart_corp_code.py`; immutable ZIP snapshot → 3C1 parse/resolve; tests use injected transport only.
- ~~Real Intake **3D1** — provider mapping registry fixture-first~~ Done: `provider_mapping_registry.py`, `config/provider_mappings.paper.toml.example`, `ops/validate_provider_mapping.py`; maps internal symbol → yfinance provider_symbol / DART corp_code; no network/env.
- ~~Real Intake **3E1** — static KR real-company sample universe + provider mapping~~ Done: `config/universe.kr-real.sample.toml`, `config/provider_mappings.kr-real.sample.toml`, `tests/test_kr_real_sample_universe.py`; two locally verified companies (Samsung Electronics `005930`, SK hynix `000660`); corp_code from `tests/fixtures/research/dart/corp_code_sample.xml`; no network/env.
- ~~Real Intake **3E2** — KR real sample live PRICE smoke~~ Done: `ops/run_kr_real_price_smoke.py`, `tests/test_kr_real_price_smoke.py`; provider mapping → yfinance snapshot → generic PRICE replay → JSONL; store-seeded Date-ID allocation; 8B validate-only + 8C symbol coverage verified in tests; no DART/FRED/env. Non-goal: 종목별 hard-coded PRICE magnitude band (장기 시세 변동을 잘못 차단할 수 있음).
- ~~Real Intake **3E3** — KR real sample live DART disclosure smoke~~ Done: `ops/run_kr_real_dart_smoke.py`, `tests/test_kr_real_dart_smoke.py`; provider mapping → OpenDART snapshot → adapter replay → combined-batch Date-ID → JSONL; 8B/8C no-require-coverage + Scout 3A.1 context verified in tests; no yfinance/FRED.
- ~~Real Intake **3E4** — combined FRED+PRICE+DART context with Date.md/Scout budget caps~~ Done: `source_record_context_selector.py`, capped 8B `--context-budget-profile kr-real-smoke`, `ops/build_kr_real_combined_context_smoke.py`, `tests/test_combined_context_budget.py`; store retains all records; export cap only; 60KB guard unchanged.
- ~~Real Intake **3F1** — fixture-first KR universe/provider mapping generator~~ Done: `kr_provider_mapping_generator.py`, `ops/generate_kr_provider_mapping.py`, candidate fixture + disambiguation XML; corp_code from local resolver only; stock_code normalization; self-validates via existing loaders. P3 cleanup: control-char rejection at parse/CLI/render (write-safety).
- ~~Real Intake **3F2** — generator-based KR expansion workflow~~ Done: synthetic 5-candidate fixtures + scale tests (`test_kr_real_generated_universe_expansion.py`); operator-local real expansion documented in RUNBOOK (3C2 snapshot → candidate TOML → generator → validate → 3E2/3E3/3E4). Provider auto-generation from operator-curated candidates is supported; sector discovery/ranking is not.
- ~~Real Intake **3G1** — fixture-first sector-tagged KR candidate pool~~ Done: `kr_candidate_pool.py`, `ops/select_kr_candidates.py`, synthetic sector pool fixture + tests; deterministic sector/priority selection; export to 3F1 candidate TOML with pool metadata stripped.
- ~~Real Intake **3G2** — operator-local real sector pool workflow~~ Done: `ops/build_kr_real_sector_pool_mapping.py` chains 3G1 export → 3F1 generate → provider mapping validation; workflow tests use synthetic pool + corp-code fixtures only.
- ~~Real Intake **3G3-0** — live discovery/ranking guardrails~~ Done (docs-only): G1–G6 boundary checkpoint in `docs/REAL_RESEARCH_SOURCE_INTAKE.md`; approved expansion path and phase split documented; no code/tests.
- ~~Real Intake **3G3-1** — fixture-first KR candidate ranking model~~ Done: `kr_candidate_ranker.py`, `ops/rank_kr_candidates.py`, synthetic ranking signal fixture + tests; deterministic weighted score with explainable components; ranked JSON is reviewable metadata only.
- ~~Real Intake **3G3-2** — operator-local real ranking input workflow~~ Done: `ops/build_kr_real_ranked_mapping.py` chains 3G3-1 rank → 3F1 generate → provider mapping validation; operator supplies real sector pool + local ranking signals + corp-code snapshot; no live API.
- ~~Real Intake **3G3-3** — discovery snapshot replay adapter~~ Done: `kr_discovery_source_adapter.py`, `ops/replay_kr_discovery_snapshot.py`, synthetic discovery snapshot fixture + tests; snapshot → 3G1 candidate pool only; live transport deferred to 3G3-4.
- ~~Real Intake **3G3-4A** — live-shaped fake-transport discovery snapshot fetcher~~ Done: `kr_discovery_live_client.py` + tests; injected transport → immutable raw discovery snapshot; validate-before-commit; real live transport deferred to 3G3-4B.
- ~~Real Intake **3G3-4B** — operator-triggered HTTP discovery live smoke~~ Done: `kr_discovery_http_client.py`, `ops/run_kr_discovery_live_smoke.py` + tests; operator-supplied endpoint URL; sanitized HTTP errors; optional 3G3-3 candidate pool replay; no env/API keys.
- ~~Real Intake **3G3-5** — fixture-first KR discovery source schema mapper~~ Done: `kr_discovery_schema_mapper.py`, `ops/map_kr_discovery_fixture.py`, synthetic provider payload fixture + tests; source-specific fixture → canonical transport → 3G3-4A snapshot → optional 3G3-3 candidate pool; no network/env.
- ~~Real Intake **3G3-6** — operator-triggered source-specific KR discovery live endpoint adapter~~ Done: `kr_discovery_source_payload_snapshot.py`, `ops/run_kr_discovery_source_live_smoke.py` + tests; HTTP → immutable source snapshot → 3G3-5 mapper → 3G3-4A canonical snapshot → optional 3G3-3 candidate pool; no env/API keys.
- Real Intake **3G3-6+** — source-specific live adapter hardening — deferred.
- ~~Real Intake **3G4-0** — factor scoring guardrail checkpoint~~ — Done.
- ~~Real Intake **3G4-1** — fixture-first factor signal generator~~ Done: `kr_factor_signal_generator.py`, `ops/generate_kr_factor_signals.py`, synthetic factor input fixture + tests; local factor input → 3G3-1 ranking signal TOML; self-validates via existing ranker parser; no network/env.
- ~~Real Intake **3G4-2** — factor scorer → ranked mapping workflow integration~~ Done: `ops/build_kr_factor_ranked_mapping.py` + tests; thin orchestration over 3G4-1 + 3G3-2; reviewable artifacts only; no duplicated scoring/ranking/mapping logic; no network/env.
- ~~Real Intake **3G4-3** — operator-local real factor input bundle~~ Done: `ops/build_kr_factor_bundle_mapping.py` + synthetic bundle fixture + tests; operator-local bundle manifest wrapper over 3G4-2; reviewable artifacts only; no live factor scoring; no network/env.
- ~~Real Intake **3G4-4** — source-specific factor adapter~~ Done: `kr_factor_source_adapter.py`, `ops/map_kr_factor_fixture.py`, synthetic source payload fixture + tests; source-specific fixture → canonical factor input TOML → 3G4-1/3G4-2 downstream proof; no live factor transport; no network/env.
- ~~Real Intake **3G4-5** — operator-triggered live factor source smoke~~ Done: `kr_factor_source_http_client.py`, `kr_factor_source_payload_snapshot.py`, `ops/run_kr_factor_source_live_smoke.py` + tests; HTTP → immutable raw snapshot → optional 3G4-4 replay; no env/API keys; no scheduled fetch.
- ~~Real Intake **3G4-H1** — factor intake hardening cleanup~~ Done: validate-before-commit factor input TOML writer; snapshot unexpected-error sanitization; programmatic ValueError stage normalization; no workflow semantics change.
- ~~Real Intake **3H0** — operator end-to-end intake guardrail checkpoint~~ Done (docs-only): approved artifact flow discovery → factor → ranking/generation → validation → 3E smokes → Scout documented in `docs/REAL_RESEARCH_SOURCE_INTAKE.md` + RUNBOOK operator note; no code/tests; no new command.
- ~~Real Intake **3H1** — operator-local end-to-end manifest/preflight helper~~ Done: `ops/preflight_kr_end_to_end_intake.py` + synthetic manifest fixture + tests; validates existing artifact paths + provider mapping coverage; optional review-only follow-up command plan; no live fetch/smoke/8B/8C/Scout execution; no config mutation; no network/env.
- ~~Real Intake **3H2** — end-to-end preflight hardening cleanup~~ Done: atomic summary/plan writes; write-error sanitization; positive allowlist validation for follow-up command plan; no workflow semantics change; no network/env.
- ~~Real Intake **3H3** — structured follow-up plan JSON artifact~~ Done: optional `[outputs].structured_plan_out` / `--structured-plan-out`; single internal `FollowupStep` representation for Markdown + JSON; review-only; no command execution; no network/env.
- ~~Real Intake **3H4** — structured follow-up plan validator~~ Done: `ops/validate_kr_end_to_end_preflight_plan.py`; read-only schema/allowlist/review-only audit for 3H3 JSON; drift guard vs preflight allowlist; no command execution; no network/env.
- ~~Real Intake **3H5** — structured plan validator command-line safety hardening~~ Done: `_validate_command_line_safety()` uses exact unsafe execution token guard + structured-field rejection; broad command substring false positives removed; no contract/execution change.
- ~~Real Intake **3H6** — structured plan validator optional validation report~~ Done: `--report-out` / `--force` on `ops/validate_kr_end_to_end_preflight_plan.py`; compact audit JSON after successful validation only; atomic write; no command execution; no network/env.
- ~~Real Intake **3H7** — operator handoff manifest / artifact integrity index~~ Done: `ops/build_kr_end_to_end_handoff_manifest.py`; indexes artifact paths/sha256 only; embeds no artifact bodies; atomic write; no command execution; no network/env.
- ~~Real Intake **3H8** — operator handoff manifest integrity verifier~~ Done: `ops/verify_kr_end_to_end_handoff_manifest.py`; recomputes size/sha256 and validates recorded JSON metadata; writes no files; no command execution; no network/env.
- ~~Real Intake **3H9** — handoff manifest verifier schema hardening~~ Done: exact-key schema lock on top-level manifest and artifact entries; unknown keys rejected at validate stage; no workflow behavior change; no network/env.
- ~~Real Intake **3H10** — handoff manifest verifier optional path containment~~ Done: optional `--base-dir` on `ops/verify_kr_end_to_end_handoff_manifest.py`; canonical resolved-path containment for manifest and artifact paths; read-only; writes no files; no network/env.
- ~~Real Intake **3H11** — handoff manifest verifier optional verification report~~ Done: `--verification-report-out` / `--force` on `ops/verify_kr_end_to_end_handoff_manifest.py`; compact audit JSON after successful verification only; atomic write; public verifier API remains read-only; no command execution; no network/env.
- ~~Real Intake **3H12** — verification report output path containment~~ Done: when `--base-dir` and `--verification-report-out` are both supplied on `ops/verify_kr_end_to_end_handoff_manifest.py`, report output must resolve inside base_dir; validate-stage rejection before write; no new success payload keys; no command execution; no network/env.
- ~~Real Intake **3H13** — verification report schema self-validation~~ Done: in-memory exact-key validation of verification report payload before atomic write; invalid payloads never reach disk; `artifact_roles` reuses `_validate_artifact_roles`; no new success payload keys; no command execution; no network/env.
- ~~Real Intake **3H14** — handoff manifest builder validate-before-commit~~ Done: temp write → existing 3H8 verifier validation → atomic replace; invalid generated manifests never reach `--manifest-out`; no new CLI flags; no command execution; no network/env.
- ~~Real Intake **3H15** — handoff manifest builder optional path containment~~ Done: optional `--base-dir` on `ops/build_kr_end_to_end_handoff_manifest.py`; canonical resolved-path containment for supplied artifacts and `manifest_out` before read/write; 3H14 validate-before-commit calls verifier with same resolved base; manifest/API/CLI success key sets unchanged; no command execution; no network/env.
- ~~Real Intake **3H16** — end-to-end handoff bundle round-trip smoke~~ Done: fixture-only no-exec API round-trip in `tests/test_kr_end_to_end_preflight.py` (preflight summary/plan/structured plan → validation report → handoff manifest builder with `base_dir` → verifier with `base_dir` + verification report); no new ops CLI; no command execution; no network/env.
- ~~Real Intake **3H17** — in-process CLI handoff bundle round-trip smoke~~ Done: no-subprocess in-process `main([...])` round-trip in `tests/test_kr_end_to_end_preflight.py` (preflight/validator/builder/verifier CLI wiring with `--base-dir` + `--*-out` inside bundle); known-error CLI rejection asserts `rc == 1`; no new ops CLI; no command execution; no network/env.
- ~~Real Intake **3H18** — API/CLI handoff bundle round-trip parity smoke~~ Done: API round-trip vs in-process CLI round-trip compared via path/hash-independent semantic normalization in `tests/test_kr_end_to_end_preflight.py` (roles/kinds/modes/flags/entry-key sets equal; sha256 shape + size>0 + base_dir presence only; per-bundle containment; body-free; no-exec); no new ops CLI; no command execution; no network/env.
- ~~Real Intake **3H19** — generated handoff bundle tamper-detection smoke~~ Done: `tests/test_kr_end_to_end_preflight.py` mutates 3H16 round-trip bundles and re-verifies (integrity/parse/metadata/containment/report-not-written/body-free); no new ops CLI; no command execution; no network/env.
- ~~Real Intake **3H20** — CLI verifier tamper-rejection smoke~~ Done: in-process `verify_handoff_manifest_main([... "--json"])` on tampered 3H16-generated bundles (`rc == 1`; exact CLI error mode; stage mapping; no traceback/raw-body echo; report not written on failure); no subprocess; no new ops CLI; no command execution; no network/env.
- ~~Real Intake **3H21** — handoff pipeline failure no-partial-output smoke~~ Done: in-process CLI pipeline failure tests in `tests/test_kr_end_to_end_preflight.py` (no partial downstream outputs; base-dir parent not created; `--force` preserves existing report on pre-write failure; safe JSON errors; no subprocess; no network/env).
- ~~Real Intake **3H22** — normalized handoff bundle reproducibility smoke~~ Done: repeated API/CLI in-process round-trips compared via 3H18 path/hash-independent normalization in `tests/test_kr_end_to_end_preflight.py` (semantic manifest/report contract equality; per-bundle containment; exact-key body-free; no-exec); no new ops CLI; no command execution; no network/env.
- ~~Real Intake **3H23** — CLI stdout success payload contract smoke~~ Done: in-process `--json` success payload contracts for four handoff CLIs in `tests/test_kr_end_to_end_preflight.py` (exact mode/stage/status, exact key sets, body-free, no sensitive/trading fields, advisory-only preflight followup_commands, output containment; no-exec); no new ops CLI; no subprocess; no network/env.
- ~~Real Intake **3H24** — CLI stdout known-error payload contract smoke~~ Done: in-process `--json` known-domain-error payload contracts for four handoff CLIs in `tests/test_kr_end_to_end_preflight.py` (exact mode/stage/status, exact four-key set, safe message, no traceback/raw-body echo, no partial downstream outputs, no sensitive/trading field keys; preflight missing-manifest domain path only; no-exec); no new ops CLI; no subprocess; no network/env.
- ~~Real Intake **3H25** — CLI stdout JSON channel discipline smoke~~ Done: in-process `--json` stdout channel discipline for four handoff CLIs in `tests/test_kr_end_to_end_preflight.py` (single-object stdout via raw_decode, no human prefix/suffix, no traceback, no stderr JSON payload, success output existence, known-error no-partial outputs, no-exec; no subprocess; no network/env).
- ~~Real Intake **3H26** — CLI argument-domain failure no-output smoke~~ Done: in-process `--json` argument-domain failure tests for validator/builder/verifier handoff CLIs in `tests/test_kr_end_to_end_preflight.py` (`write` output-exists preservation, `validate` missing/not-dir base_dir no partial output, stable blank path `args` errors; exact four-key error payload; no traceback/sentinel echo; no subprocess; no network/env).
- ~~Real Intake **3H27** — CLI help/usage side-effect smoke~~ Done: in-process argparse `--help`/`main([])` tests for four handoff CLIs in `tests/test_kr_end_to_end_preflight.py` (`SystemExit` clean/non-zero; human usage text; no traceback/JSON; empty `tmp_path`; no subprocess/generated command execution; no network/env).
- ~~Real Intake **3H28** — CLI help/usage wording contract smoke~~ Done: in-process `--help`/`main([])` discoverability tests for four handoff CLIs in `tests/test_kr_end_to_end_preflight.py` (operator-critical flag tokens in help; usage without traceback/JSON; forbidden compound operational tokens absent; no full help snapshot; no subprocess; no network/env).
- ~~Real Intake **3H29** — CLI non-JSON human output smoke~~ Done: in-process human CLI output tests for four handoff CLIs in `tests/test_kr_end_to_end_preflight.py` (success `rc == 0` + artifact writes; known-domain error `rc == 1` + no partial outputs; non-JSON stdout/stderr; no traceback/raw-body echo; validator output-exists preservation; no-exec; preflight conflicting-flags path excluded; no subprocess; no network/env).
- Real Intake **3H30+** — end-to-end hardening (richer provenance, drift checks, orchestration ergonomics) — Deferred. Broker/PaperLoop/KIS remains out of scope for Real Research Source Intake.
- Real Intake **3G4+** — calibration / provenance / drift checks / explainability hardening — Deferred.
- Real Intake **3E5+** — provider auto-refresh, automatic universe expansion — deferred.
- Date.md must remain a read-only reference for LLM prompts; Date-ID validation failures must reject the corresponding LLM output.

### Ollama operation
- Track local model smoke/latency stability for `qwen3.6:35b-mlx` and fallback `qwen3.6:35b`.
- Do not auto-pull or auto-install Ollama models from scripts.

## P3 — Phase 16 Long Paper Trading Review / Parameter Review

- Real benchmark-relative review once benchmark series source is stable.
- DailySummary coverage projection if existing data is incomplete for review period gap analysis.
- Slippage/execution-quality review once reference prices are stored consistently in paper ledger inputs.
- Parameter recommendation human approval workflow before any config edits (Phase 16 produces candidates only).
- Explicit false-positive / missed-risk detector rules for MDD threshold review before those counts become non-zero.
- `MddThresholdReview.false_positive_suspected_count` / `missed_risk_suspected_count` currently default to 0 but the model itself does not reject non-zero values. After explicit detector rules are agreed, consider adding a validator that rejects non-zero values until those detectors exist, or document the allowed source.
- `PaperReviewInput` validates that `nav_snapshots`, `daily_summaries`, `postmortem_records`, and `emergency_events` fall within the review period, but does not yet validate that `order_intents` and `fills` timestamps fall within the period. Add explicit timestamp range validation if stale order/fill data could enter review inputs.

## P3 — Phase 15 Emergency Triggers

- Review MDD liquidation overshoot behavior: current Phase 15 planning may sell full loss positions before proportional profitable-position sales. This is safe as a conservative emergency plan foundation, but before execution integration, decide whether loss positions should support partial sells to stay closer to the target cash percentage and the 1~3% residual mismatch rule.

## P3 — Phase 14 KIS Live Read-only / Tiny-live Rehearsal

- Validate KIS OpenAPI endpoint paths, TR IDs, request parameters, and response field variants against official KIS documentation before real read-only smoke.
- `StdlibKisHttpTransport` is a manual-smoke scaffold and is not connected to scheduler/runtime default paths.
- `broker.kis_read_only.enabled=true` is not an automatic execution trigger. Read-only smoke must remain explicitly invoked unless a later phase adds a reviewed CLI or runner.
- Tiny-live actual order submission is intentionally not implemented in Phase 14. Handle it only in a later explicit manual rehearsal phase.

## P3 — Phase 13 Postmortem

- `parse_postmortem_tag_summary_from_markdown()` accepts a single fenced `json` block anywhere in the document, but rule 08 requires the tag summary to be at the **end** of every Postmortem. Before live Postmortem authoring begins, harden the parser to require the JSON block to be the last non-whitespace content (reject if any prose follows it).
- `validate_postmortem_error_tags()` coerces non-string keys via `str(raw_tag)` before catalog validation. JSON input always has string keys so this is benign in practice, but direct Python callers can pass non-string keys and have them silently stringified. Consider rejecting non-string keys explicitly to match the strictness of other validators.
- `PostmortemTagSummary` currently rejects empty `error_tags`. This matches the "오답노트" spirit, but real operations may need to record a Postmortem with no flagged mistakes (e.g., a clean week). Decide whether to allow `error_tags={}` + `top_error_tags=()` before Phase 13 outputs are consumed by Top 3 aggregation in production.

## P3 — Phase 12 Logs / DailySummary / Debug Events

- `ALLOCATOR_DATE_ID_FUTURE_SOURCE` / `ANALYSIS_DATE_ID_FUTURE_SOURCE` currently map to `DATE_ID_FORMAT_INVALID` because there is no dedicated future-source debug event code. Later consider adding a canonical `DATE_ID_FUTURE_SOURCE` or `EVIDENCE_FUTURE_SOURCE` code to `docs/DEBUG_EVENT_CODES.md`.
- `RISK_GOLD_TRADE_FREQUENCY_EXCEEDED` currently maps to `GOLD_TRADE_BLOCKED_MONTHLY_LIMIT` without distinguishing monthly vs quarterly violations. Later split the risk issue code or inspect issue metadata/path to map quarterly violations to `GOLD_TRADE_BLOCKED_QUARTERLY_LIMIT`.
- `DailySummary.range_violation_count` and `allocator_fallback_count` are Phase 12 foundation fields and currently default to 0 in projection. Later add explicit classifiers when validation/debug event semantics stabilize.

## P3 — Phase 11 Paper E2E Loop

- Phase 12 added `PAPER_NAV_SNAPSHOT_ERROR` catalog support and `debug_event_from_nav_snapshot_error()` helper. Future runtime integration may record this event if NAV snapshot write fails after a paper fill.
- Future cleanup: make `NavSnapshot` currency-agnostic instead of KRW-named fields (`total_nav_krw`, `cash_krw`, `invested_krw`).

## P3 — Phase 10 Risk

- Keep `tests/conftest.py` fixtures minimal as Phase 11 E2E tests grow.
- ~~Phase 11 must own target_weight_percent → executable quantity conversion~~ — done in Phase 11 (`QuantityResolver`).

## P3 — Phase 9 Analysis

- `ANALYSIS_CONFLICTING_PERSPECTIVES_UNSUPPORTED` is exported but not emitted in Phase 9. Revisit during Phase 10+ if explicit bear/bull conflict rules are introduced.

## P3 — Phase 8 Allocator

- `AllocatorAction` enum is currently not used by Phase 8 schema. Revisit during final API surface cleanup.

## P3 — Phase 7 Scout

- `ScoutInputBuilder` currently loads all records and filters fact types in Python. Later optimize by delegating single fact_type filters to `SQLiteDateIdSourceStore.list_records(fact_type=...)`.
- `SCOUT_SCHEMA_INVALID` currently returns one aggregate issue without Pydantic `loc` path. Later expand schema errors into per-field `ValidationIssue.path`.

## P3 — Phase 6 Data

- `DateIdGenerator` scans all records for date prefix. Later replace with SQL prefix/max sequence helper.

## P3 — Phase 4 Decision Store

- `save_decision_snapshot()` relies on callers using `SQLiteDecisionStore.transaction()`. Current tests and usage patterns enforce transaction-scoped writes, which is sufficient for Phase 4. Later hardening can either add a guard that rejects calls outside an active transaction or add an explicit docstring stating that `save_decision_snapshot()` must be called inside a transaction.

## P3 — Phase 3 Broker

- BUY insufficient-cash evaluation computes fee before rejection because cash sufficiency depends on fee-inclusive total cost. Keep fee calculators pure/no-side-effect; revisit only if fee calculation gains external dependencies or side effects.

## Reference — Completed AccountRole Vocabulary Alignment

- Migrated domain `AccountRole` from product/account-name values (`ISA`, `GENERAL`, `CMA`) to semantic portfolio roles (`KR_TAX_ADVANTAGED`, `US_REGULAR`, `CASH_BUFFER`, `PAPER`).
- Mapping: `ISA` → `KR_TAX_ADVANTAGED`, `GENERAL` → `US_REGULAR`, `CMA` → `CASH_BUFFER`, `PAPER` → `PAPER`.
- `PAPER` remains internal PaperBroker-only and is not a live broker account.
- No KIS live routing or account-number mapping was implemented in this cleanup.
- Old enum values are rejected; no alias or silent normalization.
- Persisted SQLite `account_role` values in existing runtime DBs are not auto-migrated; explicit migration required if old values exist.

## Reference — Completed Phase 3 Hardening Notes

These are already implemented and are not open backlog items.

- PaperBroker mismatch/currency validation added.
- LIMIT fill price uses `market_price.price`.
- fee/slippage calculated once per execution decision.
- `paper_cash_ledger` added.
- cash mutation public path restricted to `apply_cash_change`.
- duplicate/PENDING policies fixed by tests.