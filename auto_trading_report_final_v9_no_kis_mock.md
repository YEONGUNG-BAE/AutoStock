# LLM 기반 자동 주식 매매 프로젝트

> **작성자 주의사항:** 본 문서는 비전문가가 설계한 사이드 프로젝트 성격의 LLM 기반 자동 주식 매매 시스템 기획안이다. 따라서 일부 금융·퀀트·소프트웨어 아키텍처 용어는 업계 표준 용어와 다를 수 있고, 대화 과정에서 세부 룰 표현이 바뀌거나 누락될 수 있다. 후속 검토자는 문서의 용어 표현보다 실제 의도와 실행 로직을 우선하여 해석해야 하며, 구현 시에는 최신 문서의 명시된 룰과 Python 하드 필터 기준을 최종 기준으로 삼는다.

## 1. 개요 및 목표

### 1.1 프로젝트 목적

본 프로젝트는 **LLM의 추론 능력을 활용한 자동화된 포트폴리오 운용 시스템**을 구축하는 것을 목표로 한다. 스윙 트레이딩(수일~수주 포지션) 위주로 운용하며, 초단타 및 당일 단타는 10분 처리 지연 구조상 불가능하므로 배제한다.

### 1.2 핵심 성과 목표

절대 수익률이 아닌 **시장 대비 상대 성과 및 하방 보호**에 초점을 맞춘다.

| 시장 상황 | 시장 벤치마크 수익률 | 목표 포트폴리오 수익률 | 목표 상대 성과 |
|---|---|---|---|
| 강세장 | +10% | **+6.5%** | Beta ≈ 0.65 (의도적 언더퍼폼) |
| 약세장 | -10% | **-2.0%** | Downside Capture ≈ 20% (80% 방어) |
| 횡보장 | 0% | +2~3% | 소폭 알파 창출 |

> 이 목표는 시스템의 보수적 설계(리스크 제어 계층, 현금 보유 의무)와 자연스럽게 정합하며, 평가의 기준을 **"폭락해도 버티는 구조"** 로 전환한다.
>
> **운영 프롬프트 사용 제한:** 위 성과 목표는 기획안의 방향성과 페이퍼 트레이딩 평가 기준을 정리하기 위한 문구다. LLM 조사 인스턴스, 분석 인스턴스, Allocator, 포스트모르템 프롬프트에는 이 목표 수익률을 직접 주입하지 않는다. 초기 페이퍼 트레이딩에서는 중립적 운용 성격으로 시작하고, 결과가 과도하게 공격적이면 이후 "방어적 투자자" 성격을 단계적으로 추가한다.

### 1.3 포트폴리오 구성 전략

**국내 주식(KRX), 미국 주식(US), 금(Gold)** 3개 자산군으로 분산 운용한다.

- **금 편입의 전략적 의의:** 주식 시장과 낮은 상관관계를 지닌 안전자산 역할. 금이 하방 완충재 역할을 함으로써 **주식 전략에서 상대적으로 더 높은 공격성(비중 제한 완화 등)을 허용**한다.
- **기본 배분 가이드라인:** 국장 50% / 미장 30% / 금 20% (± LLM의 동적 조정 범위 내)
- **금 ETF 선택:** 국내 상장 ETF 중 순자산 규모 1위인 **ACE KRX금현물(411060)** 사용. 현물 기반이라 위기 시 방어 성능이 선물형 ETF(KODEX/TIGER 골드선물)보다 우수하며, 국장 시간대(09:00~15:30 KST) 거래 가능하여 미장 기반 GLD/IAU 대비 시간대 충돌 회피.
- **금 비중 운영 원칙:** 금은 안전자산이므로 잦은 매매를 지양한다. 평상시 비중 18~22% 범위에서만 조정, 위기 감지 시 25%까지 INCREASE 허용, 강세장 명확 시 15%까지 DECREASE 허용. 미세 조정(±3% 미만 `adjust_percent`)은 수수료만 발생시키므로 Python 후처리에서 자동 MAINTAIN으로 변환한다. INCREASE/DECREASE 결정 시 거시 변수 변화·VIX·환율 추세 등 명분을 반드시 인용해야 하며, "최근 추세상" 같은 모호한 근거는 금지한다.
- **금 비중 비대칭 운영 룰:** "방어 자산은 빨리 늘리고 천천히 줄인다"는 보수적 원칙을 적용한다. 위기 신호 감지 시 INCREASE는 즉시 +5%까지 가능, 강세장 진입 시 DECREASE는 한 번에 -2%까지만 허용. 위기 후 안전자산 잔고를 유지하여 재발 위기 시 충분한 방어력 확보가 목적이다.
- **금 매매 빈도 추적:** Python이 월 단위로 금 매매 횟수를 카운팅하여 분석 프롬프트에 정확한 수치로 주입한다. 목표 빈도는 월 평균 0~2회, 분기당 4회 이하. 초과 시 "이번 달 금 매매 횟수: N회 (목표 0~2회 초과 중)" 형태로 LLM에 인지시켜 자제 또는 강한 명분 제시를 유도한다. LLM은 매매 횟수 카운팅을 직접 수행하지 않는다(환각 방지).

---

## 2. 시스템 아키텍처

### 2.1 핵심 설계 원칙

- **LLM은 판단만, 실행 권한은 Python에:** Python이 Ollama API 직접 호출 및 증권사 API 주문 실행.
- **단일 모델 통일:** 조사·분석·포스트모르템·Allocator 전 과정을 Qwen3.6-35B-A3B(UD-Q4_K_XL 양자화) 단일 모델로 처리한다. MoE 구조(3B 활성)로 경량 모델에 준하는 응답 속도를 확보하므로 별도 경량 모델을 두지 않는다.
- **조사 인스턴스 분리:** 매 조사 시 새 인스턴스 생성 → 컨텍스트 편향(Context Bias) 방지.
- **분석 인스턴스 기록 유지:** 하루 단위 DailySummary 주입으로 연속성 확보.
- **하드 필터 및 MDD 서킷 브레이커:** LLM이 우회할 수 없는 Python 레벨 안전 장치.
- **메시지 파싱 원칙 (자기 합리화 차단):** Ollama API는 stateless이므로 매 호출이 새 인스턴스 컨텍스트로 시작된다. 그러나 messages 배열에 자기 응답(reasoning 원본)을 포함시키면 LLM은 학습된 일관성 패턴(Sycophancy)에 따라 그 응답을 옹호하는 경향이 있다. 따라서 다음 원칙을 적용한다.
  - 후속 호출(검증, 다음 분석 등)에 reasoning 원본 미첨부 — `summary_one_liner` 1줄 요약과 결과(action, 비중)만 전달
  - 검증 호출 시 단답 강제 (YES/NO/REVISE) — 합리화 추론 시간 차단

### 2.2 다중 자산 분석 파이프라인

#### 조사 인스턴스

- 조사를 수행할 때마다 **새 인스턴스를 생성**하여, 이전 조사에 대한 과잉 집중 현상(Context Bias)을 방지한다.
- 데이터 수집 파이프라인: 다음 4개 카테고리에서 로우 데이터를 수집하고 Date-ID를 부여한다.
  - **공시 (후행 지표):** 국장 공시(OpenDartReader), 미장 공시(edgartools)
  - **가격/지표:** yfinance
  - **매크로/심리 (한국 금 시장 특화 6종):** 글로벌 금 가격에 직접 영향을 주는 미국 매크로 지표 + 한국 시장 특화 지표 조합
    - VIX (yfinance `^VIX`): 글로벌 변동성 지수
    - DXY (yfinance `DX-Y.NYB`): 달러 인덱스
    - US 10Y (FRED `DGS10`): 미국 10년물 국채 금리
    - GC=F (yfinance `GC=F`): 국제 금 선물 가격
    - USD/KRW (yfinance `KRW=X`): 원/달러 환율 (한국 금 ETF는 원화 환산 영향 직접 받음)
    - V-KOSPI (KRX 또는 yfinance `^VKOSPI`): 한국 시장 변동성 지수
  - **뉴스 (선행 지표):** 산업 동향·기술 트렌드를 공시보다 먼저 포착하기 위해 신뢰도 B급 이상 매체로 한정 수집
- 조사된 결과는 `[YYMMDD-N]` 형태의 **Date-ID**로 저장된다.
- **입력:** Python이 조립한 프롬프트 (현재 포트폴리오 ROI, Postmortem 기반 투자 판단 오류 Top 3, 금 시장 매크로 지표 포함)
- **출력:** 원문 데이터 기반 요약 JSON (긍정/부정/중립 균형 쿼터 적용). JSON에 `summary_one_liner` 필드 포함 — 수집된 정보의 핵심을 1줄로 요약 (DailySummary 기록·검증 호출 시 활용)
- **검증:** Python이 Date-ID 존재 여부 및 수치 오차 1% 이내 확인

**Date-ID stale 검증 원칙:** Date-ID는 존재 여부만 확인하면 부족하다. 가격·수급·환율처럼 빠르게 변하는 사실은 오래된 Date-ID가 오늘 판단 근거로 재사용될 수 있으므로 Python이 `source_timestamp`, `fact_type`, `allowed_staleness_hours`를 함께 검증한다. 초기 기본값은 PRICE/FLOW/FX 24시간, NEWS 7일, DISCLOSURE 90일, MACRO 지표별 설정값으로 둔다. 오래된 Date-ID가 새 사실처럼 사용되면 해당 LLM 출력은 거부하고 Debug.md에 기술 이벤트로 기록한다.

**뉴스 데이터 도입 근거:**

공시는 본질적으로 후행 지표다. 분기 실적 발표 시점에는 이미 시장이 기대를 선반영하여 차익실현 매물이 쏟아지는 "Sell on news" 구간이 되는 경우가 많다. 예: 하이닉스 HBM 호조는 외신·전문 매체 → 일반 경제지 → 주가 반영 순으로 정보가 흐르며, 공시(분기 실적)는 가장 마지막에 도달한다. 공시와 매크로/심리 지표만으로는 산업 흐름·기술 트렌드 변화를 적시에 포착할 수 없으므로 뉴스 소스를 1차 데이터로 편입한다.

**뉴스 매체 신뢰도 등급 시스템:**

| 등급 | 정의 | 처리 |
|---|---|---|
| A급 | 1차 출처 (회사 공식 발표, 정부 통계, 거래소 공시) | 최우선 가중치, Date-ID 검증 엄격 적용 |
| B급 | 주요 매체 (Reuters, Bloomberg, 한국경제, 매일경제, 연합뉴스 등) | 정상 가중치, 검증 가능 사실 추출 |
| C급 이하 | 일반 매체, 클릭베이트 위주 매체, 블로그, 커뮤니티, 소셜 미디어 | **수집 대상에서 제외** |

C급 이하는 노이즈 비율이 너무 높고 환각 위험을 증폭시키므로 1차 수집 단계에서 차단한다. 소셜 미디어(Twitter/X, Reddit, StockTwits 등)도 동일한 이유로 도입하지 않는다.

**뉴스 데이터 소스:**

- **국장:** 네이버 뉴스 검색 API 또는 RSS 크롤링 (한국경제, 매일경제, 연합뉴스 화이트리스트 기반 필터링)
- **미장:** Finnhub News API (무료 60req/min, 종목 코드 직접 검색 지원, Reuters/Bloomberg/CNBC 등 B급 이상 매체 포함)
- **거시 흐름 (선택):** Reuters RSS, 연합뉴스 RSS

**뉴스 데이터 처리 룰 (토큰 예산 및 환각 대응):**

뉴스를 그대로 Scout에 넘기면 종목당 5,000토큰 × 20종목 = 100,000토큰으로 컨텍스트 한계를 초과한다. 따라서 다음 사전 필터링을 Python 단계에서 수행한다.

1. **종목명 직접 언급 + 최근 7일 + B급 이상 매체** 조건으로 1차 필터링
2. 종목당 **최근 5개 기사**까지만 선별
3. 각 기사를 Python이 **3문장 요약**으로 압축 후 Scout에 전달
4. 검증 가능한 사실(수치, 일자, 인용)에만 Date-ID 부여 — 추측성 표현(`~할 것으로 보인다`, `전망된다`)은 가중치 ↓

**Scout 균형 쿼터의 뉴스 적용:**

뉴스는 구조적으로 Bull 편향이 강하다. 기자가 기업 IR로부터 정보를 받아 작성하는 경우가 많고, "호재" 기사는 많은 반면 "리스크" 기사는 상대적으로 적다. 따라서 Scout 프롬프트에 다음 룰을 추가한다.

- 뉴스 데이터에서 추출한 긍정 요인이 과반인 경우 **부정 요인 재탐색을 강제**한다.
- 분석 인스턴스의 Bear 역할 프롬프트에 **"기사들의 낙관 편향을 비판적으로 평가하라"** 지시를 명시한다.
- 1차 출처(A급) 데이터가 있는 경우 뉴스(B급) 해석을 1차 출처로 검증한다.

**Scout 균형 쿼터 시스템:**

Scout 인스턴스는 원문 데이터에서 정보를 추출할 때 긍정/부정 균형을 강제한다. 고정 숫자 쿼터가 아닌 **균형 원칙**을 적용하여 없는 요인을 억지로 생성하는 환각을 방지한다.

```
Scout 프롬프트 지시:
- 발견한 요인을 긍정 / 부정 / 중립으로 분류하라. (Date-ID 필수)
- 긍정 또는 부정 중 한쪽만 존재한다면, 반드시 그 이유를 명시하라.
  (예: "이 공시는 단순 정정이라 부정 요인이 존재하지 않음")
- 양쪽 요인이 모두 존재함에도 한쪽이 0개면 재탐색하라.
- 없는 요인을 만들어내지 마라. 없으면 없다고 명시하는 것이 정확한 데이터다.
```

> **설계 근거:** 조사 인스턴스에 Lost in the Middle 위험이 있다는 지적이 있으나, 조사 인스턴스의 입력은 원문 전체가 아니라 Python이 전처리한 요약본이다. 원문은 Python이 청크 단위로 처리하여 구조화된 데이터로 변환한 뒤 LLM에 전달하므로, 원문 길이 자체가 컨텍스트 창에 직접 영향을 주지 않는다.

> **환각 검증의 한계와 대응:** Date-ID + 수치 오차 검증은 방향성 환각(수치는 맞지만 해석이 반대)을 잡지 못한다. 이를 완전히 차단하는 방법은 근거 원문을 JSON 옆에 그대로 첨부하여 분석 인스턴스가 원문과 LLM 해석을 동시에 보도록 하는 것이다. temperature=0 설정으로 일관성을 확보하며, 원문과 해석이 인접한 상태에서 정반대 방향의 환각이 발생할 가능성은 구조적으로 낮다.

#### 분석 인스턴스 (단일 인스턴스 4역할 순차 수행, Bear First)

- 분석을 시작할 때마다 **새 인스턴스를 생성하지만, DailySummary를 통해 과거의 기록을 넘겨준다.**
- 이전 투자에 대한 과잉 신뢰는 **Python이 매 분석 시작 시 프롬프트 상단에 다음 두 가지를 자동 주입**하는 방식으로 방지한다.
  - 현재 포트폴리오 ROI (증권사 API에서 실시간 추출)
  - 최근 Postmortem에서 집계된 투자 판단 오류 Top 3 (Python 카운팅)
- 기획안의 목표 수익률(강세장/약세장/횡보장 목표)은 분석 인스턴스 프롬프트에 주입하지 않는다. 목표 수익률을 직접 주입하면 LLM이 목표 달성을 위해 과도하게 공격적 또는 방어적으로 자기 조정할 수 있으므로, 초기 페이퍼 트레이딩에서는 중립적 지시만 사용한다.
- 수익률 하락 시 분석 강도를 높이는 가중치 로직은 배제한다. 감정적 대응에 해당하며 시스템 일관성을 해친다.
- 분석의 결과로 Action이 진행되면, 해당 시간과 Action을 [Date-ID]와 함께 **DailySummary**에 저장한다.
- 각 신생 인스턴스의 분석 결과는 JSON 형태로 TodayThink.md에 저장되며, 오답노트 작성에 활용된다.

분석 프롬프트는 다음의 **Bear First 4역할 순차 수행 구조** 를 따른다.

1. **Bear (약세론자):** 하락 리스크와 악재 우선 분석 — Date-ID 필수 인용
2. **Bull (강세론자):** Bear 논거에 동조하지 않고 독립적으로 호재 분석 — Date-ID 필수 인용
3. **Risk Manager (평가자):** Bear·Bull 의견을 공격적 / 중립적 / 보수적 3관점으로 평가. **판단 및 비중 결정 없음 — 평가만 수행**
4. **Fund Manager (결정자):** 전체 종합 후 최종 Action 결정 (주식 BUY/SELL/HOLD). 자산군 비중(국장/미장/금)은 별도 Allocator 인스턴스가 담당하므로 fund_manager는 종목 매매만 결정한다.

**Bear First 설계 근거:** LLM은 구조적 긍정 편향(Positivity Bias)을 가진다. 호재를 먼저 세우면 이후 역할 전체의 톤이 낙관적으로 굳어진다. 하방 방어를 철학으로 삼는 시스템에서 Bear가 먼저 판을 깔면 이후 역할들이 긴장감을 유지한 채 분석한다.

**역할 분리 원칙:** Risk Manager는 평가만 담당하고 결정을 하지 않는다. Fund Manager만이 최종 종목 매매 결정(BUY/SELL/HOLD + 비중)을 한다. 단, 자산군 비중(국장/미장/금)은 fund_manager가 아닌 별도 Allocator 인스턴스가 담당한다.

**4역할 설계 근거 — 출력 토큰 예산:** 기존 TradingAgents 원본 구조(7역할)를 그대로 구현하면 약 17,400 토큰 출력으로 로컬 환경에서 23~24분이 소요된다. 6역할 단일 호출로 압축해도 약 6,800 토큰으로 "Lost in the Middle" 위험 구간(4K~8K)에 진입한다. **4역할 압축 시 약 4,200 토큰으로 35% 감소**하여 처리 시간 약 7분을 달성한다.

**예시 프롬프트:**

```python
system_prompt = f"""
너는 3,000만 원의 실전 자산을 굴리는 냉철한 AI 퀀트 매니저다.
오직 아래 [제공된 데이터]만 사용하여 판단하고, 너의 내부 지식(과거 학습 데이터)은 절대 섞지 마라.
판단 근거를 작성할 때는 반드시 해당 데이터의 [Date-ID]를 문장 끝에 명시하라. (예: "영업이익이 증가했다[260418-1]")

[시스템 주입 정보: 리스크 경고]
- 현재 포트폴리오 ROI: {current_roi}%
- 현재 시간: {current_time} (한국 시간 기준)
- 최근 너의 잦은 실수: {top_3_error_tags} → 이 실수를 반복하지 않도록 극도로 경계할 것.

[제공된 데이터]
{json_from_scout_instance}
{macro_data_from_python}

[수행 역할 및 출력 규격]
아래 4가지 역할을 순차적으로 수행하되, 결과물은 반드시 아래 제시된 JSON 형식으로만 출력하라.
마크다운(```json)이나 다른 설명은 일체 덧붙이지 마라.

=== 역할 전환: Bear ===
제공된 데이터에서 하락 리스크와 악재만 극단적으로 발굴하라. (Date-ID 필수)
희망적 해석은 금지한다. 최악의 시나리오를 상정하라.

=== 역할 전환: Bull ===
[주의: 앞선 Bear의 논리에 절대 동조하지 마라. 너는 완전히 독립된 분석가다.]
원문 데이터의 호재만을 극단적으로 발굴하라. (Date-ID 필수)

=== 역할 전환: Risk Manager ===
Bear와 Bull 의견을 보고 공격적 / 중립적 / 보수적 3관점으로 평가하라.
비중이나 최종 판단은 내리지 마라. 평가만 수행한다.

=== 역할 전환: Fund Manager ===
모든 의견을 종합하여 최종 판결을 내려라.
주식 ACTION은 BUY / SELL / HOLD 중 택 1.
**자산군 비중 결정(국장/미장/금 비율)은 Allocator 인스턴스가 별도로 담당하므로,
너는 Allocator가 정해준 허용 범위 내에서 종목 매매만 결정하라.
금 비중 조정 결정은 출력하지 않는다.**

{{
  "stock_logic": {{
    "bear_perspective": "",
    "bull_perspective": "",
    "risk_manager": {{
      "aggressive": "",
      "neutral": "",
      "conservative": ""
    }},
    "fund_manager": {{
      "reasoning": "",
      "summary_one_liner": "",
      "action": "BUY/SELL/HOLD",
      "weight_percent": 0
    }}
  }}
}}
"""
```

**JSON 스키마 변경 사항:**

- `macro_logic` 필드 제거 — 자산군 비중 결정은 신규 Allocator 인스턴스가 담당 (2.5 자산 배분 결정 섹션 참조)
- `summary_one_liner` 필드 추가 — fund_manager 결정의 핵심을 1줄로 요약 (DailySummary 기록·검증 호출 시 reasoning 원본 대신 첨부되어 자기 합리화 차단)

**논문 근거:**
- **SPP (Wang et al., NAACL 2024):** 단일 LLM에 fine-grained 페르소나를 부여하면 환각을 줄이고 추론 능력을 유지함.
- **Single-Agent LLMs Outperform MAS (arXiv 2604.02460):** 동일 토큰 예산 조건에서 단일 에이전트가 멀티 에이전트를 대부분 추월.
- **SELF-DEBATING (EMNLP 2025 Findings):** 단일 LLM에 상반된 관점을 동시에 생성하게 한 뒤 최종 판단을 내리는 방식이 환각 감지에 효과적임.

### 2.3 리스크 제어 계층화

본 문서에서 리스크 제어 규칙은 **하드 필터 / 소프트 운영 밴드 / 예외 검증 / 실행 모드 예외**로 구분한다.

- **하드 필터:** Python이 무조건 차단하는 규칙. 단, 모든 하드 필터가 모든 실행 모드에서 동일하게 작동하는 것은 아니다. 운용 부분 하한·자산군 밴드·슬리피지처럼 LLM의 과잉 매매나 환각을 통제하기 위한 필터는 `NORMAL` 모드에서 엄격 적용하고, `REBALANCING`·`EMERGENCY_TRIGGER`·`MDD_KILLSWITCH`에서는 목적에 맞게 일시 우회할 수 있다.
- **절대 우회 금지 안전장치:** 거래정지·주문 불가·브로커 API 오류·라이브모드 게이트·감사 로그 저장처럼 거래소/브로커/시스템 무결성과 관련된 검사는 어떤 실행 모드에서도 우회하지 않는다.
- **소프트 운영 밴드:** 평상시 지켜야 하는 권장 비중 범위. 단, 시장 상황에 따라 일시적 위반이 가능하며, 위반 발생 시 Python이 주문 실행을 보류하고 LLM 검증 호출을 수행한다. 예: 자산군 비중 15~55% 범위.
- **예외 검증:** 소프트 운영 밴드를 벗어나는 일반 주문에 대해서만 수행한다. 검증 호출은 주문 실행 전 이루어지며, YES / NO / REVISE 단답으로 처리한다.
- **실행 모드 예외:** 리밸런싱·긴급 트리거·MDD 킬스위치처럼 “먼저 줄이고 나중에 복구 판단”이 필요한 상황에서는 평상시 운용 하한/현금 밴드를 일시적으로 깰 수 있다. 이탈 상태는 DailySummary와 Debug.md에 기록하고 다음 정기 분석 또는 Allocator에서 복구 여부를 검토한다.

Python 레벨 하드 필터와 소프트 운영 밴드를 자산군 특성 및 실행 모드에 맞게 계층화한다.

| 계층 | 대상 | 규칙 | 비고 |
|---|---|---|---|
| **개별 종목 매수 한도** | 국내/미국 개별 주식 | 단일 종목의 **누적 매수 원금 기준 비중 최대 5%** | 현재 평가 비중이 아니라 매수 당시 투입한 원금 기준. 수익으로 평가 비중이 5%를 초과하는 것은 허용 |
| **자산군 총량** | 국장 총합 / 미장 총합 / 금 총합 | 각 자산군 비중 **15% ~ 55%** (운용 부분 100% 기준) | 소프트 운영 밴드. 운용 부분 = 전체 자산 - 현금. 이탈 시 Python이 주문 보류 후 예외 검증 호출 |
| **운용 부분 비중** | 주식 + 금 합산 | 프로덕션 목표 **70% ~ 90%** (전체 계좌 기준) | `NORMAL` 모드 하드 필터. `REBALANCING`·`EMERGENCY_TRIGGER`·`MDD_KILLSWITCH`에서는 일시 이탈 허용. 단, 페이퍼 트레이딩 관찰 모드에서는 봇의 자연 성향 확인을 위해 명시적 config로 하한을 50%까지 낮출 수 있음 |
| **주문 실행** | 전체 거래 | JSON 스키마, Date-ID, 비중, 유동성, 슬리피지, MDD 등 Python 검증 통과 시 실행 | 초기 MVP에서는 LLM 자기평가식 confidence를 주문 차단 하드필터로 사용하지 않음 |
| **현금 보유** | 전체 포트폴리오 | 평상시 **10% ~ 30%** 유지 | `NORMAL` 모드 목표 밴드. 리밸런싱·긴급 매도·MDD 청산 과정에서는 일시 이탈 가능하며, 이탈 사유와 복구 검토 상태를 기록 |
| **슬리피지** | 모든 주문 | 방향성 슬리피지 적용 (아래 참조) | 방향 무관 고정 범위 적용 시 폭락장 마비 위험 |
| **일일 매매 횟수** | 시장별 | 시장당 신규 진입 하루 최대 1회 | 과매매 방지, 스윙 트레이딩 원칙 유지 |

**페이퍼 트레이딩 관찰 모드:** 초기 MVP에서는 봇의 자연 현금 선호·위험 회피 성향을 관찰하기 위해 `paper_observation_invested_min_percent`를 별도 config로 둘 수 있다. 이 값은 프로덕션 목표 70% 하한을 바꾸는 것이 아니라, 페이퍼 단계에서만 하한을 완화하는 실험용 설정이다.


**Execution Mode 우선순위:** 주문 실행은 다음 5개 모드 중 하나로 분류한다. Boolean `rebalancing=True/False` 하나로 처리하면 긴급 트리거와 MDD가 같은 예외처럼 섞일 수 있으므로, 구현 단계에서는 enum으로 분리한다.

```python
class ExecutionMode(str, Enum):
    NORMAL = "normal"
    REBALANCING = "rebalancing"
    EMERGENCY_TRIGGER = "emergency_trigger"
    MDD_KILLSWITCH = "mdd_killswitch"
    MANUAL = "manual"
```

| 실행 모드 | 사용 상황 | 운용 하한 70% | 자산군 15~55% | 슬리피지 | 비고 |
|---|---|---:|---:|---:|---|
| `NORMAL` | 정기 분석 기반 일반 매매 | 적용 | 소프트 밴드/예외 검증 | 방향성 적용 | 평상시 기본 모드 |
| `REBALANCING` | Allocator 목표 비중 반영, 자산군 이동 | 일시 우회 가능 | 일시 우회 가능 | 방향성 적용 | 매수/매도 순서 때문에 현금·운용비중이 잠시 깨질 수 있음 |
| `EMERGENCY_TRIGGER` | STOCK_DROP / INDEX_CRASH / PORTFOLIO_LOSS / PROFIT_RUN 긴급 대응 | 우회 가능 | 우회 가능 | 방향성 적용 또는 설정값에 따라 완화 | 손상 포지션을 먼저 줄이고 다음 분석에서 복구 판단 |
| `MDD_KILLSWITCH` | 계좌 MDD -10/-15/-20% 룰베이스 청산 | 우회 | 우회 | 우회 | LLM 없이 Python 룰베이스로 실행 |
| `MANUAL` | 운영자 수동 개입 | 수동 승인 시 우회 | 수동 승인 시 우회 | 수동 승인 시 우회 가능 | 사유와 전후 상태 로그 필수 |

**우회 가능 범위:** 실행 모드 예외가 우회할 수 있는 것은 LLM 판단 통제용 필터(운용 하한, 자산군 소프트 밴드, LLM 검증 호출, 일부 슬리피지 필터)다. 라이브모드 게이트, 거래정지/주문 가능 여부, 브로커 API 안전장치, 계좌·주문 수량 무결성, 감사 로그 저장은 어떤 모드에서도 우회하지 않는다.

**개별 종목 5% 룰의 정확한 의미:**

단일 종목 5% 제한은 현재 평가 비중 기준이 아니라 **누적 매수 원금 기준**이다.

- 최초 매수 시 포트폴리오 대비 5%까지 매수 가능
- 최초 매수 비중이 3%였다면, 이후 최대 2%까지 추가 매수 가능
- 주가 상승으로 평가 비중이 10%, 15%, 20%까지 커지는 것은 룰 위반이 아님
- 추가 매수 가능 여부는 `initial_buy_weight` 또는 `cumulative_buy_weight`를 기준으로 Python이 판단
- 평가 비중이 커진 경우에는 PROFIT_RUN 룰로 별도 모니터링한다

예: A 종목을 최초 3% 비중으로 매수한 뒤 주가 상승으로 현재 평가 비중이 10%가 된 경우, 현재 평가 비중은 5%를 초과했지만 누적 매수 원금 기준은 3%이므로 룰 위반이 아니다. 따라서 추가 매수 가능 한도는 2%이며, 동시에 평가 비중 10% 도달로 PROFIT_RUN 1단계 모니터링은 작동한다.

- **섹터 분산:** 동일 섹터(상관계수 0.7 이상) 총합 비중 15% 초과 금지
- **유동성 필터:** 당일 거래대금 50억 미만 또는 호가 스프레드 0.5% 초과 시 진입 금지
- **MDD 킬스위치 (단계화):** 계좌 전체 고점 대비 하락률(MDD) **-10% / -15% / -20%** 단계 도달 시 룰베이스 즉시 청산. 단계별 목표 현금 비중 **50% / 80% / 95%** (가속 곡선). LLM 분석 우회(시간 손실 방지). 동일 단계는 하루 1회만 발동, 다른 단계는 같은 날에도 발동 가능. 단계 간 4시간 쿨다운 적용(10% 감소 후, 2시간 내로 -15%까지 찍어도 4시간까진 관망. -20% 찍으면 시간 상관없이 청산). -20% 도달 후 추가 하락 시 시스템 매매 일시 중단(수동 개입). MDD 청산 후 고점(historical_peak)은 그대로 유지(NAV 회계상 즉시 변경 없으므로 인위적 초기화 불필요). **임계값 적정성은 페이퍼 트레이딩 6개월 데이터 누적 후 정기 리뷰하여 조정 가능.**

**자산군 하한/상한 위반 시 처리 룰:**

분석 인스턴스의 매매 결정으로 자산군 비중이 소프트 운영 밴드 이탈(15% 미만 또는 55% 초과)을 일으킬 경우 다음 케이스별 처리한다. 매수와 매도는 위반 시점·체결 가역성이 다르므로 분리 처리한다.

| 발생 케이스 | 실행 모드 | 처리 방식 |
|---|---|---|
| 정기 분석 — 매도로 자산군 하한 위반 (15% 미만) | `NORMAL` | 즉시 검증 호출 → 응답 후 실행/철회 결정 (체결 전 차단 가능) |
| 정기 분석 — 매수로 자산군 상한 위반 (55% 초과) | `NORMAL` | 즉시 검증 호출 → 응답 후 실행/철회 결정 (체결 전 차단 가능) |
| 정기 분석 — 매도로 운용 부분 70% 하한 위반 | `NORMAL` | 매도 차단. 검증 호출도 하지 않음 |
| Allocator 목표 비중 반영 중 일시 이탈 | `REBALANCING` | 일시 허용. 이탈 사유·목표 복구 경로 기록 |
| 긴급 트리거(STOCK_DROP/INDEX_CRASH/PORTFOLIO_LOSS/PROFIT_RUN)로 위반 | `EMERGENCY_TRIGGER` | 검증 X, 트리거 결정 우선 실행. 손상 포지션 축소가 목적이므로 운용 하한 70%도 일시적으로 깰 수 있음 |
| MDD 킬스위치로 위반 | `MDD_KILLSWITCH` | LLM 분석·Date-ID·자산군 밴드·운용 하한·슬리피지 우회. 단, 거래정지/주문 가능 여부/브로커 안전장치/로그는 유지 |

**처리 흐름:**

1. 분석 인스턴스 출력 또는 Python 룰베이스 트리거 도착
2. Python이 `ExecutionMode`를 먼저 확정한다
3. `NORMAL` 모드이면 시뮬레이션으로 자산군/운용비중 위반 여부 판단
4. `NORMAL` 모드 소프트 밴드 위반 시 → 주문 실행 보류 + 검증 호출
5. 검증 응답 (YES/NO/REVISE)에 따라 분기:
   - YES → 주문 실행 (위반 상태 DailySummary 기록)
   - NO → 주문 철회
   - REVISE → 새 계획 검증 후 실행
6. `REBALANCING`·`EMERGENCY_TRIGGER`·`MDD_KILLSWITCH`는 해당 모드의 우회 정책을 적용하고, 이탈 상태와 복구 필요 여부를 로그에 남긴다
7. 위반 미발생 시 → 일반 흐름대로 주문 실행

**검증 호출 구조:**

검증 호출은 신규 호출로 시작하며 분석 결과의 핵심 정보를 user 메시지로 직접 작성한다. reasoning 원본은 첨부하지 않고 `summary_one_liner`만 사용 (메시지 파싱 원칙 적용).

```
[검증 시스템 프롬프트]
너는 자산 배분 룰 검증자다. 단답으로만 답하라.

[user 메시지]
다음 매매 결정의 사전 검증을 요청한다 (아직 체결 전):
- 종목: {symbol}
- 액션: {action} (BUY/SELL)
- 비중: {weight}%
- 결정 근거 1줄 요약: {summary_one_liner}

이 결정을 실행하면 자산군 룰 위반 발생:
- 자산군: {asset_class}
- 실행 후 예상 비중: {predicted_weight}%
- 룰 위반 내용: {violation_detail} (예: 미장 13%로 하한 15% 미달)

YES / NO / REVISE 중 단답:
- YES: 의도한 결정. 그대로 실행한다.
- NO: 실수. 주문 철회.
- REVISE: 종목/수량 재조정 (1줄 새 계획 제시).

긴 설명 금지.
```

**운용 부분 75% 미만 매수 검토 알림:**

운용 부분이 75% 미만(=현금 25% 초과) 감지 시, 다음 정기 분석 프롬프트 상단에 강제 주입:

```
[현금 비중 알림]
현재 운용 부분 비중 73% (현금 27%).
권장 현금 상한 25% 초과 중.
적극적 매수 후보를 검토하여 운용 부분 75% 이상 회복을 목표로 하라.
단, 무리한 매수는 금지 — 매수할 종목이 없으면 그대로 유지.
```

분석 인스턴스는 매번 신규 생성되므로 동일 알림 반복으로 인한 둔감화 우려 없음. 운용 부분 75% 회복 시 알림 자동 제거.


**긴급 매도 후 복구 리뷰:** `REBALANCING` 또는 `EMERGENCY_TRIGGER`로 운용 부분 70% 하한이 깨진 경우 이는 허용된 일시 상태다. 다만 상태를 방치하지 않기 위해 Python은 다음 필드를 포트폴리오 상태와 DailySummary에 기록한다.

```json
{
  "portfolio_state": {
    "invested_percent": 62,
    "below_invested_min": true,
    "below_min_reason": "EMERGENCY_TRIGGER",
    "requires_recovery_review": true,
    "recovery_review_after": "next_regular_analysis"
  }
}
```

다음 정기 분석 또는 Allocator 호출에는 다음 경고를 주입한다.

```text
[운용 하한 복구 알림]
긴급 트리거 또는 리밸런싱으로 운용 비중이 {invested_percent}%까지 하락했다.
이는 허용된 일시 상태이나, 신규 매수 후보를 검토해 운용 비중 70% 이상 복구 가능성을 판단하라.
단, 손상된 시장에서 무리한 재진입은 금지한다.
```

**방향성 슬리피지 설계:**

기존 방향 무관 슬리피지(국내 ±0.5%, 미국 ±0.2%)는 폭락장에서 치명적 모순을 발생시킨다. 트리거 발동 → LLM 10분 분석 → SELL 결정 → 그사이 주가 추가 하락 → 슬리피지 초과 → 손절 주문 취소라는 "주문 거부 루프"가 발생할 수 있다.

이를 방지하기 위해 매수/매도 방향에 따라 슬리피지 로직을 분리한다.

```python
def check_slippage(action, analysis_price, current_price, market,
                   emergency=False):
    # MDD 킬스위치 등 긴급 상황은 슬리피지 무시
    if emergency:
        return

    tolerance = 0.005 if market == "KR" else 0.002
    deviation = (current_price - analysis_price) / analysis_price

    if action == "BUY":
        # 매수: 가격이 올랐을 때만 차단 (비싸게 사는 것 방지)
        if deviation > tolerance:
            raise SlippageError("매수 가격 불리 — 주문 취소")

    elif action == "SELL":
        # 매도: 가격이 더 내렸으면 통과 (손절 지연이 더 위험)
        # 가격이 반등했을 때만 차단 (재분석 필요)
        if deviation > tolerance:
            raise SlippageError("매도 가격 반등 — 재분석 필요")
```

**MDD 청산 우선순위 — 단순 하이브리드 룰:**

킬스위치 발동 시 청산은 다음 3단계로 진행한다.

1. **생존 필터링:** 거래 정지 종목(주문 자체가 체결 불가) 및 한국 시장 하한가 묶인 종목(매수자 0이라 체결 불가)은 청산 대상에서 제외. 호가 스프레드 기준은 적용하지 않는다 — 폭락장에선 평소 0.05%였던 종목도 1~5%까지 벌어지는 것이 정상이며, 이를 기준으로 필터링하면 위기 상황에서 청산 대상의 절반 이상이 제외되어 청산 자체가 불가능해진다. MDD 킬스위치는 "정확한 가격"이 아니라 "확실한 탈출"이 목적이므로 스프레드 손실은 위기 비용으로 받아들인다.
2. **손실 종목 우선 전량 매도:** 매수가 대비 손실률(`pnl_vs_cost`) 큰 순서로 정렬하여 보유량 전량 시장가 매도. 목표 현금 비중 도달 시 즉시 종료. "매수 근거 자체가 틀린 종목 = 썩은 살" 우선 정리 원칙.
3. **수익 종목 비례 보충:** 손실 종목만으로 목표 미달 시, 수익 중인 종목들을 시가총액 비율로 균등 부분 매도. 한국 주식은 정수 단위 거래 강제이므로 `math.floor()` 처리. 누적 오차로 목표 비중에 1~3% 미달하더라도 무시하고 종료한다(MDD 상황에서 정밀도보다 신속성이 우선). 거래 정지 종목 다수로 인한 미달도 동일하게 처리하며, 부족분은 다음 단계(-15%, -20%) 발동 시 자연스럽게 추가 청산된다.

**주문 방식:** 시장가 주문 고정. 지정가 사용 시 폭락장에서 호가가 계속 밀려 내려가 "주문 고아(Orphaned Order)" 상태가 발생하기 때문. MDD 킬스위치는 방향성 슬리피지 필터도 우회한다(`emergency=True` 플래그).

**정렬 기준 — 매수가 대비 손실률 채택:** "보유 기간 고점 대비"는 패닉셀 직후 매수한 종목을 손실 종목으로 오분류하므로 부적합. "전체 시장 고점 대비"는 종목 매수 시점의 정보 기준과 어긋남. 매수가 대비가 매수 근거 유효성을 직접 평가하는 기준으로 가장 적합하다.

**자산군별 MDD 분리 금지 — 설계 결정 근거:**

자산군별 MDD를 별도로 두지 않는다. 이유는 금 편입의 전략적 의의(하방 완충재)와 직접 충돌하기 때문이다. 예: 미장 -12%, 국장 -2%, 금 +8% 상황에서 자산군 분리 룰은 미장만 강제 청산하지만, 계좌 전체 룰은 -3% 수준으로 방어되어 정상 작동한다. 자산군 분리 시 금이 일하고 있는 와중에 시스템이 패닉 매도하는 모순이 발생한다. 특정 시장 위기는 `INDEX_CRASH` 트리거(LLM 분석)가 담당하며, MDD 킬스위치는 "자산 배분 1차 방어선까지 뚫린 진짜 패닉장"에서만 발동되는 최후 보루다. 단, 자산군별 손익률은 정기 분석 프롬프트에 **참고 정보**로 주입하여 LLM이 자산 배분 조정 판단에 활용할 수 있도록 한다.

### 2.4 긴급 트리거 시스템

정기 분석 사이클 외에 가격 변동을 1분 단위로 폴링하여 긴급 분석을 호출한다. 트리거 종류는 다음 5종이다.

| 트리거 | 발동 조건 | LLM 분석 | 분석 대상 | 시간 윈도우 |
|---|---|---|---|---|
| `STOCK_DROP` | 보유 종목 당일 -3% | ✓ | 그 종목 + 동일 섹터 보유 종목 | 직전 N시간 |
| `INDEX_CRASH` | 시장 지수 -1.5% | ✓ | 해당 시장 보유 종목 전체 | 직전 N시간 (매크로 지표 우선) |
| `PORTFOLIO_LOSS` | 포트폴리오 당일 -2% | ✓ | 손실 기여도 Top 3 종목 | 직전 N시간 |
| `MDD_KILLSWITCH` | 계좌 고점 대비 -10/-15/-20% | ✗ (룰베이스) | — (LLM 우회) | — |
| `PROFIT_RUN` | 단일 종목 평가 비중 10/15/20% 도달 | 단계별 차등 | 그 종목 | 정기 분석 또는 긴급 |

**PROFIT_RUN 단계별 처리:**

| 비중 도달 | 처리 |
|---|---|
| 10% | 정기 분석 프롬프트에 모니터링 알림 추가(정기 분석에 추가적인 조사 및 분석 요청), 일반 평가 |
| 15% | 정기 분석에서 우선 평가(정기 분석에 추가적인 조사 및 분석 요청) + 매도 검토 적극 지시 |
| 20% | 긴급 분석 호출 (정기 시간 외에도 즉시) |

비중 자연 증가는 강제 5% 복귀시키지 않는다("Let Winners Run" 원칙). 5% 룰은 현재 평가 비중 제한이 아니라 누적 매수 원금 기준 추가매수 한도이며, 수익으로 인한 평가 비중 증가는 제한하지 않는다. 강제 복귀는 세금/거래비용 발생 + 이긴 종목에 페널티 부여 모순을 일으킨다. 대신 비중 단계별 모니터링 강화로 펀더멘털 변화를 추적한다.

**정기 분석 프롬프트의 보유 종목 컨텍스트 주입:**

정기 분석 시 LLM이 비중 큰 종목과 고점 대비 변동을 자연스럽게 인지하도록 다음 정보를 프롬프트에 주입한다.

**영향도 점수 공식 (분석 우선순위 정렬):**

```
Score = min(초기비중 × max(초기비중, 현재비중) / min(초기비중, 현재비중), 15)
```

- 초기비중: 매수 시점 시드 대비 비중 (단일 종목 누적 매수 원금 기준 5% 룰 적용 시 통상 5% 이하)
- 현재비중: 현재 포트폴리오 비중
- 상한 15: 가격 3배 변화(±200% 또는 -67%) 이상 종목은 모두 동점 1순위 (LLM이 작은 점수 종목을 무시하지 않도록 차이 제한)
- 손익 방향 무관 동등 가중 (수익/손실 모두 분석 우선) — 시스템 철학(하방 방어)과 정합

**프롬프트 표시 형식:**

```
[보유 종목 - 영향도 정렬]

| 종목 | 비중 | 초기비중 | 매수가 대비 | 영향도 |
|------|------|----------|-------------|--------|
| 삼성전자 | 9.8% | 5% | +95% | 9.80 |
| 한미반도체 | 3.0% | 5% | -40% | 8.33 |
| SK하이닉스 | 5.5% | 5% | +10% | 5.50 |
| AAPL | 4.5% | 5% | -10% | 5.56 |

상위 종목은 비중 변화가 큰 종목이므로 펀더멘털 변화 신호를 적극 탐색하라.
영향도 동점 시 변화 방향(수익/손실) 모두 동등하게 분석하라.

[보유 기간 가격 추이]
- 삼성전자: 매수 100원 → 보유 기간 고점 195원 → 현재 195원 (peak 대비 0%)
- 한미반도체: 매수 100원 → 보유 기간 고점 100원 → 현재 60원 (peak 대비 -40%)

[자산군별 현황 (260427 기준)]

| 자산군 | 비중 | 일일 | 벤치마크 | 알파 | 주간 | 월간 |
|--------|------|------|----------|------|------|------|
| 국장 | 39.0% | -2.3% | -2.5% (KOSPI) | +0.2%p | -1.5% | -3.0% |
| 미장 | 22.0% | +1.5% | +1.0% (S&P500) | +0.5%p | +2.0% | +5.0% |
| 금 | 19.0% | +0.8% | +0.7% (GC=F) | +0.1%p | +1.0% | +2.5% |
| 현금 | 20.0% | - | - | - | - | - |

벤치마크: 국장=KOSPI, 미장=S&P500, 금=GC=F (국제 금 선물)
```

**분석 시간대별 차등 표시:**

- 첫 분석 (09:00 또는 22:30): 풀 정보 (영향도 + 가격 추이 + 자산군 표 전체)
- 후속 분석 (11:00, 13:00 등): 영향도만 (자산군 표는 일일 정보만 간략)
- 마지막 분석 (14:00 또는 03:30): 풀 정보 (포스트모르템 준비)

보유 기간 고점 대비 정보는 **트리거화하지 않고 정기 분석 참고 자료로만 활용**한다. 트리거화 시 고점 부근 횡보에서 반복 발동되어 토큰을 낭비하는 함정이 있다. 고점 대비 매도 판단은 정기 분석에서 LLM이 종합적으로 수행한다.

**스로틀링 룰 (타겟 종목 기준):**

| 케이스 | 처리 |
|---|---|
| 같은 종목, 같은 트리거 타입 30분 내 재발동 | 무시 |
| 같은 종목, 개별→시장 트리거 중첩 | 분석은 하되 "이미 분석 중" 플래그 |
| 30분 내 시장 트리거(INDEX_CRASH/PORTFOLIO_LOSS) 발동 | 시장 트리거 우선권 (광범위 컨텍스트) |
| 다른 종목 | 무관하므로 긴급 분석 수행 |

**긴급 Scout 컨텍스트 강제 주입:**

긴급 트리거 발동 시 Scout 프롬프트 상단에 트리거 상황을 강제 주입한다.

```
[🚨 긴급 분석 컨텍스트]
- 발동 사유: {trigger_payload['reason']}
- 시간 윈도우: 최근 {trigger_payload['time_window_hours']}시간
- 분석 목표: 단순 노이즈 vs 펀더멘털 훼손 판별

[중요] 긴급 상황이라도 균형 쿼터는 유지하라.
하락 원인뿐 아니라 반등 가능성(매수자 진입, 기술적 지지선 등)도 평가하라.
공포에 휩쓸린 일방적 SELL 판단을 경계한다.
```

긴급이라고 부정 요인만 보면 패닉 매도로 이어지므로 Bear/Bull 균형 쿼터는 그대로 유지한다.

**TriggerPayload 표준 스키마 (트리거별 분리 구조):**

긴급 분석 호출 시 트리거 정보를 트리거별 데이터 구조로 전달한다. 공통 필드는 `BaseTriggerPayload`에 두고, 트리거별 metadata는 자식 클래스에서 추가하여 타입 안전성을 확보한다.

**공통 필드 (BaseTriggerPayload):**

- `trigger_id`: 발동 고유 식별자 (UUID). 분석 결과·주문 결과까지 추적되어 사후 분석 시 이 ID로 전 과정 연결 가능.
- `trigger_type`: 5종 트리거 중 하나 (Literal 타입)
- `target_symbols`: 필터링된 분석 대상 종목 리스트
- `time_window_hours`: 데이터 수집 시간 범위
- `bypass_llm`: True인 경우 LLM 분석을 호출하지 않고 Python 룰베이스 실행으로 전환한다. 현재는 `MDD_KILLSWITCH`만 True다. 이 값은 LLM 관련 방어 계층(LLM 분석, LLM 환각 검증, Date-ID 검증, 자산군 소프트밴드 검증, 일반 슬리피지 필터)을 우회한다는 의미이며, 거래정지/주문 가능 여부/브로커 API 안전장치/라이브모드 게이트/감사 로그 저장을 우회한다는 의미가 아니다. `is_emergency` 별도 플래그는 두지 않고, 실제 주문 경로의 예외 범위는 `ExecutionMode`와 트리거 타입으로 결정한다.
- `priority`: 스로틀링 충돌 시 우선순위 (MDD > PORTFOLIO_LOSS > INDEX_CRASH > STOCK_DROP > PROFIT_RUN)
- `detected_at`: 발동 시각
- `source`: 발동 출처 (`scheduler` / `realtime_monitor` / `manual`). 봇 내부 로직에는 영향 없으나 사후 디버깅·분석 시 발동 경로 추적에 사용.

**자식 클래스 구조:**

```
BaseTriggerPayload (공통 필드)
├── StockDropPayload (drop_rate, sector 등)
├── IndexCrashPayload (index_name, index_change 등)
├── PortfolioLossPayload (daily_pnl, top_contributors 등)
├── MddKillswitchPayload (mdd_level, target_cash_ratio 등)
└── ProfitRunPayload (symbol, current_weight, threshold_stage 등)
```

각 자식 클래스의 구체적 필드 정의는 구현 단계에서 확정한다.

페이로드 구조는 트리거별 Scout 조사 범위 결정, 프롬프트 컨텍스트 주입, 스로틀링 판정의 단일 입력으로 사용된다.

### 2.5 자산 배분 결정 (Allocator)

자산군 비중 결정(국장/미장/금 비율)은 분석 인스턴스 출력의 `macro_logic` 필드를 분리하여 **별도의 Allocator 인스턴스**가 담당한다. 분석 인스턴스의 fund_manager는 `stock_logic`(종목 매매)만 결정하며, Allocator가 정한 허용 범위(±5%) 내에서 동작한다.

**분리 근거:**

- **책임 분리:** 분석 인스턴스가 자산군 + 종목 동시 판단 시 어느 한쪽 어텐션 부족 위험. 실제 펀드 운용사 구조(CIO 자산 배분 → 매니저 종목 선택)와 정합.
- **결정 시점 차이:** 자산 배분은 매크로 시그널 기반 큰 그림 (일 1회 충분). 종목 매매는 시장 상황 반응 (일 4회 필요).
- **자기 합리화 차단:** 분석 인스턴스가 자산군 결정 + 종목 결정 동시 수행 시 둘 사이의 일관성 옹호 발생. 분리하면 각 인스턴스가 단일 책임에 집중.

**Allocator 호출 시점:**

화~금 08:30 KST (직전 미국 거래일 마감 후, 그날 09:00 국장 시작 전 + 그날 밤 미장 거래 전). 토요일은 다음 영업일이 월요일이라 Allocator를 호출하지 않는다. 월요일은 주간/월간 리뷰와 최근 포스트모르템 요약을 반영한 뒤 06:00에 Allocator를 호출한다.

Allocator 결과가 스키마 오류 또는 명백한 환각으로 폐기될 수 있으므로, 08:30 KST는 단일 호출 시각이 아니라 **최종 확정 목표 시각**으로 본다. 실제 구현에서는 다음처럼 여유 시간을 둔다.

```
07:50 KST  Allocator용 조사 인스턴스 실행
08:05 KST  Allocator 1차 호출
08:15 KST  Python 검증 및 폐기 여부 판단
08:30 KST  최종 권장 비중 확정
```

월요일은 주간/월간 리뷰와 최근 포스트모르템 요약을 먼저 반영해야 하므로 06:00 KST를 최종 확정 목표 시각으로 본다.

**Allocator 입력:**

- 어제 권장 현금 비중 (`previous_cash_target`) — Python이 주입
- 어제 권장 자산군 비중 (`previous_targets`) — Python이 주입
- 현재 실제 현금 비중 (`current_cash_weight`) — Python이 주입
- 현재 실제 자산군 비중 (`current_actual_weights`) — Python이 주입
- 양쪽 시장 DailySummary (국장 + 미장)
- 직전 주간/월간 리뷰 및 직전 기간 포스트모르템 요약
- 기존 국장/미장 Scout 요약 — 종목 판단 재수행이 아니라 시장 흐름 재사용 목적
- Allocator용 조사 인스턴스가 Date.md에 저장한 금/매크로 관련 근거
- 매크로 6종 지표 (VIX, DXY, US 10Y, GC=F, USD/KRW, V-KOSPI)
- 자산군별 손익 표 (다중 기간)
- 금 매매 횟수 (`gold_trade_count_this_month`) — Python이 카운팅하여 주입

**Allocator Scout 입력 원칙:**

Allocator는 국장/미장 종목 판단을 다시 수행하지 않는다. 국장·미장 시장 흐름은 기존 Scout 요약과 DailySummary를 재사용한다.

다만 자산 배분 판단에는 금, 환율, 변동성, 금리 등 공통 매크로 지표가 중요하므로, Allocator용 입력에는 금/매크로 지표를 별도로 포함한다. 즉 Allocator는 기존 시장 Scout 결과를 재사용하되, 금/매크로 지표만 별도 보강하는 **하이브리드 방식**으로 동작한다.

Allocator용 조사 인스턴스는 금과 매크로 판단에 필요한 사실을 Date.md에 `[YYMMDD-N]` 형태로 저장한다. Allocator는 이 근거 중 실제 판단에 사용한 항목을 각 자산군 `reasons[].date_id`로 반드시 인용한다.

**Allocator 판단 순서:**

Allocator는 하나의 프롬프트 안에서 다음 순서로 판단한다.

0. **Signal Summary**
   - 기존 국장/미장 Scout 요약, DailySummary, Allocator 전용 금/매크로 지표를 종합한다.
   - 이 단계에서는 비중을 결정하지 않고, 시장 체제를 `RISK_ON / NEUTRAL / RISK_OFF / CRISIS_WATCH`로 요약한다.
1. **Cash Manager**
   - 전체 계좌 기준 현금 목표 비중(`cash_target_percent`)을 결정한다.
   - 평상시 현금 범위는 10~30%다.
   - MDD 킬스위치 상황은 Python이 별도 처리하므로, Allocator가 임의로 대규모 청산을 지시하지 않는다.
2. **Asset Allocator**
   - 현금을 제외한 운용 자산 안에서 KR/US/Gold 목표 비중을 결정한다.
   - `target_weights`의 합계는 항상 100%여야 한다.
   - 금은 평상시 18~22%, 위기 시 최대 25%, 강세장 명확 시 최소 15%까지 허용한다.
3. **Consistency Checker**
   - `cash_policy`와 `target_weights`가 서로 모순 없는지 확인한다.
   - `target_weights` 합계가 100인지 확인한다.
   - 각 `action`과 `adjust_percent`의 부호가 일치하는지 확인한다.
   - 모든 판단 근거에 Date-ID가 존재하는지 확인한다.
4. **Final JSON**
   - 위 판단을 정해진 JSON 스키마로만 출력한다.

**Allocator 출력 JSON 스키마:**

Allocator LLM은 판단에 필요한 최소 필드만 출력한다. 날짜, 이전 비중, 현재 비중, delta, 검증 결과는 Python이 계산·저장한다.

```json
{
  "allocator_decision": {
    "summary_one_liner": "VIX와 원/달러 상승이 동시에 확인되어 금 비중 확대를 우선한다.",
    "reasoning": "긴 추론",

    "cash_policy": {
      "cash_target_percent": 20,
      "action": "MAINTAIN",
      "adjust_percent": 0,
      "reasons": [
        {
          "reason": "변동성은 확대됐지만 MDD 킬스위치 수준의 위험은 아니므로 현금 비중은 기존 수준을 유지한다.",
          "date_id": "260505-1"
        }
      ],
      "final_decision": "현금 비중은 전체 계좌 기준 20%로 유지한다."
    },

    "target_weights": {
      "kr": 47,
      "us": 30,
      "gold": 23
    },

    "asset_policies": {
      "kr": {
        "action": "DECREASE",
        "adjust_percent": -3,
        "reasons": [
          {
            "reason": "VIX 상승으로 위험 자산 선호가 약화되어 국장 비중을 줄인다.",
            "date_id": "260505-1"
          }
        ],
        "final_decision": "국장 비중을 3%p 축소한다."
      },
      "us": {
        "action": "MAINTAIN",
        "adjust_percent": 0,
        "reasons": [
          {
            "reason": "미장 변동성은 확대됐지만 기존 비중을 즉시 줄일 정도의 추가 악재는 부족하다.",
            "date_id": "260505-1"
          }
        ],
        "final_decision": "미장 비중은 유지한다."
      },
      "gold": {
        "action": "INCREASE",
        "adjust_percent": 3,
        "reasons": [
          {
            "reason": "VIX 상승으로 방어 자산 비중 확대 필요성이 커졌다.",
            "date_id": "260505-1"
          },
          {
            "reason": "원/달러 상승으로 원화 기준 금 ETF의 방어 효과가 강화될 수 있다.",
            "date_id": "260505-2"
          }
        ],
        "final_decision": "두 지표가 모두 금 비중 확대 쪽으로 작용하므로 금 비중을 3%p 확대한다."
      }
    },

    "reason_codes": [
      "MACRO_RISK_OFF",
      "VIX_SPIKE",
      "FX_KRW_WEAKNESS"
    ]
  }
}
```

**출력 필드 의미:**

- `cash_policy`: 전체 계좌 기준 현금 목표 비중과 판단 근거. 현금 비중은 자산군 `target_weights`와 분리한다.
- `target_weights`: 최종 목표 자산군 비중. 현금을 제외한 운용 부분 100% 기준이며, KR/US/Gold 합계는 항상 100%다.
- `asset_policies`: 각 자산군별 판단 근거. 국장/미장/금의 성격이 다르므로 모든 자산군에 대해 별도 판단을 남긴다.
- `reasons[].date_id`: Date.md에 존재하는 실제 근거 ID. 문장별 Date-ID를 붙여 디버깅 가능성을 높인다.
- `reason_codes`: 사후 통계와 reasoning-결정 일관성 검증을 위한 표준 코드.
- `reasoning`: 저장은 하되, 후속 검증 호출에는 원문 reasoning을 첨부하지 않는다. 검증에는 `summary_one_liner`와 구조화 필드만 사용한다.

**초기 reason_codes 코드셋:**

| 코드 | 의미 |
|---|---|
| `STEADY_STATE` | 특별한 조정 근거 없음, 기존 비중 유지 |
| `MACRO_RISK_ON` | 위험 자산 선호 강화 |
| `MACRO_RISK_OFF` | 위험 회피 심리 강화 |
| `VIX_SPIKE` | VIX 급등 또는 변동성 확대 |
| `RATE_CHANGE` | 금리 추세 변화 |
| `DXY_SHIFT` | 달러 인덱스 추세 변화 |
| `FX_KRW_WEAKNESS` | 원화 약세 / 원달러 상승 |
| `KR_INDEX_DROP` | 국장 지수 약세 |
| `US_INDEX_DROP` | 미장 지수 약세 |
| `GOLD_MOMENTUM` | 금 가격 또는 금 ETF 모멘텀 변화 |

**Python 생성/저장 필드:**

Python은 Allocator 출력에 다음 정보를 래핑하여 최종 `allocator_record`로 저장한다.

```json
{
  "allocator_record": {
    "schema_version": "1.0",
    "decision_date": "2026-05-05",
    "previous_cash_target": 20,
    "previous_targets": { "kr": 50, "us": 30, "gold": 20 },
    "current_cash_weight": 20,
    "current_actual_weights": { "kr": 48, "us": 32, "gold": 20 },
    "llm_decision": {},
    "python_derived": {
      "cash_delta_from_previous": 0,
      "target_deltas_from_previous": { "kr": -3, "us": 0, "gold": 3 },
      "cash_band_10_30_ok": true,
      "sum_to_100": true,
      "asset_band_15_55_ok": true,
      "gold_band_ok": true,
      "date_id_required_ok": true,
      "reason_code_allowed_ok": true,
      "requires_validation_call": false,
      "violated_rules": []
    }
  }
}
```

**Allocator 보수적 운영 원칙 (시스템 프롬프트):**

```
너는 자산 배분 결정자다.
어제까지의 권장 비중과 그 결과, 오늘의 매크로 시그널 변화를 받아 권장 비중을 결정한다.

[보수적 운영 원칙]
1. 기존 현금 비중과 기존 자산군 비중 유지를 기본값으로 한다.
2. 매크로 시그널이 명확히 변화한 경우에만 현금 또는 자산군 비중을 조정한다.
3. 현금 비중은 전체 계좌 기준 10~30% 범위에서 결정한다. MDD 킬스위치 상황은 Python이 별도 처리하므로 Allocator가 임의로 대규모 청산을 지시하지 않는다.
4. target_weights는 현금을 제외한 운용 자산 기준이며, 국장/미장/금 합계는 반드시 100%다.
5. 한 번에 ±3%p 이상 조정 금지.
6. 조정 시 명분을 명확히 인용해야 한다 (Date-ID 필수).
7. "최근 추세상" 같은 모호한 근거는 금지.
8. 각 자산군 비중은 원칙적으로 15~55% 소프트 운영 밴드 내에서 결정한다. 밴드 이탈 시 Python 사전 검증을 거친다.
9. 금은 평상시 18~22% 범위에서 운용한다. 명확한 약세장/위기 근거가 있으면 최대 25%까지 확대 가능하고, 명확한 강세장/위험 선호 근거가 있으면 최소 15%까지 축소 가능하다.
10. 금 15% 미만 또는 25% 초과는 허용하지 않는다.
```

**허용 범위 (tolerance):**

`tolerance`는 5% 고정. 분석 인스턴스는 Allocator 권장 비중 ±5% 범위 내에서 종목 매매를 결정한다.

예: Allocator가 국장 47% / 미장 30% / 금 23% 결정 시
- 국장 분석: 42~52% 범위 내 매매
- 미장 분석: 25~35% 범위 내 매매
- 금: Allocator 결정에 따라 국장 시간대에 실행 가능 여부를 Python이 판단

**금 비중 검증 룰:**

- 평상시: 18~22%
- 위기/약세장 근거 명확: 23~25% 허용
- 강세장/위험 선호 근거 명확: 15~17% 허용
- 15% 미만 또는 25% 초과: 명백한 룰 위반으로 Allocator 응답 전체 폐기
- `adjust_percent` 절대값이 3% 미만이면 Python 후처리에서 자동 MAINTAIN 처리
- 월/분기 금 매매 횟수 초과 시 Python이 금 거래를 차단하고 Debug.md에 기술 이벤트로 기록
- 단, 이 기록은 Postmortem용 투자 판단 오류 태그가 아니며 Top 3 집계에는 포함하지 않음

**Allocator 출력 실패 처리:**

Allocator 출력에서 명백한 환각 또는 스키마 위반이 감지되면, 해당 응답의 일부만 채택하지 않는다. 특히 금 목표 비중이 허용 범위(15~25%)를 벗어나는 경우, 이는 Allocator 판단 신뢰성이 깨진 것으로 보고 국장/미장 판단까지 포함해 전체 응답을 폐기한다.

폐기 조건 예시:

- JSON 파싱 실패
- `cash_policy.cash_target_percent`가 10~30% 범위를 벗어남
- `target_weights` 합계가 100이 아님
- 금 목표 비중이 15~25% 범위를 벗어남
- 각 자산군 `reasons[].date_id`가 없거나 Date.md에 존재하지 않음
- `action`, `adjust_percent`, `target_weights`가 서로 모순됨
- `reason_codes`가 비어 있거나 허용되지 않은 코드 사용
- 판단 문장과 Date-ID 근거 방향이 명백히 반대임

폐기된 경우 Python은 전일 Allocator 권장 비중(`previous_targets`)을 그대로 유지하며, 해당 사실을 DailySummary와 Debug.md에 `REJECTED_AND_FALLBACK` 기술 이벤트로 기록한다. 이 기록은 시스템 디버깅용이며 Postmortem용 투자 판단 오류 태그로 저장하지 않는다. temperature=0 설정에서는 동일 입력 재질의가 같은 오류를 반복할 가능성이 있으므로, 기본적으로 재질의하지 않는다.

**Allocator 자체 룰 위반 시 처리:**

- 형식 오류, Date-ID 누락, 금 15~25% 범위 위반처럼 신뢰성을 깨는 오류는 **검증 호출 없이 전체 폐기**한다.
- 국장/미장 자산군 소프트 운영 밴드(15~55%) 이탈처럼 의도적 예외 가능성이 있는 경우에만 권장 비중 적용 전 단답 검증 호출을 수행한다.
- 검증 호출에도 reasoning 원문은 첨부하지 않고, `summary_one_liner`, `target_weights`, `asset_policies`, `reason_codes`만 전달한다.

```
[검증]
권장 비중 출력: 국장 60% / 미장 25% / 금 15%
위반: 국장 자산군 60%로 상한 55% 초과
1줄 요약: {summary_one_liner}

YES (의도. 그대로 적용) / NO (수정 필요) / REVISE (재계산)
```

**범위 이탈 시 처리 (분석 인스턴스가 Allocator 권장 ±5% 이탈 시):**

```
[검증]
국장 분석 결과 비중 35%로 사전 권장 범위(42~52%) 이탈.
이탈 폭: -7%p
1줄 요약: {summary_one_liner}

YES / NO / REVISE
```

**현금·운용비중 일시 이탈 허용:**

자산군 비중 조정 과정에서 매수가 필요한 경우, 현금 비중이 평상시 하한 10% 미만으로 일시적으로 내려가는 것은 허용한다. 반대로 긴급 트리거나 MDD 청산으로 매도가 먼저 발생하면 운용 부분 70% 하한이 일시적으로 깨질 수 있다. 이 두 경우 모두 `NORMAL` 상태가 아니라 `REBALANCING` 또는 `EMERGENCY_TRIGGER`/`MDD_KILLSWITCH` 상태로 기록하며, 다음 정기 분석 또는 Allocator 호출에서 복구 여부를 판단한다. 운용 부분 70~90%와 현금 10~30%는 평상시 목표 밴드이지, 탈출 매도를 막는 절대 장벽이 아니다.

**MDD 킬스위치와의 관계:**

MDD 발동 시 Allocator 권장 비중은 무효. MDD 청산 후 운용 부분 비중이 권장에 못 미치는 상태가 되며, 다음 Allocator 호출 시 새로운 권장 비중을 결정한다.


### 2.6 브로커/API 및 계좌 구조

본 프로젝트에서 API는 **브로커 API**와 **분석 데이터 API**로 분리한다. 브로커 API는 주문·잔고·체결·실시간 시세를 담당하고, 분석 데이터 API는 공시·매크로·뉴스·과거 가격 데이터를 담당한다. 두 범주를 혼동하면 "KIS 하나면 전체 시스템이 충분하다"는 잘못된 결론이 나오므로 구현 단계에서 명확히 분리한다.

**브로커 API 채택 원칙:**

- 브로커 API는 **한국투자증권 KIS Developers 단일 채택**을 기본값으로 한다.
- KIS는 REST/WebSocket 기반이므로 macOS 개발 환경과 정합한다.
- 국내주식, 미국주식, 국내 상장 ETF(ACE KRX금현물 411060)를 같은 브로커 어댑터 계층에서 다룬다.
- 키움 OpenAPI+, LS xingAPI, CYBOSPlus처럼 Windows COM/OCX 기반 API는 본 프로젝트에서 제외한다.
- 다중 증권사 주문 연결은 MVP에서 금지한다. 증권사를 둘 이상 연결하면 OrderIntent 인터페이스, 자금 배분, 계좌 통합 NAV, MDD 계산이 모두 복잡해지므로 KIS 단일 브로커로 시작한다.
- **KIS 모의투자 adapter는 MVP 기본 경로에서 제외한다.** KIS 모의투자 adapter는 3개월 제한과 재발급 이슈가 있어 장기 페이퍼 원장으로 부적합하고, 현재 단계에서는 자체 PaperBroker와 KIS live read-only/극소액 수동 테스트만으로 충분하다.

**API 역할 분리:**

| 구분 | 소스 | 용도 | MVP 반영 |
|---|---|---|---:|
| 브로커/주문 | KIS Developers | 주문, 정정/취소, 체결조회, 잔고조회, 국내/해외 시세, ISA/일반계좌 API smoke test | 필수 |
| 미국 매크로 | FRED API | US 10Y(`DGS10`) 등 금·위험선호 판단용 매크로 | 필수 |
| 한국 공시 | DART/OpenDART | 사업보고서, 분기보고서, 주요 공시, 재무 데이터 | 필수 |
| 가격 보조 | yfinance | 지수, 미국주식, 금 선물, USD/KRW, 백필/검증용 보조 데이터 | 필수 보조 |
| 미국 공시 | edgartools / SEC | 10-K, 10-Q 등 미국 기업 공시 | 2차 |
| 한국 뉴스 | 네이버 뉴스 API 또는 RSS | B급 이상 매체 필터링 후 Scout 입력 | MVP 이후 |
| 미국 뉴스 | Finnhub News API 등 | 종목 뉴스, Reuters/Bloomberg/CNBC 등 선별 | MVP 이후 |
| KRX 공식 데이터 | KRX OPEN API 또는 KRX 직접 조회 | V-KOSPI, 거래소 공식 보강 데이터 | 필요 시 |

**초기 API 신청 우선순위:**

1. KIS Developers — 실전 계좌 API 키를 발급하되, 초기에는 `allow_live_trading=false`로 두고 read-only/smoke test 또는 극소액 수동 승인 전까지 주문 경로를 열지 않는다.
2. FRED API — 매크로 6종 중 US 10Y 및 기타 미국 지표 수집용.
3. DART OpenAPI — 한국 공시 Date-ID 근거 수집용.
4. 뉴스 API — 초기 MVP에서는 보류하고, Scout/Date-ID/JSON 검증이 안정화된 뒤 도입한다.

**KIS 계좌 역할:**

| 계좌 | 역할 | 자동매매 연결 |
|---|---|---:|
| 중개형 ISA | 국내주식, 국내 상장 ETF, ACE KRX금현물(411060) | KIS ISA smoke test 통과 후 사용 가능 |
| 일반 국내/해외주식 종합매매계좌 | 미국주식, ISA 한도 초과분, ISA API 문제 시 백업 | 미장 기본 계좌 |
| CMA | 프로젝트 외 대기 현금, 입출금 관리 | 직접 주문 계좌로 사용하지 않음 |

국내주식과 ACE KRX금현물 ETF를 ISA에서 운용하면 세제상 유리할 수 있으나, 시스템 회계는 복잡해진다. 따라서 ISA를 사용할 경우 Python은 모든 계좌를 통합 NAV 기준으로 계산해야 한다.

```text
Total NAV = ISA 평가금액 + 일반 종합매매계좌 평가금액 + 프로젝트 현금
```

MDD, 자산군 비중, 현금 비중, 운용 부분 비중은 개별 계좌가 아니라 **통합 NAV** 기준으로 계산한다. ISA 내부 국장 손실과 일반계좌 미장 수익이 따로 계산되면 MDD 킬스위치가 왜곡되므로, 모든 리스크 판단은 통합 포트폴리오 상태를 기준으로 한다.

**ISA API smoke test 필수:**

KIS Developers에서 ISA 계좌 선택 및 조회/주문이 가능하더라도, 실제로 본 시스템이 사용할 주문·잔고·체결조회 경로가 ISA에서 안정적으로 동작하는지는 실측해야 한다. Mac mini 환경 구축 후 다음 순서로 확인한다.

1. ISA 계좌번호를 KIS API 설정에 등록 가능 여부 확인
2. access token 발급
3. ISA 잔고 조회
4. 국내주식 현재가/호가 조회
5. 국내주식/ETF 주문 가능 여부 확인 — 실제 주문 전까지는 극소액 수동 승인 테스트만 허용
6. 체결조회/정정/취소 endpoint 동작 확인

ISA API smoke test가 실패하면 국내주식/금 ETF도 일반 종합매매계좌에서 운용하고, ISA는 자동매매 대상에서 제외한다.

**Secrets 관리 원칙:**

- App Key, App Secret, 계좌번호, HTS ID, 토큰은 GitHub에 커밋하지 않는다.
- `.env`, macOS Keychain, 1Password/Bitwarden 등 외부 비밀 저장소를 사용한다.
- committed config에는 환경변수 이름만 둔다.
- 실전 계좌 키는 read-only 검증 단계까지 보관만 하고, `allow_live_trading=true`와 확인 환경변수가 동시에 설정되기 전에는 주문 경로를 열지 않는다.
- API 키가 만료/재발급되더라도 paper ledger는 초기화하지 않는다.

### 2.7 Paper Trading 및 BrokerAdapter 구조

장기 모의투자는 증권사 모의투자 계좌가 아니라 **시스템 내부 PaperBrokerAdapter와 자체 paper ledger**를 기준으로 수행한다. KIS 모의투자 adapter는 기간 제한 및 계좌 재발급 가능성이 있으므로, MVP 기본 경로에서 제외한다.

**역할 분리:**

| 구성요소 | 역할 |
|---|---|
| `PaperBrokerAdapter` | 장기 페이퍼 트레이딩 본체. 내부 ledger 기준으로 주문·체결·현금·포지션·NAV·MDD를 기록 |
| `KisLiveBrokerAdapter` | KIS 실전 계좌 경로. 초기에는 read-only 검증만 허용하고, 주문은 live gate + 극소액 수동 승인 이후에만 허용 |

**표준 인터페이스:**

```python
class BrokerAdapter(Protocol):
    def get_cash(self) -> CashSnapshot: ...
    def get_positions(self) -> list[Position]: ...
    def place_order(self, order: OrderIntent) -> OrderResult: ...
    def cancel_order(self, order_id: str) -> CancelResult: ...
    def get_fills(self) -> list[Fill]: ...
```

LLM 분석 결과는 어떤 경우에도 broker 메서드를 직접 호출하지 않는다. 모든 주문은 반드시 다음 파이프라인을 통과한다.

```text
LLM Decision → ParsedDecision → ValidationResult → RiskApprovedDecision → OrderIntent → BrokerAdapter.place_order()
```

**Paper ledger 최소 구성:**

| 테이블/파일 | 내용 |
|---|---|
| `paper_orders` | OrderIntent, source decision, execution mode, 상태 |
| `paper_fills` | 가상 체결가, 수량, 수수료, 세금, 슬리피지 |
| `paper_positions` | 보유 수량, 평균단가, 평가가, 미실현손익, 비중 |
| `paper_cash_ledger` | 통화별 현금 증감, 주문 연결, 잔고 |
| `paper_nav_snapshots` | 총 NAV, 현금, 운용비중, 자산군 비중, MDD, 일간 수익률 |
| `paper_risk_events` | 리스크 필터 차단, MDD 이벤트, 운용 하한 이탈, 복구 리뷰 상태 |
| `paper_decisions` | LLM raw output, parsed decision, validation result, Date-ID 사용 목록 |

**초기 체결 정책:**

초기 MVP는 과도한 정교함보다 보수적이고 재현 가능한 체결을 우선한다.

```text
시장가 BUY  → 현재가 × (1 + 기본 슬리피지)
시장가 SELL → 현재가 × (1 - 기본 슬리피지)
```

초기 기본값:

| 시장 | BUY 체결가 | SELL 체결가 |
|---|---:|---:|
| 국내주식/ETF | 현재가 × 1.001 | 현재가 × 0.999 |
| 미국주식 | 현재가 × 1.0005 | 현재가 × 0.9995 |

이후 KIS read-only 호가 조회가 안정화되면 paper 체결 모델을 호가 기반으로 고도화한다.

```text
BUY  → 매도1호가 기준 체결
SELL → 매수1호가 기준 체결
```

그 다음 단계에서 거래대금·스프레드·부분체결 모델을 추가한다. 단, 초기 MVP에서 부분체결 모델을 과도하게 정교화하지 않는다.

**KIS 모의투자 제외 원칙:**

KIS 모의투자 adapter는 더 이상 MVP 기본 adapter가 아니다. 이유는 다음과 같다.

- 3개월 제한과 재신청/계좌 변경 이슈가 있어 장기 페이퍼 성과 원장으로 부적합하다.
- 자체 PaperBrokerAdapter가 이미 장기 모의투자 원장을 담당한다.
- KIS API 리허설의 대부분은 실전 계좌 read-only 조회와 mock HTTP contract test로 검증할 수 있다.
- 실제 주문 endpoint 검증은 실전 전환 직전 **극소액 수동 승인 tiny-live** 단계에서 수행한다.

따라서 MVP의 브로커 adapter는 `paper`와 `kis_live`만 둔다. KIS 모의투자 adapter가 다시 필요해지면 Phase 10 이후 별도 optional feature로 추가하며, 기본 설정·기획서·Cursor rules에는 포함하지 않는다.

**권장 전환 순서:**

```text
Phase A: 자체 PaperBrokerAdapter 3~6개월
Phase B: KIS live read-only 검증 — access token, 잔고조회, 현재가/호가, ISA 계좌 조회
Phase C: 극소액 manual tiny-live — LLM 자동 주문 금지, 수동 OrderIntent만 허용
Phase D: 제한적 자동 live 검토
```

API 키 만료·재발급·계좌 변경은 Debug.md에 기술 이벤트로 기록하되, 장기 paper ledger는 이어서 사용한다.

---

## 3. 운영 환경 및 스케줄

- **하드웨어:** Mac Mini Pro (64GB RAM). 2026년 5월 중후 배송으로 일정이 앞당겨졌으므로, 모델 성능·메모리·JSON 안정성은 추정값이 아니라 실제 Mac 도착 직후 벤치마크 통과 조건으로 검증한다.
- **LLM:** Qwen3.6-35B-A3B 단일 모델 (Ollama MLX 기반 실행, MoE 구조로 3B 활성, temperature=0)
- **Ollama keep_alive 설정:** 24시간 유지 (`keep_alive="24h"`). 분석 사이 간격(2~3시간)에 모델 언로드 시 재로딩 비용(30초~몇 분) 발생 → 24시간 유지로 응답 속도 확보. 매일 1회는 재로딩되어 메모리 누수 방지.
- **메모리 사용량 (Mac Mini Pro 64GB 기준):**
  - 모델 가중치 (UD-Q4_K_XL): 약 21~22GB (상시 점유, keep_alive 영향)
  - KV 캐시 (컨텍스트 30K 기준): 약 7~8GB (호출 시 점유, 종료 시 해제)
  - 시스템/추론 오버헤드: 약 2GB
  - **피크 시 약 32GB, 평시(idle) 약 22GB**
  - macOS 및 기타 프로세스: 약 10~15GB
  - 여유분: 약 17~22GB (피크 시)
  - 메모리 상주 자체의 전력 소모는 미미함 (idle 상태)
- **컨텍스트 한계 (실용적):** 모델 자체는 256K 컨텍스트 지원하나, KV 캐시가 컨텍스트에 비례 증가하므로 64K 초과 시 메모리 부담 급증(64K → 약 41GB, 128K → 약 57GB). 우리 시스템 평균 입력 30K 수준이라 안정 운영 가능하나, 기획안 외 대규모 컨텍스트 사용 시 메모리 모니터링 필요.

### 3.1 LLM/Ollama 버전 거버넌스

Ollama 런타임, 모델 가중치, quantization, Modelfile, prompt template, schema version은 모두 투자 판단 결과에 영향을 줄 수 있는 변경 요소로 간주한다. 모델 업데이트가 일반적으로는 버그 수정·성능 개선 목적이라도, 자동매매 시스템에서는 **출력 분포와 판단 정책을 변경할 수 있는 이벤트**로 취급한다.

**기본 원칙:**

- 초기 개발 단계에서는 최신 Ollama와 최신 모델을 사용할 수 있다.
- 단, 첫 기준 벤치마크를 통과한 순간 해당 조합을 `stable baseline`으로 고정한다.
- 페이퍼 트레이딩 또는 실전 운용 중에는 `latest` 태그를 직접 사용하지 않는다.
- 운용 모델은 명시적 모델명, digest, Modelfile, Ollama version, prompt template version, schema version을 `RunManifest`에 기록한다.
- Ollama 또는 모델 업데이트는 자동으로 운용 환경에 반영하지 않는다.
- 업데이트 버전은 `candidate` 환경에서 golden test, replay test, JSON schema test, latency/memory test를 통과한 경우에만 `stable`로 승격한다.
- 검증 전에는 장중, 긴급 트리거 감시 중, 실전 주문 가능 상태에서 Ollama 재시작·모델 pull·모델 교체를 수행하지 않는다.

**Stable / Candidate 분리:**

| 구분 | 역할 | 운용 가능 여부 |
|---|---|---|
| `stable_model` | 페이퍼/실전 판단 기준 모델 | 가능 |
| `candidate_model` | 업데이트 검증용 모델 | 불가 |

예시 설정:

```toml
[llm]
provider = "ollama"
stable_model = "qwen3.6-35b-a3b-trading-baseline-v1"
candidate_model = "qwen3.6-35b-a3b-trading-candidate-v2"
temperature = 0
seed = 42
num_ctx = 32768
format = "json"
```

**RunManifest 기록 항목:**

```json
{
  "run_manifest": {
    "run_id": "2026-05-20T09:00:00+09:00-KR-ANALYSIS-001",
    "mode": "paper",
    "execution_mode": "NORMAL",
    "ollama_version": "x.y.z",
    "model_name": "qwen3.6-35b-a3b-trading-baseline-v1",
    "model_digest": "sha256:...",
    "modelfile_hash": "sha256:...",
    "prompt_template_version": "analysis-bear-first-v1",
    "schema_version": "analysis-output-v1",
    "temperature": 0,
    "seed": 42,
    "num_ctx": 32768,
    "git_commit": "...",
    "created_at": "2026-05-20T09:00:00+09:00"
  }
}
```

`RunManifest`는 TodayThink, DailySummary, Debug 이벤트와 연결하여 저장한다. 사후 포스트모템에서 판단 오류가 발견될 경우, 전략 문제인지 모델/런타임 변경 문제인지 분리하기 위함이다.

**업데이트 승격 조건:**

| 테스트 | 최소 통과 기준 |
|---|---:|
| JSON 파싱 성공률 | 99% 이상 |
| Pydantic 검증 성공률 | 98~99% 이상 |
| Date-ID 누락률 | 0% |
| Allocator `target_weights` 합계 | 항상 100 |
| 금 비중 룰 위반 | 0% |
| 같은 입력 반복 안정성 | Action 변동률이 기존 baseline 대비 과도하지 않을 것 |
| Latency | 기존 baseline 대비 허용 범위 내 |
| RAM/swap | swap 폭증 또는 hang 없음 |
| Replay 결과 | 기존 baseline보다 명확히 악화되지 않을 것 |

업데이트 검증에서는 수익률만 보지 않는다. 새 모델이 과거 replay에서 우연히 높은 수익률을 내더라도 JSON 불안정, Date-ID 누락, 과도한 매매 증가, 금 비중 룰 위반, latency 악화가 있으면 승격하지 않는다.

**업데이트 허용 시점:**

| 단계 | 정책 |
|---|---|
| Mac 도착 직후 초기 세팅 | 최신 Ollama와 최신 모델 사용 가능 |
| 첫 benchmark 통과 후 | baseline 고정, 이후 변경은 모두 기록 |
| 페이퍼 트레이딩 중 | candidate에서만 업데이트 검증 |
| 실전 전환 후 | 주말 또는 장 마감 후, 백업 완료 후에만 검증 |
| 장중/긴급 감시/주문 가능 상태 | 업데이트·재시작·모델 pull 금지 |

**금지 패턴:**

- 운용 코드에서 `:latest` 태그 직접 사용
- 기준 benchmark 없이 Ollama 또는 모델 버전 교체
- 장중 Ollama 재시작
- candidate 모델을 stable 승격 없이 주문 판단에 사용
- 모델 업데이트 후 기존 Postmortem 통계와 혼합하여 성과 평가
- 모델 digest, Modelfile, prompt template version 없이 TodayThink/DailySummary 저장

**Fallback 원칙:**

업데이트 검증 실패 시 기존 `stable baseline`을 유지한다. 업데이트가 버그 수정 목적이라도, 자동매매 시스템에서는 검증 실패한 변경을 운용 환경에 반영하지 않는다. 단, 기존 stable에서 치명적 보안·런타임 장애가 발생한 경우에는 trading halt 상태로 전환한 뒤 수동 판단으로 대응한다.

- **분석 주기:** 국장 4회(월~금 09:00~14:00 KST) — 국장 종목 전용 분석, 미장 4회(미국 거래일 기준, NYSE/Nasdaq 정규장 09:30~16:00 ET를 한국 시간으로 변환한 구간 내) — 미장 종목 전용 분석. 미국 서머타임 여부에 따라 한국 시간 분석 구간은 유동적으로 계산하되, 스윙 트레이딩 원칙에 따라 하루 4회 분석은 유지한다. 한국 토/일 밤(=미국 토/일 휴장)에는 미장 분석 없음.
- **스케줄러 기준 분리:** 국장 분석, Allocator, 주간/월간 리뷰, 주간/월간 포스트모르템은 모두 국장 운영 흐름에 맞춰 **KST 고정 시간**으로 운영한다. 반면 미장 분석은 미국 거래소 캘린더와 서머타임을 반영해 한국 시간 기준 실행 시각을 유동 계산한다.
- **Allocator 호출:** 화~금 08:30 KST + 월요일 06:00 KST. 화~금은 직전 미국 거래일의 DailySummary 정리 이후, 그날 국장 09:00 시작 전. 월요일은 주간/월간 리뷰와 최근 포스트모르템 요약 반영 후. 토요일은 다음 영업일이 월요일이라 호출 안 함.
- **자산군별 분석 분리:** 국장 분석 사이클은 국장 종목만, 미장 분석 사이클은 미장 종목만 다룬다. 자산군 비중 결정은 Allocator 인스턴스 담당. 자산군별 손익률은 두 사이클 모두에서 참고 정보로 프롬프트에 주입.
- **장 마감 후 자동 작업:**
  - 평일(월~금) 15:40 KST: 국장 DailySummary 마감 기록
  - 미국 거래일 장 마감 후: 미장 DailySummary 마감 기록 (미국 거래소 캘린더 + 서머타임 반영)
  - 매주 금요일 국장 마감 후: 직전주 국장 WeeklyPostmortem 작성
  - 매주 토요일 미국장 마감 후: 직전주 미장 WeeklyPostmortem 작성
  - 매월 말 이후 첫 해당 시장 마감 후: 직전월 MonthlyPostmortem 작성 (국장/미장 분리)
  - 매주 월요일 05:00 KST: 주간 리뷰 + 월간 리뷰(롤링) 작성, 06:00 Allocator 호출
- **요일별 작업 정리:**

| 요일 | 새벽 (~08:30) | 장중 | 마감 후 |
|---|---|---|---|
| 월 | **05:00 주간 리뷰 + 05:30 월간 리뷰 + 06:00 Allocator** | 국장 분석 4회 (09:00~14:00) + 미장 분석 4회 (미국 거래소 캘린더 기준, 미국 월요일 거래분) | 15:40 국장 DailySummary 기록 |
| 화~목 | 직전 미국 거래일 미장 DailySummary 정리 + 08:30 Allocator | 국장 분석 4회 + 미장 분석 4회 | 15:40 국장 DailySummary 기록 |
| 금 | 직전 미국 거래일 미장 DailySummary 정리 + 08:30 Allocator | 국장 분석 4회 + 미장 분석 4회 | 15:40 국장 DailySummary 기록 + 직전주 국장 WeeklyPostmortem |
| 토 | 미국 금요일 마감 후 미장 DailySummary 기록 + 직전주 미장 WeeklyPostmortem (Allocator 없음) | 휴장 | 휴장 |
| 일 | 휴장 | 휴장 | 휴장 |

**운영 인스턴스 종류 (총 9종):**

| 인스턴스 | 호출 빈도 | 책임 |
|---|---|---|
| 국장 분석 | 평일(월~금) 4회 | 국장 종목 매매 결정 (Allocator 권장 ±5% 내) |
| 미장 분석 | 미국 거래일 기준 하루 4회 (NYSE/Nasdaq 정규장 09:30~16:00 ET를 한국 시간으로 변환한 구간 내) | 미장 종목 매매 결정 (Allocator 권장 ±5% 내) |
| Allocator | 화~금 08:30 KST + 월요일 06:00 KST (주간/월간 리뷰 및 최근 포스트모르템 요약 반영 후) | 현금 목표 비중 결정 + 자산군 비중 결정 (국장/미장/금) + 금 실행 방향 결정 |
| 주간 리뷰 | 주 1회 (월요일 05:00 KST) | 직전 1주 5영업일 압축 (시장 통합) |
| 월간 리뷰 (롤링) | 주 1회 (월요일 05:30 KST) | 직전 4주 슬라이딩 윈도우 압축 (시장 통합) |
| 국장 WeeklyPostmortem | 매주 금요일 국장 마감 후 | 직전주 국장 매매/Allocator 판단 오답노트 |
| 미장 WeeklyPostmortem | 매주 토요일 미국장 마감 후 | 직전주 미장 매매/Allocator 판단 오답노트 |
| 국장 MonthlyPostmortem | 매월 말 이후 첫 국장 마감 후 | 직전월 국장 판단 및 반복 오류 오답노트 |
| 미장 MonthlyPostmortem | 매월 말 이후 첫 미국장 마감 후 | 직전월 미장 판단 및 반복 오류 오답노트 |

**스케줄러 시간 산정 원칙:**

시간 기준은 작업 성격에 따라 분리한다.

- **KST 고정 작업:** Allocator, 주간 리뷰, 월간 리뷰, 국장 분석, 국장 Weekly/Monthly Postmortem. 이 작업들은 국장 시작 전/후 운영 흐름에 맞춰 설계되므로 한국 시간 기준으로 고정한다.
- **미국 거래소 캘린더 기반 작업:** 미장 분석, 미장 DailySummary 마감 기록, 미장 Weekly/Monthly Postmortem. 기준 시장은 NYSE/Nasdaq 정규장(09:30~16:00 ET)이며, 한국 시간 변환은 미국 서머타임 여부를 반영한다.
- **미장 분석 횟수:** 스윙 트레이딩 원칙에 따라 미국 거래일 하루 **4회로 고정**한다. 분석 시점은 장 시작 직후, 장중 2회, 마감 전 1회로 배치한다.
- **휴장/조기폐장:** 미국 휴장일과 조기폐장일은 거래소 캘린더 기준으로 자동 조정한다. 조기폐장일에도 가능한 범위에서 4회 분석을 유지하되, 세션 길이가 지나치게 짧으면 Python 스케줄러가 간격을 압축하거나 일부 슬롯을 생략할 수 있다.

각 인스턴스는 매 호출마다 신규 컨텍스트로 시작되며 (Ollama API stateless 특성), 인스턴스 간 정보 공유는 DailySummary와 Date-ID를 통해서만 이루어진다.

**장중 분석 흐름:**
```
[Python: 프롬프트 조립]
  - 현재 포트폴리오 ROI (증권사 API)
  - 시장별 Top 3 투자 판단 오류 태그 (Weekly/MonthlyPostmortem 집계)
  - 당일 조사 JSON
        ↓
[조사 인스턴스 (Qwen3.6-35B-A3B)] → 균형 쿼터 적용 JSON + Date-ID 매핑
        ↓
[Python: 환각 검증 (Date-ID 존재 여부 + 수치 오차 1% 이내 확인)]
        ↓
[분석 인스턴스 (Qwen3.6-35B-A3B)] → Bear First 4역할 순차 분석
  1. Bear: 하락 논거 [Date-ID 필수]
  2. Bull: 상승 논거 [인지적 단절 선언 후, Date-ID 필수]
  3. Risk Manager: 공격적/중립적/보수적 3관점 평가 (판단 없음)
  4. Fund Manager: 최종 Action 결정 (Allocator 권장 ±5% 내 종목 매매)
        ↓
[Python: 1차 수치 필터링]
  - JSON 규격 검증
  - 계좌 잔고 확인
  - 펌프앤덤프 패턴 감지
  - 방향성 슬리피지 검증
        ↓
[Python: 하드 필터 / 서킷 브레이커 (하드코딩, LLM 우회 불가)]
  - 단일 종목 누적 매수 원금 기준 비중: 포트폴리오의 5%
  - 일일 신규 진입 시장당 최대 1회
  - LLM 출력 스키마 및 Date-ID 검증 통과
  - 최소 현금 보유 비중 유지
        ↓
[증권사 API: 실제 주문 실행]
```

**장 마감 후 DailySummary 기록 흐름:**
```
[Python: 종가 데이터 및 매매 결과 정리]
        ↓
[Python: DailySummary 기록]
  - 당일 액션, 체결, 슬리피지, 자산군 비중, Range Violation 기록
  - 스윙 트레이딩 판단의 성패는 당일 확정하지 않음
        ↓
[Python: 운영 오류 발생 시 Debug.md 기록]
  - JSON 파싱 실패, Date-ID 누락, 룰 위반 출력, Allocator 폐기, 체결/API 오류, 금 매매 차단 등
```

**지연 포스트모르템 흐름:**
```
[Python: 평가 대상 기간 확정]
  - WeeklyPostmortem: 현재주가 아니라 직전주 평가
  - MonthlyPostmortem: 현재월이 아니라 직전월 평가
        ↓
[포스트모르템 인스턴스 (Qwen3.6-35B-A3B, 신규 인스턴스)]
  - DailySummary + TodayThink/체결 로그 + Date.md 근거 기반 평가
  - 국장/미장 분리 실행
  - 포스트모르템은 오답노트이며, 단기 노이즈가 아닌 반복 판단 오류를 기록
        ↓
[Python: Postmortem 태그 요약 파싱 및 저장]
  - Weekly/MonthlyPostmortem의 machine-readable tag summary만 Top 3 집계에 사용
  - Debug.md에는 Postmortem용 투자 판단 오류 태그를 저장하지 않음
```

> **포스트모르템 설계 근거:** 동일 모델이 자신의 오류를 진단하는 자기 합리화 위험이 있다. 이를 완화하기 위해 포스트모르템은 장중 분석 인스턴스와 **분리된 신규 인스턴스**로 실행하며, DailySummary(일일 운영 로그), TodayThink.md(분석 원본), Date.md(근거 원문), 체결 로그를 함께 입력하여 당시 맥락과 이후 결과를 동시에 보게 한다. 스윙 트레이딩 특성상 당일 또는 동일 주간의 가격 반응만으로 판단 성패를 확정하지 않고, 주간/월간 지연 평가를 통해 오답노트화한다.

**상시 모니터링 흐름:**
```
Python 상시 모니터링 (1분 단위 가격 폴링)
        ↓ (트리거 종류별 조건 충족 시)

[STOCK_DROP / INDEX_CRASH / PORTFOLIO_LOSS / PROFIT_RUN(20%)]
        ↓
TriggerPayload 생성 → Orchestrator 전달
        ↓
긴급 Scout (트리거별 데이터 범위 차등)
긴급 컨텍스트 강제 주입 + 균형 쿼터 유지
        ↓
긴급 분석 (Bear First 4역할, 정기와 동일 파이프라인)
(MoE 구조로 3B만 활성화되어 경량 모델에 준하는 응답 속도)
        ↓
하드 필터 / 서킷 브레이커 + 방향성 슬리피지 → 주문 실행

[MDD_KILLSWITCH (-10/-15/-20% 단계)]
        ↓
LLM 분석 우회 (시간 손실 방지)
        ↓
Python 룰베이스 즉시 청산
  1. 생존 필터링 (거래 정지/한국 하한가 종목 제외)
  2. 손실 종목 우선 전량 매도 (매수가 대비 손실 큰 순)
  3. 수익 종목 비례 보충 (목표 현금 비중 도달까지, 1~3% 오차 무시)
        ↓
시장가 주문 (슬리피지 필터 우회)
        ↓
DailySummary 및 MDD 이벤트 로그 기록 → 지연 포스트모르템에서 사후 평가
```

---


### 3.2 Mac mini 도착 후 초기 검증 절차

Mac mini가 도착했고 Ollama 설치가 진행 중이므로, 이제 문서상 성능 가정은 모두 실측으로 전환한다. 초기 1주는 자동매매 기능 확장보다 **로컬 LLM 입출력 안정성, JSON 파싱, config/schema, PaperBroker 골격**을 검증하는 데 집중한다.

**즉시 검증 순서:**

1. Ollama 설치 및 모델 pull
2. `ollama list`, 모델 digest, Ollama version 기록
3. 1K/5K/15K/30K 입력으로 JSON-only 출력 테스트
4. 같은 입력 20회 반복 후 JSON 파싱 성공률, Pydantic 검증 성공률, latency, RAM/swap 기록
5. `stable_model` 후보를 RunManifest에 기록
6. Cursor 프로젝트에 최종 rules 패키지 복사
7. `config.toml` paper mode로만 시작
8. PaperBrokerAdapter + 최소 ledger + golden JSON 1개 검증
9. KIS/FRED/DART 키는 환경변수로만 등록하고, 코드에는 절대 하드코딩하지 않음

**Week 1 완료 기준:**

| 항목 | 완료 기준 |
|---|---|
| Ollama | 로컬 모델 실행, digest 기록, temperature=0 호출 성공 |
| JSON | golden input 1개 이상에서 JSON 파싱 + Pydantic 검증 성공 |
| Config | paper mode typed config 로딩 성공, live gate는 기본 차단 |
| PaperBroker | 주문 의도 1건 → 가상 체결 → paper ledger 기록 성공 |
| Replay | 같은 입력/상태에서 같은 validation_result와 OrderIntent 재현 |

Week 1에는 Allocator 전체 구현, Scheduler 전체 구현, 긴급 트리거, KIS 실주문 연결을 목표로 삼지 않는다. 먼저 LLM 출력 안정성과 내부 paper ledger를 고정한다.


## 4. 메모리 및 오답노트 시스템

- **단기 메모리 (DailySummary):** `memory/daily/YYYY-MM-DD-{KR|US}.md` (시장별 분리, 영구 보존)
- **주간 리뷰 (WeeklyReview):** `memory/weekly/YYYY-W##.md` (시장 통합, 영구 보존, 매주 월요일 05:00 작성)
- **월간 리뷰 (MonthlyReview):** `memory/monthly/YYYY-W##-rolling.md` (시장 통합, 영구 보존, 매주 월요일 05:30 갱신, 롤링 4주 윈도우)
- **단기 분석 메모리 (TodayThink):** `memory/think/TodayThink-{KR|US}.md` (시장별 분리, 포스트모르템 대상 기간까지 보존 후 삭제 또는 압축)
- **디버그 로그 (Debug):** `memory/debug/Debug.md` (LLM 출력 실패·스키마 오류·체결/API 오류·금 매매 차단 등 기술/운영 이벤트 로그, 영구 보존)
- **주간 포스트모르템:** `memory/postmortem/weekly/YYYY-W##-{KR|US}-Postmortem.md` (시장별 분리, 영구 보존)
- **월간 포스트모르템:** `memory/postmortem/monthly/YYYY-MM-{KR|US}-Postmortem.md` (시장별 분리, 영구 보존)
- **장기 메모리:** `MEMORY.md` (불변 원칙, 시장 통합, 영구 보존)

### 4.1 파일별 역할 명세

| 파일 | 역할 | 작성 주체 | 보존 기간 | 다음 분석 첨부 여부 |
|---|---|---|---|---|
| DailySummary | 일일 운영 로그 — 매크로 시그널 + 액션 + 결과 | Python (자동) + Allocator/분석 인스턴스 출력 | 영구 | ✓ (시점별 차등) |
| WeeklyReview | 직전 1주 압축 (시장 통합) | Qwen (매주 월요일 05:00) | 영구 | ✓ (직전 1주) |
| MonthlyReview | 직전 4주 압축 (롤링 윈도우, 시장 통합) | Qwen (매주 월요일 05:30) | 영구 | ✓ (1개) |
| TodayThink | 분석 JSON 원본 (감사용) | 분석 인스턴스 | 포스트모르템 대상 기간까지 | ✗ |
| Debug.md | LLM 출력 실패·스키마 오류·체결/API 오류·금 매매 차단 등 기술/운영 이벤트 로그 | Python | 영구 | ✗ |
| WeeklyPostmortem | 직전주 매매/Allocator 판단 오답노트 | 포스트모르템 인스턴스 | 영구 | 간접 (요약 및 Top 3 태그) |
| MonthlyPostmortem | 직전월 판단 및 시스템 성향 오답노트 | 포스트모르템 인스턴스 | 영구 | 간접 (요약 및 Top 3 태그) |
| MEMORY.md | 시스템 불변 원칙 | 수동 갱신 | 영구 | 분석 시작 시 항상 주입 |

### 4.2 DailySummary 상세

DailySummary는 **일일 운영 로그**다. 당일 액션, 체결 결과, 자산군 비중, Range Violation, Allocator 폐기 여부, 단기 가격 반응을 기록한다. 별도의 DailyReview는 만들지 않는다.

DailySummary는 포스트모르템의 근거 자료로 사용되지만, 그 자체가 오답노트는 아니다. 스윙 트레이딩 판단의 성패를 당일 가격 반응만으로 확정하지 않는다.

**용도:**

1. **일일 운영 로그** — 당일 시장 상황, 액션, 체결, 결과 기록
2. **다음 날 분석 컨텍스트** — 다음 분석 인스턴스가 단기 흐름(이번 주 일별 + 지난주 목/금) 인지. 중장기 흐름은 별도 주간/월간 리뷰가 담당.
3. **Allocator 입력** — 매일 08:30 양쪽 시장 DailySummary를 받아 자산군 비중 결정
4. **포스트모르템 근거 자료** — Weekly/Monthly Postmortem이 지연 평가를 수행할 때 참조

**섹션 순서 원칙:** 매크로(큰 그림) → Allocator → 액션(개별 종목) → 결과(평가) 순으로 배치한다. LLM이 DailySummary를 읽을 때 자연스럽게 "시장 상황 → 자산 배분 → 종목 → 결과" 순서로 인지하게 되어 인간 트레이더의 일반적 분석 흐름과 정합한다.

**작성 주체별 섹션:**

| 섹션 | 누가 채우나 | 언제 |
|---|---|---|
| `## Market Context` | Python + 조사 인스턴스 | 장 시작 전/장중 |
| `## Allocator Decision` | Allocator 출력 + Python 검증 결과 | 08:30 KST 또는 월 06:00 KST |
| `## Actions` | Python (분석 인스턴스 출력 파싱) | 액션 발생 시마다 |
| `## Portfolio` | Python (증권사 API 종가 조회) | 장 마감 시 |
| `## Asset Allocation Result` | Python (자산 비중 계산) | 장 마감 시 |
| `## Range Violations` | Python (자산군/Allocator 권장 위반 감지) | 위반 발생 시 |
| `## System Events` | Python | Allocator 폐기, MDD, 스키마 오류 등 시스템 사건 발생 시 |

**핵심 원칙 — reasoning 원본 미포함:**

DailySummary는 `summary_one_liner` 1줄 요약 + 조사 근거 Date-ID로 압축한다. Bear/Bull/Risk Manager의 reasoning 원본은 포함하지 않는다. 이유:

- **자기 합리화 차단:** 어제 사고 과정 디테일을 보면 다음 분석 인스턴스가 "어제 결정 옹호" 압박을 받음. 1줄 요약은 외부 사실(어제 이런 일이 있었다)로 인식되어 비판적 평가 가능.
- **토큰 절약:** 일별 누적 + 주간/월간 리뷰까지 포함된 컨텍스트에 reasoning 원본까지 추가하면 토큰 폭증으로 처리 시간 ↑ + Lost in the Middle 위험.

**DailySummary 포맷 예시:**

```markdown
# DailySummary 260427-KR

## Market Context
- VIX 18.5(+2.1) [260427-12]
- DXY 104.2(-0.3) [260427-13]

## Allocator Decision
- 권장 비중: 국장 47% / 미장 30% / 금 23%
- 1줄 요약: VIX와 원/달러 상승이 동시에 확인되어 금 비중 확대를 우선
- reason_codes: MACRO_RISK_OFF, VIX_SPIKE, FX_KRW_WEAKNESS
- 금 판단 근거: VIX 상승 [260427-12], 원/달러 상승 [260427-14]
- Python 검증: PASS

## Actions
- 09:15 삼성전자 BUY 70주 @ 73,200
  - 분석 1줄: HBM 수요 증가 + 메모리 가격 회복으로 매수 적합
  - 조사 근거: [260427-3], [260427-7]
- 11:30 AAPL HOLD
  - 분석 1줄: 단기 변동성 높으나 펀더멘털 유효

## Portfolio
- 시작: 30,150,000
- 종료: 29,890,000
- ROI: -0.86%

## Asset Allocation Result
- 국장: 39% / 미장: 22% / 금: 19% / 현금: 20%
- 권장 대비: 국장 +2%p (범위 내), 미장 -2%p (범위 내), 금 -2%p (범위 내)

## System Events
- 없음
```

### 4.3 DecisionSnapshot 저장 원칙

페이퍼 트레이딩의 핵심은 재현성이다. 모든 LLM 판단과 주문 의도는 나중에 동일 입력으로 재검증할 수 있도록 `DecisionSnapshot` 형태로 저장한다.

필수 저장 항목:

```json
{
  "decision_id": "YYMMDD-MARKET-N",
  "timestamp": "ISO-8601",
  "market": "KR/US/GLOBAL",
  "input_snapshot_hash": "...",
  "date_ids_used": ["YYMMDD-N"],
  "llm_raw_output_path": "...",
  "parsed_decision": {},
  "validation_result": {},
  "risk_filter_result": {},
  "order_intent": {},
  "paper_fill": {},
  "portfolio_before": {},
  "portfolio_after": {}
}
```

주문 흐름은 반드시 `LLM Decision → ParsedDecision → ValidationResult → RiskApprovedDecision → OrderIntent → PaperBroker.fill()` 순서를 따른다. 분석 결과에서 broker API를 직접 호출하는 코드는 금지한다.

리플레이 테스트는 같은 Date.md, DailySummary, LLM raw output, portfolio snapshot을 입력했을 때 항상 같은 validation result와 order intent가 나오는지를 확인한다.

### 4.4 Debug.md 상세

Debug.md는 시장 분석의 오답노트가 아니라 **기술/운영 디버그 로그**다. LLM 출력 파싱 실패, 스키마 오류, Date-ID 누락, 명시 룰 위반성 환각 출력, 체결/API 오류, 금 매매 정책 차단처럼 코드·파서·실행 계층에서 확인해야 하는 사건을 기록한다. DailySummary는 일일 운영 로그이고, Postmortem은 투자 판단 오답노트다.

Debug.md에는 Postmortem용 투자 판단 오류 태그(`error_tags`)를 저장하지 않는다. 대신 별도 파일 `DEBUG_EVENT_CODES.md`에 정의된 기술/운영 이벤트 코드만 사용한다. 이 코드는 사람이 운영·구현 문제를 디버깅하기 위한 분류이며, LLM 조사·분석·Allocator·Postmortem 프롬프트에는 주입하지 않는다.

**저장 대상:**

- JSON 파싱 실패
- Date-ID 누락 또는 Date.md 미존재
- Allocator 출력 폐기 (`REJECTED_AND_FALLBACK`)
- Allocator가 출력한 현금 목표 비중 10~30% 범위 위반
- Allocator가 출력한 금 비중 15~25% 범위 위반
- Allocator `target_weights` 합계 100% 위반
- `action`, `adjust_percent`, `target_weights`의 구조적 모순
- 허용되지 않은 `reason_codes` 사용
- 증권사 API 오류, 주문 미체결/부분체결 처리 오류, paper broker 체결 시뮬레이션 오류
- 월/분기 금 매매 횟수 초과 또는 미세 조정 무시 룰로 인한 금 거래 차단
- 코드 예외, 파서 예외, 데이터 어댑터 오류처럼 구현/운영 계층에서 수정이 필요한 사건

**비저장 대상:**

- 시장 분석 자체의 오판
- 매수/매도 thesis의 사후 실패
- 거시 변수 해석 오류
- 뉴스·공시 해석 오류
- 자산배분 판단의 성과 부진

위 투자 판단 관련 문제는 Weekly/MonthlyPostmortem에 기록한다. Debug.md에는 Postmortem용 `error_tags`를 저장하지 않으며, Debug.md의 기술 분류는 Top 3 투자 판단 오류 태그 집계에 포함하지 않는다.

**Debug.md 예시:**

```markdown
# Debug Log

## 260505-KR-001
- source: Allocator
- severity: HIGH
- event_code: ALLOCATOR_GOLD_TARGET_OUT_OF_RANGE
- detail: gold target 30% 출력으로 허용 범위 15~25% 위반
- action_taken: REJECTED_AND_FALLBACK
- fallback: previous_targets 유지
- human_note: DEBUG_EVENT_CODES.md 기준의 기술/운영 이벤트이며 Postmortem error_tags가 아님

## 260506-ALLOC-002
- source: Allocator
- severity: MEDIUM
- event_code: DATE_ID_MISSING
- detail: asset_policies.gold.reasons[0].date_id가 비어 있음
- action_taken: REJECTED_AND_FALLBACK
- fallback: previous_targets 유지
```

Debug.md의 `event_code`는 사람이 보는 디버깅 분류다. Python은 이 코드를 Top 3 Error Tags 집계에 사용하지 않으며, LLM 프롬프트에도 주입하지 않는다.

### 4.5 포스트모르템 지연 평가 원칙

포스트모르템은 가장 자주 발생하는 문제를 바로 찍어주는 도구가 아니라, 먼저 **오답노트**를 만들고 그 오답노트에서 반복 패턴을 집계하는 구조다.

본 시스템은 스윙 트레이딩(수일~수주 보유)을 전제로 하므로, 당일 또는 동일 주간의 가격 반응만으로 매매 판단의 성패를 확정하지 않는다.

- `DailySummary`: 일일 운영 로그
- `Debug.md`: LLM 출력 실패, 스키마/룰 검증 실패, 체결/API 오류, 금 매매 차단 등 기술/운영 이벤트 로그
- `WeeklyPostmortem`: 현재주 장 마감 후, **직전주**의 매매 판단과 Allocator 판단을 평가
- `MonthlyPostmortem`: 현재월 종료 후, **직전월**의 주요 판단과 시스템 성향을 평가

포스트모르템은 국장과 미장을 분리한다. 국장과 미장은 거래 시간, 수급 구조, 거시 변수 민감도, 뉴스 반영 속도가 다르므로 같은 태그라도 학습 의미가 다르다.

**핵심 원칙:**

```text
DailySummary = 기록
Debug.md = 기술/운영 이벤트 인덱스 (Top 3 집계 제외)
WeeklyPostmortem = 직전주 오답노트
MonthlyPostmortem = 직전월 오답노트
Top 3 Error Tags = Weekly/MonthlyPostmortem에서 집계한 반복 투자 판단 오류
```

### 4.6 WeeklyPostmortem

WeeklyPostmortem은 현재주를 즉시 평가하지 않는다. 현재주 장 마감 후 작성하되, 평가 대상은 **직전주**다.

예:

```text
2026-W20: 매매 및 Allocator 결정 발생
2026-W21: 관찰 기간
2026-W21 장 마감 후: 2026-W20 WeeklyPostmortem 작성
```

**작성 시점:**

- 국장: 매주 금요일 국장 마감 후, 직전주 국장 판단 평가
- 미장: 매주 토요일 미국장 마감 후, 직전주 미장 판단 평가
- 파일명은 작성 시점이 아니라 평가 대상 주차 기준으로 저장한다.

```text
memory/postmortem/weekly/2026-W20-KR-Postmortem.md
memory/postmortem/weekly/2026-W20-US-Postmortem.md
```

**입력:**

- 평가 대상 주의 DailySummary
- 평가 대상 주의 TodayThink 원본 또는 압축본
- 평가 대상 이후 관찰 기간의 DailySummary
- Date.md 근거 원문
- 체결 로그 및 포트폴리오 변화
- 관련 기술/운영 이벤트(Debug.md) 및 MDD/주문 이벤트 로그

**평가 대상:**

- 직전주 BUY/SELL/HOLD 판단이 이후 관찰 기간에서 어떻게 작동했는가
- 매수/매도 근거 Date-ID가 며칠 뒤에도 유효했는가
- LLM reasoning과 실제 결정, `reason_codes`가 일치했는가
- Allocator의 국장/미장/금 비중 조정이 시장 흐름과 맞았는가
- 금 23~25% 확대 또는 15~17% 축소가 실제로 타당했는가
- 오류가 반복된다면 어떤 태그로 기록해야 하는가

### 4.7 MonthlyPostmortem

MonthlyPostmortem도 현재월을 즉시 평가하지 않는다. 현재월 종료 후 작성하되, 평가 대상은 **직전월**이다.

예:

```text
5월: 매매 및 Allocator 결정 발생
6월: 관찰 기간
6월 말 이후: 5월 MonthlyPostmortem 작성
```

**작성 시점:**

- 국장: 매월 말 이후 첫 국장 마감 후, 직전월 국장 판단 평가
- 미장: 매월 말 이후 첫 미국장 마감 후, 직전월 미장 판단 평가
- 파일명은 평가 대상 월 기준으로 저장한다.

```text
memory/postmortem/monthly/2026-05-KR-Postmortem.md
memory/postmortem/monthly/2026-05-US-Postmortem.md
```

**평가 대상:**

- 시스템이 특정 시장에서 과도하게 공격적/방어적이었는가
- 특정 reason_code가 반복적으로 틀렸는가
- 금 비중 운용이 포트폴리오 방어에 실제로 기여했는가
- 뉴스 기반 판단이 과신됐는가
- 국장/미장별 반복 에러 태그 Top 3는 무엇인가
- 다음 달 프롬프트 성격 또는 Python 룰을 조정할 필요가 있는가

### 4.7 다음 분석 인스턴스에 전달되는 컨텍스트

매 분석 사이클 시작 시 Python이 다음을 조립하여 프롬프트로 주입한다.

```
[프롬프트 상단 — Python 자동 주입]
- MEMORY.md (불변 원칙)
- 시장별 Top 3 투자 판단 오류 태그 (Weekly/MonthlyPostmortem 집계)
- 현재 포트폴리오 ROI
- 운용 부분 75% 미만 시 매수 검토 알림

[기술/운영 오류 컨텍스트]
- Debug.md와 DEBUG_EVENT_CODES.md는 사람 전용 디버깅 자료다.
- LLM 조사/분석/Allocator/Postmortem 프롬프트에 Debug.md 이벤트 코드나 기술 오류 요약을 주입하지 않는다.
- LLM에 주입하는 오류 신호는 Weekly/MonthlyPostmortem에서 확정된 투자 판단 오류 Top 3로 제한한다.

[중장기 컨텍스트 — 시점 무관]
- 롤링 월간 리뷰 (1개) — 직전 4주 추세
- 직전 주간 리뷰 (1개) — 지난 한 주 흐름
- 최근 Weekly/MonthlyPostmortem 요약 — 시장별 반복 오류

[단기 컨텍스트 — 요일별 차등]
- 월요일: 지난주 목/금 DailySummary 2일치 (주말 갭 보완)
- 화요일~금요일: 이번 주 영업일 일별 누적

[당일 조사 데이터]
- 조사 인스턴스 출력 JSON (Date-ID 부여된 사실)
- 매크로 6종 지표 당일 값
- 보유 종목 영향도 정렬 표 / 보유 기간 가격 추이 / 자산군 손익 표
```

기술/운영 이벤트와 투자 판단 오류 태그는 분리한다. 예를 들어 `DATE_ID_MISSING`은 Scout/Allocator/Python 검증 개선용으로 Debug.md에 기록하지만 Top 3 투자 판단 태그에는 포함하지 않고, LLM 프롬프트에도 주입하지 않는다. `#정보_과신`처럼 Weekly/MonthlyPostmortem에서 확정된 판단 오류만 다음 분석 인스턴스에 주입한다.

**시점별 첨부 일별 데이터:**

| 분석 시점 | 첨부 일별 | 토큰 추정 |
|---|---|---|
| 월요일 | 지난주 목/금 (2일) | 7,600 |
| 화요일 | 월요일 (1일) | 6,800 |
| 수요일 | 월/화 (2일) | 7,600 |
| 목요일 | 월/화/수 (3일) | 8,400 |
| 금요일 | 월~목 (4일) | 9,200 |

### 4.9 시장별 분리 설계 근거

국장과 미장은 거래 시간, 수급 구조, 거시 변수 민감도가 모두 다르므로 같은 태그(`#공시_해석_오류` 등)라도 학습 패턴이 다르다. Top 3 태그 카운팅 시 시장이 통합되면 한쪽 데이터가 다른 쪽 학습 신호를 묻어버리므로 분리 카운팅이 통계적으로 옳다.

MDD 킬스위치는 본질적으로 **자산 배분 1차 방어선이 뚫린 상황**이며, 그 지점까지 누적된 매수/홀드 결정의 책임은 국장과 미장 양쪽에 모두 있다. 따라서:

- 동일 MDD 사건을 **MDD 이벤트 로그와 DailySummary에 공통 사건 ID로 기록**한다.
- 사건 식별자 `[MDD-YYMMDD-N]`을 부여하여 양쪽 포스트모르템에서 추적 가능하도록 한다.
- 국장 포스트모르템은 국장 보유 종목의 MDD 기여도, 미장 포스트모르템은 미장 보유 종목의 MDD 기여도를 각자 분석한다.
- 포스트모르템 인스턴스에 "이 사건은 다른 시장 포스트모르템에서도 분석되고 있다"는 정보를 명시 주입하여 회피("이건 다른 시장 책임") 차단.

### 4.10 자동 에러 태그 시스템

Postmortem용 투자 판단 오류 태그와 Debug.md 기술 이벤트 분류를 분리한다.

- **Debug.md:** 코드 오류, 파싱 오류, LLM 출력 환각, 체결/API 오류, 금 매매 정책 차단 등 기술/운영 사건을 기록한다. Debug.md에는 Postmortem용 `error_tags`를 저장하지 않는다.
- **Postmortem 태그:** 투자 판단 오류와 반복되는 의사결정 문제 추적용. Weekly/MonthlyPostmortem에서 확정되며, 시장별 Top 3 Error Tags 집계 대상이다.

따라서 Python은 Weekly/MonthlyPostmortem의 machine-readable tag summary만 읽어 시장별 Top 3 투자 판단 오류 태그를 집계한다. Debug.md의 `event_code`는 `DEBUG_EVENT_CODES.md`에 정의된 사람 전용 디버깅 코드이며, 개발자/프롬프트/파서/체결 로직 개선용 분류로만 사용한다. 이 코드는 LLM 프롬프트에 전달하지 않는다.

**Debug.md 이벤트 코드:**

Debug.md의 기술/운영 이벤트 코드는 별도 파일 `DEBUG_EVENT_CODES.md`에서 관리한다. 이 코드는 사람이 운영·구현 문제를 디버깅하기 위한 참조표이며, LLM에게 전달하지 않는다.

기획서 본문에는 코드 체계만 요약한다. 세부 코드 목록은 `DEBUG_EVENT_CODES.md`를 기준으로 한다.

| 구분 | 예시 코드 | 정의 |
|---|---|---|
| LLM 출력 오류 | `LLM_JSON_PARSE_ERROR`, `LLM_SCHEMA_ERROR` | LLM 응답이 JSON 파싱 또는 Pydantic 스키마 검증에 실패 |
| Date-ID 오류 | `DATE_ID_MISSING`, `DATE_ID_NOT_FOUND` | 판단 근거 Date-ID 누락 또는 Date.md 미존재 |
| Allocator 오류 | `ALLOCATOR_GOLD_TARGET_OUT_OF_RANGE`, `ALLOCATOR_TARGET_SUM_INVALID` | Allocator 출력이 명시 룰 또는 구조 제약을 위반 |
| 주문/브로커 오류 | `BROKER_API_ERROR`, `ORDER_FILL_ERROR`, `PAPER_BROKER_SIM_ERROR` | 체결/API/paper broker 계층에서 수정이 필요한 사건 |
| 금 정책 이벤트 | `GOLD_TRADE_BLOCKED_MONTHLY_LIMIT`, `GOLD_MICRO_ADJUST_IGNORED` | 금 매매 빈도·미세 조정·비대칭 조정 정책으로 거래 차단 또는 무시 |
| 설정/런타임 오류 | `CONFIG_LOAD_ERROR`, `RUNTIME_EXCEPTION` | 설정 로딩, 환경변수, 코드 예외 등 런타임 문제 |

> Debug.md의 `event_code`는 Postmortem용 `error_tags`가 아니다. Top 3 Error Tags는 아래 Postmortem 투자 판단 태그에서만 집계한다.

**Postmortem 투자 판단 태그:**

| 구분 | 태그 | 정의 |
|---|---|---|
| 내부 판단 | `#추격_매수` | 과열 구간에서 이성적 근거 없이 진입 |
| 내부 판단 | `#손절_지연` | 정해진 손절 라인을 지키지 않고 보유 연장 |
| 내부 판단 | `#공시_해석_오류` | 공시 수치나 문구의 실질적 영향을 잘못 판단 |
| 내부 판단 | `#비중_조절_실패` | 리스크 대비 과도한 자금 투입 |
| 내부 판단 | `#정보_과신` | 특정 뉴스나 소스에 지나치게 편향된 가중치 부여 |
| 외부/해석 | `#지수_역행` | 종목 호재는 유효했으나 시장 전체 하락에 밀림 |
| 외부/해석 | `#재료_소멸` | 호재 발표 즉시 차익 실현 매물 쏟아짐 (Sell on news) |
| 외부/해석 | `#수급_착시` | 외국인/기관의 일시적 매수세를 대세 상승으로 오판 |
| 외부/해석 | `#거시_변수_발생` | 환율, 금리 등 예측 불가능한 매크로 악재 발생 |
| Allocator | `#금_비중_오판` | 금 확대/축소 판단이 시장 흐름 또는 방어 성과와 불일치 |
| Allocator | `#자산배분_오판` | 국장/미장/금 비중 조정이 전체 포트폴리오 성과와 불일치 |

> Top 3 Error Tags는 위 Postmortem 투자 판단 태그에서만 집계한다. Debug.md의 `event_code`는 사람 전용 디버깅 코드이며 LLM 프롬프트에 주입하지 않는다.

### 4.10 주간/월간 리뷰 시스템

DailySummary가 누적된 데이터를 압축하여 중장기 흐름을 인지할 수 있도록 주간/월간 리뷰를 자동 생성한다. 리뷰는 포스트모르템과 다르다. 리뷰는 흐름 압축 메모리이고, 포스트모르템은 오답노트다.

**리뷰 사용처:**

1. **정기 분석 인스턴스 (국장/미장):** 매 분석 호출 시 롤링 월간 리뷰(1개) + 직전 주간 리뷰(1개)가 컨텍스트로 첨부됨. 중장기 추세 인지 + 단기 흐름 인지 동시 확보.
2. **Allocator:** 매일 자산 배분 결정 시 양쪽 시장 DailySummary와 함께 직전 주간 리뷰 + 롤링 월간 리뷰 + 최근 포스트모르템 요약을 입력으로 받아 매크로 추세 변화와 반복 오류를 인지.
3. **포스트모르템:** 평가 대상 기간의 DailySummary와 리뷰를 참고해 판단 오류가 일회성인지 반복 패턴인지 구분.

**작성 시점 — 매주 월요일 새벽:**

```
05:00 주간 리뷰 작성 (Qwen, 지난주 5영업일 압축)
05:30 월간 리뷰 갱신 (Qwen, 직전 4주 주간 리뷰 압축)
06:00 Allocator 호출 (Qwen)
09:00 국장 분석 시작
```

월요일 휴장 시에도 새벽에 리뷰는 작성하고, 다음 영업일부터 사용한다.

**주간 리뷰:**

- 입력: 지난주 5영업일 DailySummary (양쪽 시장)
- 출력: 시장 통합 1개 파일 (`memory/weekly/YYYY-W##.md`)
- 내용: 한 주 매크로 추세 / 자산 배분 변화 / 주요 액션 패턴 / 자산군별 손익 / Debug 주요 이벤트
- 시장 통합 이유: 주간 단위는 큰 그림이라 자산군 간 상호작용 인지가 의미 있음 (반면 포스트모르템은 시장별 판단 오류를 다루므로 분리)

**월간 리뷰 (롤링 4주 윈도우):**

- 입력: 직전 4주 주간 리뷰 (W-4 ~ W-1)
- 출력: 시장 통합 1개 파일 (`memory/monthly/YYYY-W##-rolling.md`, ##은 그 주의 ISO 주차)
- 갱신 방식: 매주 새 파일 생성 (이전 월간 리뷰는 보존, 슬라이딩 윈도우 추적 가능)
- 내용: 4주 매크로 흐름 / 자산 배분 추세 변화 / 누적 수익률 / 반복 에러 패턴

**Qwen 사용 근거:**

- 시스템 단일 모델 통일 원칙과 정합 (분석/포스트모르템/Allocator 모두 Qwen)
- 외부 의존성(Gemini API) 제거 — 비용 0, 네트워크 장애 무관
- 응답 시간 무관 (자동 새벽 작업, 사람이 기다리는 작업 아님)

---

## 5. 기대 성과 및 리스크 관리

### 5.1 예상 성과 시나리오

| 시장 상황 | 시장 수익률 | 국내/미국 주식 전략 | 금 편입 효과 | 포트폴리오 전체 예상 |
|---|---|---|---|---|
| 강세장 | +15% | +10% (Beta 0.7 수준) | +5% (중립) | **+8~9%** |
| 중립장 | +5% | +3% | +2% | **+4~5%** |
| 약세장 | -15% | -5% (방어율 67%) | +8% (안전자산 선호) | **-1 ~ 0%** |

- **장기 연평균 기대 수익률:** **7~9%** (연 10% 근접 가능)
- **핵심 성공 요인:** 하락장에서 금 자산의 방어 기여 + 주식 부문 하드 필터/MDD 서킷 브레이커의 손실 제한

### 5.2 주요 리스크 및 대응 방안

| 리스크 요인 | 대응 방안 |
|---|---|
| LLM의 금융 도메인 추론 오류 | 최초 3개월간 LLM 판단 비중 30% 제한 및 페이퍼 트레이딩 병행 |
| 10분 지연으로 인한 타이밍 불일치 | 스윙 트레이딩(수일~수주)으로 목표 명확화, 단타 배제 |
| 폭락장 슬리피지 마비 | 방향성 슬리피지 + MDD 킬스위치 발동 시 슬리피지 우회 |
| 금 자산 API 연동 복잡성 | **ACE KRX금현물(411060)**을 KRX 주식 주문/체결 경로로 처리하여 별도 해외 금 ETF API 의존성 제거 |
| 다중 자산 환각 검증 누락 | 금 뉴스 소스(Kitco 등) 추가 및 매크로 지표(FRED) 크롤링 자동화 |
| Mac Mini 단일 장비 SPOF | 시스템 장애 시 핸드폰 긴급 알림 + 증권사 서버 측 기본 스톱로스 주문 이중화 |
| LLM 긍정 편향 | Bear First 순서 + 인지적 단절 프롬프트로 구조적 차단 |
| Scout 정보 누락 | 균형 쿼터 시스템으로 긍정/부정 균형 강제 |
| KIS 모의투자 3개월 제한 | KIS 모의투자 adapter를 MVP 기본 경로에서 제외. 장기 페이퍼 트레이딩은 `PaperBrokerAdapter` 자체 ledger를 기준으로 수행하고, KIS API 검증은 live read-only와 극소액 수동 tiny-live로 단계화 |
| KIS/데이터 API 키 만료·재발급 | SecretsManifest와 환경변수로만 관리, 만료 30/14/7일 전 알림, 키 교체는 Debug.md 기술 이벤트로 기록 |
| ISA API 지원 불확실성 | ISA 잔고/주문/체결조회 smoke test 통과 전까지 ISA 자동 주문 금지. 실패 시 일반 종합매매계좌로 fallback |
| 자체 PaperBroker 체결 과대평가 | 현재가에 보수적 슬리피지 적용, 이후 KIS 호가 기반 체결로 고도화, replay test로 재현성 검증 |

---

## 6. 변경 및 보완 이력

| 날짜 | 변경 내용 |
|---|---|
| 4/14 | OpenClaw 제거, 단일 인스턴스 다중 역할 구조 도입 |
| 4/18 | Qwen3.6-35B-A3B 모델 확정, 조사/분석 인스턴스 분리, 환각 검증 레이어 추가 |
| 4/18 | 자동 포스트모르템 태그 시스템 도입 (10개 표준 태그, 외부/내부 동시 선택 규칙) |
| 4/18 | 목표 재정의 (상대 성과 기반 + 하방 보호), 3자산 분리, 서킷 브레이커 계층화 |
| 4/18 | 분석 인스턴스 매번 재생성 + DailySummary 주입 방식 확정 |
| 4/21 | 단일 모델 통일 확정 — 경량 모델 제거, Qwen3.6-35B-A3B 단독 운용 |
| 4/21 | 분석 인스턴스 6역할 → 4역할 압축 (Bear / Bull / Risk Manager / Fund Manager) |
| 4/21 | Risk Manager: 공격적/중립적/보수적 3관점 평가만 수행, 비중 결정 배제 |
| 4/21 | Fund Manager: 최종 Action + 금 비중 결정 단독 담당 |
| 4/21 | 단타 제거 → 스윙 트레이딩(수일~수주)으로 목표 단일화 |
| 4/21 | 일일 매매 횟수 상한 추가 (시장당 신규 진입 1회) |
| 4/21 | Bear First 순서 도입 + 인지적 단절(Cognitive Break) 프롬프트 추가 |
| 4/21 | Scout 균형 쿼터 시스템 도입 (긍정/부정 균형 강제, 없으면 없다고 명시) |
| 4/21 | 방향성 슬리피지 설계 — 매수/매도 방향 분리, MDD 긴급 시 우회 |
| 4/21 | 포스트모르템 신규 인스턴스 분리 실행 명시 (자기 합리화 완화) |
| 4/21 | Date-ID 표기 전체 통일 (`[YYMMDD-N]` 형식) |
| 4/26 | MDD 킬스위치 단계화 확정 — -10%/50%, -15%/80%, -20%/95% 가속 곡선, 동일 단계 하루 1회 + 4시간 쿨다운 |
| 4/26 | MDD 킬스위치 LLM 우회 룰베이스 격리 확정 — 기존 architecture.md 모순(emergency=True LLM 호출) 정정 |
| 4/26 | MDD 청산 우선순위 룰 도입 — 단순 하이브리드 3단계 (생존 필터 → 손실 종목 우선 전량 → 수익 종목 비례 보충) |
| 4/26 | 청산 정렬 기준: 매수가 대비 손실률(`pnl_vs_cost`) 채택, 시장가 주문 고정, Peak 재설정 안 함 |
| 4/26 | 자산군별 MDD 분리 검토 후 기각 — 금 완충 효과 무력화 위험으로 계좌 전체 단일 룰 유지 |
| 4/26 | 긴급 트리거 시스템 5종 정의 (STOCK_DROP / INDEX_CRASH / PORTFOLIO_LOSS / MDD_KILLSWITCH / PROFIT_RUN 신규) |
| 4/26 | PROFIT_RUN 단계별 처리 — 10% 모니터링 알림, 15% 우선 평가, 20% 긴급 분석 호출 |
| 4/26 | 정기/긴급 분석 데이터 범위 분리 — 정기는 광범위, 긴급은 트리거별 차등 |
| 4/26 | 긴급 Scout 컨텍스트 강제 주입 도입 — 단, 균형 쿼터 유지로 패닉 매도 편향 방지 |
| 4/26 | 스로틀링 룰 정교화 — 타겟 종목 기준 + 개별→시장 트리거 우선권 + 30분 재발동 방지 |
| 4/26 | TriggerPayload 표준 스키마 도입 — 긴급 분석 호출 시 트리거 정보 단일 입력 |
| 4/26 | 자산군별 정기 분석 분리 — 국장은 국장만, 미장은 미장만, 금은 국장 사이클에서 결정 |
| 4/26 | 금 ETF 변경: GLD → ACE KRX금현물(411060) — 시총 1위 + 현물 기반 + 국장 시간대 거래 |
| 4/26 | 금 보수적 운영 룰 추가 — 평상시 18~22%, ±3% 미만 조정 자동 무시, 명분 강제 |
| 4/26 | 자산군별 손익률은 청산 트리거가 아닌 LLM 정기 분석 참고 정보로만 활용 |
| 4/26 | 메모리 시스템 시장별 분리 — DailySummary/TodayThink/Review 모두 `-{KR\|US}` 접미사로 분리 저장, Top 3 태그 카운팅도 시장별 분리 |
| 4/26 | MDD 킬스위치 사건은 양쪽 시장 Review에 동시 기록 — `[MDD-YYMMDD-N]` 사건 ID 부여, 각 시장은 자기 관점에서 책임 분석 |
| 4/27 | TriggerPayload 트리거별 데이터 구조 분리 확정 — `BaseTriggerPayload` + 5개 자식 클래스 구조, `metadata: dict` 단일 필드 폐기 |
| 4/27 | TriggerPayload에 `trigger_id`(UUID), `source`(scheduler/realtime_monitor/manual) 필드 추가 — 사후 분석·디버깅 용도 |
| 4/27 | `is_emergency` 별도 플래그 도입 검토 후 기각 — LLM 호출 우회 여부는 `bypass_llm`, 주문 검증 우회 범위는 `ExecutionMode`로 분리 운용 |
| 4/27 | 생존 필터 단순화 — 호가 스프레드 1% 기준 제거, 거래 정지 + 한국 하한가 종목만 제외 (위기 시 스프레드 기반 필터링은 청산 자체를 마비시킴) |
| 4/27 | 비례 매도 누적 오차(1~3%) 무시 룰 명시 — MDD 상황에서 정밀도보다 신속성 우선, 부족분은 다음 단계에서 자연 보충 |
| 4/27 | 보유 기간 고점 대비 트리거 도입 안 함 — 정기 분석 프롬프트에 보유 기간 가격 추이 정보로만 주입 (트리거화 시 고점 횡보에서 반복 발동 함정) |
| 4/27 | 정기 분석 프롬프트에 보유 종목 비중 현황 + 보유 기간 가격 추이 명시 주입 — LLM이 비중 큰 종목·고점 대비 변동을 자연스럽게 인지 |
| 4/27 | 금 비대칭 운영 룰 도입 — INCREASE 즉시 +5%까지, DECREASE 한 번에 -2%까지 (방어 자산은 빨리 늘리고 천천히 줄인다) |
| 4/27 | 금 매매 빈도 Python 카운팅 도입 — 월 평균 0~2회·분기당 4회 이하 목표, 정확한 수치를 프롬프트로 LLM에 주입 (LLM 직접 카운팅 시 환각 위험) |
| 4/27 | 자산군 비중 룰 재정의 — 하한 20%→15%, 상한 50%→55% 통일, 자산군 비율은 "운용 부분(전체-현금) 100%" 기준으로 계산. 현금 최소 10%는 별도 |
| 4/27 | 조사 인스턴스 데이터 소스에 뉴스 추가 — 공시 후행성 보완 (Sell on news 함정 회피). 매체 신뢰도 등급 시스템(A/B/C) 도입, C급 이하 및 소셜 미디어는 수집 대상 제외 |
| 4/27 | 뉴스 데이터 처리 룰 — Python 사전 필터링(종목명+7일+B급 이상) → 종목당 5개 → 3문장 요약 후 Scout 전달. Bull 편향 대응으로 Scout 부정 요인 재탐색 강제 + Bear 역할에 낙관 편향 비판 지시 |
| 4/28 | 메시지 파싱 원칙 도입 — Ollama API stateless 특성 활용, reasoning 원본 미첨부 + summary_one_liner 1줄 요약만 전달, 자기 합리화(Sycophancy) 차단 |
| 4/28 | summary_one_liner JSON 필드 추가 — 조사/분석/Allocator 모든 인스턴스 출력에 포함, DailySummary 기록·검증 호출 시 활용 |
| 4/28 | 금 매크로 지표 6종 확정 — 한국 금 시장 특화 (VIX, DXY, US 10Y, GC=F, USD/KRW, V-KOSPI), 매일 08:30 KST 수집, `[YYMMDD-N]` 통일 Date-ID 사용 |
| 4/28 | 자산군 손익률 표 형태 표시 확정 — 다중 기간(일일/주간/월간) + 벤치마크(KOSPI/S&P500/GC=F) + 알파 표시. 분석 시간대별 차등 (첫/마지막 풀, 후속 간략) |
| 4/28 | 영향도 점수 공식 확정 — `min(초기비중 × max(초기,현재) / min(초기,현재), 15)`. 손익 방향 동등 가중, 상한 15로 LLM 어텐션 분산 방지, 보유 종목에만 적용 |
| 4/28 | 운용 부분 비중 룰 신설 — 평상시 70~90% 유지, `NORMAL` 모드 70% 미만 매도 차단, 75% 미만 시 매수 검토 알림 주입, 자산군 조정 시 일시적 90% 초과 허용 |
| 4/28 | 자산군 하한/상한 위반 시 처리 룰 — 정기 분석은 단답 검증(YES/NO/REVISE), 트리거는 그대로 실행, MDD는 검증 우회, 운용 70% 매도 차단은 `NORMAL` 모드에 한정 |
| 5/17 | ExecutionMode 명시 — NORMAL / REBALANCING / EMERGENCY_TRIGGER / MDD_KILLSWITCH / MANUAL 모드로 운용 하한·자산군 밴드·슬리피지 우회 범위를 분리 |
| 5/17 | `bypass_llm=True` 의미 정정 — LLM 분석·환각 방어 계층 우회이며, 거래정지·브로커 안전장치·라이브모드 게이트·감사 로그는 우회하지 않음 |
| 5/17 | 긴급 트리거와 리밸런싱은 운용 부분 70% 하한을 일시적으로 깰 수 있도록 확정 — 다음 정기 분석/Allocator에서 복구 리뷰 수행 |
| 5/17 | LLM/Ollama 버전 거버넌스 도입 — stable/candidate 모델 분리, RunManifest 기록, `latest` 운용 금지, 업데이트 검증 후 승격 원칙 확정 |
| 5/19 | API 역할 분리 확정 — 브로커 API는 KIS Developers 단일 채택, 분석 데이터는 FRED/DART/yfinance/뉴스 API로 분리. 뉴스 API는 MVP 이후 단계적 도입 |
| 5/19 | 계좌 구조 확정 — 국내주식·ACE KRX금현물은 ISA 우선, 미국주식은 일반 종합매매계좌, CMA는 대기 현금. 리스크 계산은 계좌별이 아니라 통합 NAV 기준 |
| 5/19 | KIS 모의투자 adapter 제외 — 3개월 제한과 운영 복잡성 때문에 MVP 기본 경로에서 제거하고, 자체 PaperBroker + KIS live read-only/tiny-live 단계로 단순화 |
| 5/19 | 자체 PaperBrokerAdapter 도입 — 장기 모의투자는 내부 paper ledger 기준으로 수행, 브로커 adapter는 MVP에서 `PaperBrokerAdapter / KisLiveBrokerAdapter` 2계층으로 단순화 |
| 5/19 | Mac mini 도착 후 초기 검증 절차 반영 — Ollama 설치 후 모델 digest, JSON 안정성, Pydantic 검증률, latency, RAM/swap을 RunManifest 기준으로 기록 |
| 4/28 | Allocator 인스턴스 신설 — 자산군 비중 결정을 분석 인스턴스 fund_manager에서 분리. 매일 08:30 KST 호출, 보수적 운영 원칙(±3%p 이상 조정 금지), tolerance ±5% 허용 |
| 4/28 | 분석 인스턴스 fund_manager에서 macro_logic 제거 — 자산군 비중 결정은 Allocator 전담, 분석 인스턴스는 Allocator 권장 ±5% 내 종목 매매만 결정 |
| 4/28 | DailySummary 포맷 변경 — 액션 로그 + summary_one_liner + 조사 근거 Date-ID + Allocator 결정 + 자산 배분 결과. reasoning 원본 미포함 (자기 합리화 차단 + 토큰 절약) |
| 4/28 | 메모리 시스템 4장 통합 정리 — 파일별 역할/작성 주체/보존 기간 표 추가, DailySummary 섹션별 작성 주체 명세, 다음 분석 컨텍스트 조립 명세 |
| 4/28 | 주간/월간 리뷰 시스템 신설 — 매주 월요일 05:00 주간 리뷰 + 05:30 월간 리뷰 갱신, Qwen 사용 (Gemini API 대체), 롤링 4주 윈도우로 월말 정보 공백 해결 |
| 4/28 | DailySummary 보존 영구로 변경 — 다음 분석 첨부는 시점별 차등 (월요일은 지난주 목/금, 평일은 이번 주 누적). 모든 시점 토큰 6,800~9,200 범위로 안정화 |
| 4/28 | 메모리 디렉토리 구조 정리 — `memory/daily/`, `memory/weekly/`, `memory/monthly/`, `memory/think/`, `memory/review/` 분리 저장 |
| 4/28 | Ollama keep_alive 24시간 명시 — 분석 사이 모델 언로드/재로딩 비용 회피, idle 상태 전력 소모 미미, 매일 1회 재로딩으로 메모리 누수 방지 |
| 4/28 | 메모리 사용량 명세 추가 — 모델 가중치 22GB(상시) + KV 캐시 8GB(30K 컨텍스트) + 오버헤드 2GB = 피크 32GB, 64GB 시스템에서 안정 운영. 컨텍스트 64K 초과 시 메모리 부담 급증 인지 필요 |
| 4/28 | MDD 킬스위치 임계값 정기 리뷰 명시 — 페이퍼 트레이딩 6개월 누적 후 조정 가능 |

| 5/05 | Allocator 출력 스키마 확장 — `target_weights` + `asset_policies` + 문장별 `date_id` + `reason_codes` 구조로 변경. Python은 previous/current/delta/검증 결과를 별도 래핑 저장 |
| 5/05 | 금 비중 룰 명확화 — 평상시 18~22%, 위기/약세장 최대 25%, 강세장 최소 15%. 15% 미만/25% 초과 출력은 Allocator 전체 폐기 후 previous_targets 유지 |
| 5/05 | DailyReview 신설 취소 — DailySummary는 일일 운영 로그로 유지, Debug.md는 LLM 출력 실패·체결/API 오류·금 매매 차단 등 기술/운영 이벤트만 저장 |
| 5/05 | 포스트모르템 지연 평가 도입 — Weekly/Monthly Postmortem을 국장/미장 분리, 현재 기간이 아닌 직전주/직전월 오답노트로 작성 |
| 5/05 | Debug.md 이벤트 코드 카탈로그 분리 — `DEBUG_EVENT_CODES.md`를 사람 전용 디버깅 참조표로 신설. Debug event code는 LLM 프롬프트에 주입하지 않고 Top 3 투자 판단 태그 집계에서도 제외 |

---

## To-do (미확정 사항)

다음 항목은 1차 구현 진행하면서 별도로 결정한다.

> 아래 표의 “Gemini 권장사항”은 운영 모델로 Gemini를 사용한다는 의미가 아니라, 멀티에이전트 기획서 작성 과정에서 Gemini가 제안한 구현 보강 항목을 뜻한다.

| 항목 | 설명 | 현재 상태 |
|---|---|---|
| TriggerPayload 자식 클래스 필드 정의 | `StockDropPayload`, `IndexCrashPayload`, `PortfolioLossPayload`, `MddKillswitchPayload`, `ProfitRunPayload` 각각의 구체적 필드 확정. pydantic 기반 구현 권장 | 핵심 필드(symbol/drop_rate, top_contributors, mdd_level 등) 합의됨, 코드 구현 단계에서 최종 확정 |
| reason_codes 및 Debug 이벤트 코드 관리 | Allocator/분석/포스트모르템 reason_codes와 Debug.md 사람 전용 이벤트 코드를 페이퍼 트레이딩 결과에 맞춰 확장·정리 | 초기 10개 reason_codes 및 `DEBUG_EVENT_CODES.md` 기준 Debug 이벤트 코드 체계 합의, 페이퍼 트레이딩 후 조정 |
| MDD 킬스위치 임계값 적정성 | -10/-15/-20% 임계값과 50/80/95% 청산 비율의 실전 적정성 | 페이퍼 트레이딩 6개월 후 정기 리뷰 |
| V-KOSPI 데이터 소스 | yfinance 제공 여부 미확인, KRX 직접 조회 또는 별도 크롤링 필요 가능성 | 구현 단계에서 데이터 소스 검증 |
| 영향도 점수 임계값 | "우선 분석" 영역으로 분리할 점수 임계값 (현재 시안: 단순 정렬, 별도 임계값 없음) | 페이퍼 트레이딩으로 적정성 검증 |
| 비동기 데이터 수집 파이프라인 | yfinance/OpenDartReader/edgartools 등 동기 라이브러리를 asyncio.to_thread로 감싸서 동시 호출 | Gemini 권장사항, 구현 시 적용 |
| API Rate Limit 대응 | tenacity 라이브러리 + Exponential Backoff | Gemini 권장사항, 구현 시 적용 |
| 토큰 카운팅 라이브러리 | tiktoken 대신 Qwen 토크나이저 사용 (`AutoTokenizer.from_pretrained`), 프롬프트 길이 사전 검증 | 구현 시 적용 |
| LLM JSON 파싱 실패 처리 | pydantic 검증 + 일반 분석은 제한적 재시도, Allocator는 명백한 오류 시 재질의 없이 previous_targets fallback | 구현 시 표준화 |
| 동시성/레이스 컨디션 | 정기 분석 진행 중 긴급 트리거 발동 시 큐잉 처리, DailySummary 동시 쓰기 파일 락 | 구현 시 검증 |
| 긴급 분석 LLM 호출 실패 fallback | Ollama 응답 실패 시 N회 재시도, 최종 실패 시 보수적 룰베이스 처리(현금 늘리기 등) | 구현 시 표준화 |

| 5/05 v5_fix | README 기준 문서명 고정, confidence 하드필터 MVP 제외, 페이퍼 관찰 모드 투자 하한 완화 옵션, Date-ID stale 검증, DecisionSnapshot/OrderIntent/replay 원칙 추가 |

---

## 7. 구현 로드맵

> **Phase numbering policy**
>
> 본 프로젝트의 Phase 번호는 실제 구현 이력 기준으로 고정한다.
>
> 기획안은 상위 로드맵을 제공하고, 세부 구현 규칙은 `CODING_RULES.md` 및 `.cursor/rules/*.mdc`를 따른다.

### Phase 0 — Runtime Config / Live Gate [DONE]

런타임 설정과 live trading 안전 게이트를 구현한다.

완료 범위:

- `config.toml` 기반 typed settings loader
- `paper | live` trading mode
- `paper | kis_live` broker adapter
- config 부재 시 fail-closed
- live mode fail-closed gate
- `allow_live_trading`, confirmation phrase, live credentials 검증
- `kis_mock` / `KIS_MOCK_*` 제외

### Phase 1 — Ollama JSON Harness / RunManifest [DONE]

로컬 Ollama 기반 JSON 출력 안정성 측정 및 검증 harness를 구현한다.

완료 범위:

- Ollama HTTP client
- JSON-only runner
- Pydantic validation runner
- parse / validation / API / client error 구분
- markdown/code-fence output sanitizer 없이 실패
- `temperature=0`
- `think` payload 명시
- RunManifest
- smoke bench script

### Phase 2 — Core Domain Models [DONE]

자동매매 시스템의 핵심 domain model을 구현한다.

완료 범위:

- Money
- MarketPrice
- OrderIntent
- OrderResult
- Fill
- Position
- CashSnapshot
- PortfolioSnapshot
- NavSnapshot
- Decimal finite validation
- timezone-aware datetime validation
- blank string reject
- OrderIntent sizing ambiguity 차단

### Phase 3 — PaperBrokerAdapter / SQLiteLedger / Paper Ledger [DONE]

KIS 모의투자에 의존하지 않는 자체 장기 paper trading ledger를 구현한다.

완료 범위:

- BrokerAdapter Protocol
- PaperBrokerAdapter
- SQLiteLedger
- order intent/result/fill persistence
- current cash / current positions
- append-only `paper_cash_ledger`
- duplicate `order_id` 중복 체결 방지
- rejected/pending order side effect 방지
- LIMIT fill policy
  - MARKET과 LIMIT 모두 `fill_price = market_price.price`
  - LIMIT은 PENDING 게이팅만 수행
- symbol/market/currency mismatch reject
- slippage `Money.zero(currency)` 기록
- cash mutation public path는 `apply_cash_change()`로 제한
- 현재 테스트 baseline: `125 passed`

### Phase 4 — Decision Schema / DecisionSnapshot / Replay Foundation [DONE]

LLM 판단 결과를 replay 가능한 구조로 저장하기 위한 schema와 snapshot 기반을 만든다.

목표:

- Percent
- DateId
- DecisionId
- EvidenceRef 또는 SourceRef
- ValidationResult
- DecisionSnapshot
- DecisionSnapshot persistence
- deterministic replay foundation
- same input → same normalized payload
- same input → same validation_result

이 Phase에서는 외부 API, Allocator 판단 로직, Analysis 로직, RiskFilter, OrderIntent 생성은 구현하지 않는다.

### Phase 5 — Date-ID Store / Evidence Source Layer [DONE]

LLM 판단 근거를 Date-ID로 추적하고 stale 여부를 검증할 수 있는 근거 저장 계층을 만든다.

목표:

- Date-ID record model
- Date-ID storage
- source timestamp
- fact type
- allowed staleness policy
- Date-ID existence validation
- Date-ID stale validation
- `reasons[].date_id` validation
- validation failure event 기록 기반

이 Phase에서는 실제 FRED/DART/yfinance/news 호출은 구현하지 않는다.

### Phase 6 — Data API Read-only Adapters [DONE]

외부 데이터를 read-only로 수집하는 adapter 계층을 만든다.

목표:

- yfinance read-only adapter
- FRED read-only adapter
- DART read-only adapter
- fake client 기반 unit test
- fetched data → Date-ID source record 변환
- source timestamp 저장
- stale validation과 연결

뉴스 API는 이 단계 이후로 보류할 수 있다. Unit test에서 실제 외부 네트워크 호출은 금지한다.

### Phase 7 — Scout Input Builder / ScoutSummary Schema [DONE]

Date-ID source layer의 데이터를 Scout 입력으로 조립하고 ScoutSummary JSON schema를 검증한다.

목표:

- Scout input builder
- ScoutSummary schema
- positive / negative / neutral factor structure
- `summary_one_liner`
- Date-ID 근거 필수화
- Date-ID existence/stale validation 연동

이 Phase에서는 실제 매매 판단, Allocator, RiskFilter, OrderIntent 생성은 하지 않는다.

### Phase 8 — Allocator Decision Schema + Validator [DONE]

자산군 비중 결정을 위한 Allocator schema와 validator를 구현한다.

목표:

- AllocatorDecision
- Signal Summary
- Cash Manager
- Asset Allocator
- Consistency Checker
- `cash_policy.cash_target_percent`
- `target_weights`
  - 현금 제외 운용 자산 기준
  - KR/US/Gold 합계 100
- 금 비중 룰
  - 평상시 18~22
  - 예외 15~25
- action / adjust_percent sign consistency
- Date-ID 근거 검증
- invalid output은 부분 채택하지 않고 전체 폐기

### Phase 9 — Analysis Decision Schema + Validator [DONE]

종목 분석 4역할 JSON schema와 validator를 구현한다.

목표:

- AnalysisDecision
- Bear perspective
- Bull perspective
- Risk Manager evaluation
- Fund Manager decision
- BUY / SELL / HOLD action
- weight percent
- `summary_one_liner`
- Date-ID 근거 검증

이 Phase에서는 Analysis 결과가 broker를 직접 호출하지 않는다.

### Phase 10 — RiskFilter + OrderIntent Generation [DONE]

검증된 Allocator/Analysis 결과를 Python hard filter로 검증하고 OrderIntent를 생성한다.

목표:

- 단일 종목 누적 매수 원금 기준 5%
- 운용 비중 70~90%
- paper observation mode 하한 완화 config
- 현금 10~30%
- 자산군 소프트 밴드
- MDD kill switch
- 방향성 슬리피지
- 금 매매 빈도 제한
- LLM confidence를 MVP hard filter로 사용하지 않음
- validated decision → OrderIntent generation

### Phase 11 — Paper E2E Loop [DONE]

LLM 판단부터 PaperBroker 체결까지 replay 가능한 paper trading loop를 연결한다.

목표:

- ScoutSummary / AllocatorDecision / AnalysisDecision loading
- validation result
- DecisionSnapshot 저장
- RiskFilter
- OrderIntent generation
- PaperBrokerAdapter execution
- order/fill/cash/position/nav persistence
- replay test
  - same input → same validation_result
  - same input → same OrderIntent
  - same broker input → same paper ledger effect

### Phase 12 — Logs / DailySummary / Debug Events [DONE]

운영 로그와 기술 이벤트를 replay 가능하게 기록한다.

목표:

- DailySummary
- DebugEvent
- Debug.md writer
- technical event / operational event 분리
- replayable event log
- Debug event code는 Postmortem error_tags와 분리

### Phase 13 — Postmortem [DONE]

국장/미장 weekly/monthly postmortem과 error tag 집계를 구현한다.

목표:

- WeeklyPostmortem KR
- WeeklyPostmortem US
- MonthlyPostmortem KR
- MonthlyPostmortem US
- Postmortem error_tags
- Top 3 Error Tags는 Postmortem 태그만 집계
- Debug.md는 Top 3 집계에서 제외

### Phase 14 — KIS Live Read-only / Tiny-live Rehearsal [DONE]

실전 계좌 연결은 read-only 검증부터 시작한다.

목표:

- KIS live read-only adapter
- access token
- balance inquiry
- current price inquiry
- orderbook inquiry
- ISA account support check
- `allow_live_trading=false` 상태에서 read-only 검증
- tiny-live는 실전 전환 직전 극소액 수동 승인으로만 수행

금지:

- KIS mock adapter 재도입 금지
- KIS 모의투자를 장기 paper ledger로 사용 금지
- ISA smoke test 전 ISA 자동 주문 금지
- live order endpoint 자동 호출 금지

### Phase 15 — Emergency Triggers [NEXT]

정기 분석 외 긴급 트리거를 구현한다.

목표:

- STOCK_DROP
- INDEX_CRASH
- PORTFOLIO_LOSS
- PROFIT_RUN
- TriggerPayload schema
- throttling rule
- emergency Scout context
- MDD_KILLSWITCH는 Python rule-based로 처리

### Phase 16 — Long Paper Trading Review / Parameter Review

장기 paper trading 데이터를 기반으로 시스템 파라미터를 검토한다.

목표:

- 3~6개월 paper result review
- MDD threshold review
- execution model review
- asset band review
- Allocator tolerance review
- live transition readiness review

> 본 시스템은 **"버티는 투자"** 를 철학으로 한다. 공격적 수익 추구보다 **하방 경직성 확보**에 무게 중심을 두며, 시장이 주는 평균 수익을 안전하게 획득하는 것을 목표로 한다.
