"""Offline paper fast-loop composition (RTM-7c.4a).

This package wires the offline fast-loop execution stack from validated, on-disk
inputs and provides read-only operator inspection helpers. It performs no network
I/O, reads no credentials, and never starts a live runtime. See
``docs/PAPER_FAST_LOOP_COMPOSITION_CONTRACT.md``.
"""
