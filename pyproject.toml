[project]
name = "autostock"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.13.4",
    "yfinance>=1.4.1",
    "websockets>=15.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
